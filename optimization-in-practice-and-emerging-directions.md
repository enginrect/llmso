# 실전 LLM 최적화와 서빙의 새로운 방향

## Part 1. 최적화 계획 — 무엇을 최적화할지부터 정한다

### 목표를 숫자로 고정한다

이번 범위는 앞에서 나눠 배운 기법들을 하나의 워크플로로 묶는다. 대상은
**Qwen3-14B + vLLM**, 목표는 **온라인 서빙에서 단일 인스턴스의 토큰 처리량
최대화**다.

처리량을 목표로 삼는 이유는 과금 구조에 있다. 대부분의 LLM 서비스가 처리한
토큰 수로 과금하므로, 같은 GPU에서 처리량이 3배면 **토큰당 비용이 1/3**이 된다.
최적화를 하지 않으면 더 비싼 GPU를 써야 한다는 뜻이기도 하다.

### 처리량과 지연시간은 항상 같이 가지 않는다

많은 기법은 둘을 동시에 개선한다(불필요한 연산 제거, 배칭 효율화). 그러나 특정
트래픽에서는 상충한다.

- 처리량을 올리려면 요청을 **배칭·큐잉**해 하드웨어 활용도를 높인다 → 대기가
  생겨 요청당 지연시간이 늘어난다
- 지연시간만 좇으면 도착 즉시 낮은 병렬성으로 처리한다 → 자원이 놀아 전체
  처리량이 떨어진다

실무 기본값은 **"지연시간을 허용 범위 안에 두고 처리량을 우선"**이다. 사용자
경험을 해치지 않는 선에서 효율을 가져간다.

### 8단계 실행 계획

```
1. 하드웨어 점검          → 이후 모든 해석의 기준
2. 벤치마크 트래픽 생성    → 무엇으로 잴지 확정
3. 평가 지표 정의          → 비교 기준 사전 확정
4. 서빙 서비스 구성        → 메모리 배분 확인
5. 베이스라인 측정         → 튜닝 전 기록
6. 양자화 모델 측정        → 1순위 최적화
7. 워크로드별 기법 적용    → 트래픽 성격에 맞춰
8. 분산 서빙 측정          → 멀티 GPU로 확장
```

SLO를 만족하지 못하면 병목을 다시 분석하고 2번으로 돌아간다. 한 번에 끝나는
절차가 아니라 루프다.

### 하드웨어 점검이 1단계인 이유

GPU 세대마다 **텐서 코어의 양자화 지원**과 **FlashAttention 지원 세대**가 다르다.
같은 vLLM 옵션이 다른 카드에서는 동작하지 않거나 오히려 느려질 수 있어, 스펙을
모르면 측정값을 해석할 수 없다.

`nvidia-smi`로 확인할 항목은 ① CUDA·드라이버 버전과 프레임워크 호환성
② 성능 상태(P-state) ③ 전력·사용률 ④ 메모리 여유다.

**P-state는 유휴 판정에 쓴다.** P8이면 유휴, P0/P1이면 최고 속도로 동작 중이다.
직전 작업이 메모리를 붙잡고 있는 상태에서 측정하면 의미가 없으므로, 시작 전
유휴 확인이 필요하다. 스터디에서 공유된 데스크탑 실측에서는 유휴 시 P8이었다가
벤치마크를 돌리자 P2로 떨어지는 것이 관찰됐다.

전력과 사용률은 **같이 봐야 진단이 갈린다**.

| 관측 | 해석 |
|---|---|
| 부하가 있는데 사용률이 낮다 | 배칭·스케줄링 비효율 |
| 전력은 높은데 처리량이 낮다 | 메모리 병목 또는 커널 비효율 |

---

## Part 2. 무엇으로 재는가 — 트래픽과 지표

### 데이터셋이 최적화 방향을 결정한다

흔한 실수는 아무 프롬프트나 넣고 성능을 재는 것이다. 그러면 실서비스와 다른
결론이 나온다. **긴 입력이 지배적인 프리필 위주 워크로드**와 **긴 출력이
지배적인 디코딩 위주 워크로드**는 요구하는 전략이 서로 다르기 때문이다.

이번 실습은 챗봇 시나리오를 가정하고 상호보완적인 두 데이터셋을 쓴다.

| 데이터셋 | 성격 | 무엇을 재나 |
|---|---|---|
| **ShareGPT** | 실제 대화 로그 | 대화형 서빙 효율과 응답성 |
| **Prefix Repetition** | 합성(공통 접두사 + 고유 접미사) | 캐시 재사용, 반복 패턴 처리 |

Prefix Repetition은 고유 접두사 개수를 줄일수록 캐시 히트가 높은 트래픽이 된다
— 캐시 효과를 인위적으로 강하게 만들 수 있는 손잡이다.

### 재기 전에 무엇을 보낼지 먼저 본다

`inspect_dataset.py`는 추론을 돌리지 않는다. vLLM 벤치마크와 **같은 코드 경로로**
데이터셋을 로드해 길이 분포와 샘플을 보여줄 뿐이다. "내가 지금 서버에 어떤
트래픽을 보내려는가"를 벤치 전에 확정하는 도구다.

ShareGPT 100개 샘플 통계:

| | 최소 | 최대 | 평균 | 중앙값 | 표준편차 |
|---|---|---|---|---|---|
| 프롬프트 | 5 | 817 | 232.60 | 141.50 | 241.42 |
| 출력 | 4 | 771 | 220.61 | 164.50 | 210.23 |

- **입출력 비 232.60 : 220.61 ≈ 1.05 : 1** (계산 확인) — 프리필도 디코딩도 한쪽이
  지배하지 않는 균형 트래픽이다
- 다만 **평균이 중앙값보다 크다**(232.6 vs 141.5). 표준편차가 평균을 넘으므로
  분포는 짧은 프롬프트에 몰려 있고 긴 꼬리가 평균을 끌어올린 형태다. 히스토그램도
  5~95토큰 구간에 45개가 몰려 있다

### 지표는 4개로 좁힌다

전체 지표 체계는 5개 범주(처리량 / 지연시간 / 자원 활용도 / 워크로드 특성 /
신뢰성·비용)로 나뉘지만, 비교의 명확성을 위해 4개만 쓴다.

| 구분 | 지표 | 무엇을 대변하나 |
|---|---|---|
| 처리량 | **총 토큰 처리량** (입력+출력)/초 | 시스템 전체 효율 |
| 처리량 | **출력 토큰 처리량** | 디코딩 성능 — 비용을 좌우 |
| 지연시간 | **평균 TTFT** | 프리필 효율, 초기 반응성 |
| 지연시간 | **평균 ITL** | 스트리밍 체감 속도 |

TPOT와 P99 백분위는 이번 실습에서 생략하고 평균값만 쓴다. 두 처리량 지표를
나눠 보는 이유는 뒤에서 드러난다 — **총 처리량이 올라도 출력 처리량은 그대로일
수 있기 때문**이다.

---

## Part 3. 베이스라인과 양자화 — 메모리 재분배가 처리량이 된다

### 베이스라인 로그가 이미 병목을 말해준다

L40S 46GB에 Qwen3-14B를 기본 설정으로 올린 로그다.

```
Model loading took 27.5185 GiB
Available KV cache memory: 11.00 GiB
GPU KV cache size: 72,064 tokens
Maximum concurrency for 40,960 tokens per request: 1.76x
```

- 모델 27.52 + KV 11.00 = **38.52 GiB** (계산 확인)
- 교재는 "모델이 전체 GPU 메모리의 65% 이상"이라고 쓰는데, **27.52 / 46 = 59.8%**다
  (계산 검증). 60% 근처지 65%는 넘지 않는다. 사용 중인 메모리(38.52) 대비로 보면
  71.4%이므로 그 값을 말한 것일 수 있다

### KV 캐시가 처리량을 정하는 인과 사슬

```
KV 캐시 공간 부족
  → 배치 크기 축소 또는 캐시 축출 증가
    → 디코딩 중 재계산 증가
      → GPU 사용 효율 저하 + 메모리 대역폭 낭비
        → 토큰 처리량 감소
```

여기서 스터디가 강조한 실무 지침이 하나 붙는다. **처리하려는 동시성에 비해 KV
캐시 비중이 지나치게 작으면, 그 상태에서 아무리 측정해도 의미 있는 결과가 나오지
않는다.** 최적화 실험을 시작하기 전에 동시성과 메모리 여유의 기준을 먼저 잡아야
한다는 뜻이다.

### 양자화는 왜 1순위 튜닝 포인트인가

AWQ 4비트로 바꾼 뒤의 같은 로그다.

| 항목 | Qwen3-14B | Qwen3-14B-AWQ | 변화 |
|---|---|---|---|
| 모델 가중치 | 27.52 GiB | 9.36 GiB | **66% 절감** |
| KV 캐시 | 11.00 GiB | 29.15 GiB | **2.65배** |
| KV 토큰 | 72,064 | 191,056 | **2.65배** |
| 최대 동시성 | 1.76x | 4.66x | **2.65배** |

세 배수가 정확히 같은 2.65배로 맞는다(계산 확인). 가중치에서 뺀 **18.15 GiB가
그대로 KV 캐시로 이동**했다는 뜻이다.

그 결과 ShareGPT 트래픽에서:

- 총 처리량 **474 → 1,280 TPS (2.70배)**
- 평균 TTFT **103.61 → 59.29 ms (42% 개선)** — 계산 검증

양자화를 1순위로 잡는 이유가 여기 있다. 모델을 작게 만드는 효과는 VRAM 절약에서
끝나지 않고 **KV 캐시 여유 → 동시성 → 배칭 효율 → 처리량**으로 이어진다. 다만 이
실험의 AWQ는 가중치 양자화라 데이터 이동·메모리 절감에 초점이 있고, 연산량 자체를
줄이려면 활성화 양자화가 필요하다.

### 같은 모델, 다른 트래픽 — 2.37배는 어디서 왔나

모델·설정을 바꾸지 않고 트래픽만 Prefix Repetition으로 바꾼 결과다.

| 지표 | ShareGPT | Prefix Repetition |
|---|---|---|
| 총 토큰 처리량 | 474.38 | **1,123.13 (2.37배)** |
| 출력 토큰 처리량 | 227.64 | 223.31 |
| 평균 TTFT | 104.15 ms | 104.64 ms |
| 평균 ITL | 43.24 ms | 43.95 ms |

**총 처리량만 2.37배가 되고 나머지 셋은 사실상 그대로다.** 두 지표를 나눠 둔
덕분에 원인을 분해할 수 있다 (아래는 직접 계산한 값):

```
ShareGPT        총 474.38 − 출력 227.64 = 입력 246.74 tok/s
Prefix Rep.     총 1123.13 − 출력 223.31 = 입력 899.82 tok/s
                                            ↑ 3.65배
```

- 출력 처리량은 **227.64 → 223.31로 오히려 1.9% 낮다.** 토큰을 만들어내는 속도는
  전혀 빨라지지 않았다
- 늘어난 것은 **단위 시간에 통과시킨 입력 토큰 수**뿐이다. prefix caching이 공통
  접두사의 prefill을 건너뛰므로, 계산하지 않은 토큰까지 "처리한 토큰"으로 집계되어
  총 처리량 지표에 그대로 반영된다 (해석)
- 즉 이 2.37배는 **GPU가 더 빨리 계산해서가 아니라 덜 계산해서** 나온 값이다

**비교 조건에 주의할 점**: 두 벤치는 요청률(10 vs 5 req/s)과 프롬프트 길이(평균
232.6 vs 고정 512)가 다르다. 동시성 상한 10만 같다. 처리량 비교는 성립하지만
TTFT·ITL을 나란히 두고 우열을 말하기는 어렵다.

### 작은 카드에서도 방향은 같다

스터디에서 공유된 16GB 카드 실측(Qwen3-4B 계열, `--max-model-len 8192` 고정)이다.

| 지표 | 베이스(bf16) | AWQ(4bit) |
|---|---|---|
| 모델 가중치 | 7.56 GiB | 2.5 GiB (**66% 절감**) |
| KV 토큰 | 34,512 | 76,064 (**2.2배**) |
| 최대 동시성 | 4.21x | 9.29x (**2.2배**) |
| ShareGPT 총 처리량 | 1,178 | 2,107 (**+79%**) |
| Prefix Rep. 총 처리량 | 2,733 | 3,140 (+13%) |

- 가중치 절감률(66%)과 KV 증가배(2.2배)가 46GB 카드와 같은 구조로 나타난다
  (계산 확인)
- **AWQ 쪽에서 캐싱 이득이 줄어든다**: 베이스는 ShareGPT 대비 Prefix가 2.32배인데
  AWQ는 1.49배다. 이미 양자화로 처리량을 크게 끌어올린 상태라 캐싱이 더 밀어줄
  여지가 작아진 것으로 보인다 (해석)

### 직접 실측 — 4090 24GB, Qwen3-8B (RTX 4090, WSL2)

같은 워크플로를 24GB 카드에서 재현했다
([실습 기록](labs/optimization-in-practice-and-emerging-directions.md)).

| | Qwen3-8B | Qwen3-8B-AWQ | 배수 |
|---|---|---|---|
| 모델 가중치 | 15.27 GiB | 5.71 GiB | 0.37x |
| KV 캐시 | 5.87 GiB | 14.43 GiB | **2.46x** |
| KV 토큰 | 42,768 | 105,056 | **2.46x** |
| 최대 동시성 | 10.44x | 25.65x | **2.46x** |

- **KV·토큰·동시성 세 배수가 여기서도 정확히 일치한다.** 46GB 카드의 2.65배와 카드가
  달라도 같은 구조다
- 다만 **가중치에서 뺀 9.56 GiB 중 KV로 돌아온 것은 8.56 GiB**다. 나머지 약 1 GiB는
  AWQ 쪽 활성화·CUDA graph 예약분이 더 크기 때문이다(예산 22.1 GiB 기준 base 0.96 vs
  AWQ 1.96 GiB). "가중치 절감분이 그대로 KV로 간다"는 것은 근사이지 등식이 아니다
- 교재 수치 재계산: "모델이 GPU 메모리의 65% 이상"은 여기서 15.27 / 23.99 = **63.6%**로
  교재 사례(59.8%)보다 높게 나왔다. 카드가 작을수록 이 비중이 커진다

### 메모리를 너무 많이 잡아도 측정이 깨진다

베이스 측정의 꼬리가 이상했다 — 평균 TPOT 40.0ms인데 중앙값 25.2ms, P99 152ms,
P99 TTFT 4.1초다. 모니터링에서 같은 구간의 **PCIe TX가 평균 3.2 GB/s**로 잡혀
(AWQ 구간은 39 MB/s) 원인을 추적했다.

| 구성 | VRAM 점유 | Prefix 출력 TPS | TPOT (P99) | PCIe TX 평균 |
|---|---|---|---|---|
| base `--gpu-memory-utilization 0.90` | 23,991 MiB (97.9%) | 345.9 | 27.8ms (31.5) | **1,194 MB/s** |
| base `0.85` | 22,999 MiB (93.8%) | **421.2 (1.22x)** | 22.7ms (23.7) | 33 MB/s |
| AWQ `0.85` | 23,091 MiB | 510.7 | 9.7ms (11.4) | 31 MB/s |

- **KV를 20% 줄인 0.85 쪽이 오히려 출력 +22%, TPOT −18%다.** 0.90에서 VRAM이 97.9%까지
  차면 PCIe로 페이지가 오가고(버스트 5~10 GB/s, `nvidia-smi dmon` 교차 확인) 그 비용이
  KV를 더 확보한 이득을 넘어선다. Windows WDDM의 Shared Usage는 두 경우 모두 300 MiB대라
  스필 용량이 커지는 게 아니라 **같은 페이지가 반복 이동**하는 형태로 보인다 (해석)
- 그래서 **AWQ 대 베이스의 공정한 배수는 2.25배**다(둘 다 0.85, ShareGPT 출력 1,004.8 vs
  446.6). 0.90에서 측정한 3.94배는 베이스 쪽 손상분이 섞인 값이다
- 앞서 "KV 캐시 비중이 지나치게 작으면 측정이 무의미하다"고 적었는데, 반대 방향도
  성립한다 — **너무 크게 잡아도 비교가 깨진다.** 24GB 카드에서 15 GiB 모델을 0.90으로
  올리는 조합이 그 지점이었다
- 이전 실습([병목과 최적화](serving-bottlenecks-and-optimization.md))에서 원인 미확정으로
  남긴 FP8 고동시성 붕괴도 같은 기전일 가능성이 있다. 그때도 0.9대 설정에 큰 모델이었다
  (미검증)

### 2.37배 분해는 재현되지 않았다

교재 수치에서 "2.37배가 전부 입력 쪽"이었던 구조를 4090에서 확인하려 했는데,
**같은 형태로 재현되지 않았다.**

| 셀 | 총 TPS | 출력 TPS | 입력 TPS |
|---|---|---|---|
| base × ShareGPT | 515.3 | 247.4 | 267.9 |
| AWQ × ShareGPT | 2,032.2 (3.94x) | 976.0 (3.94x) | 1,056.3 (3.94x) |
| base × Prefix Rep. | 1,935.0 | 348.8 | 1,586.2 |
| AWQ × Prefix Rep. | 2,888.7 (1.49x) | 515.7 (1.48x) | 2,373.0 (1.50x) |

입력과 출력이 **같은 비율로** 움직였다. Prefix Repetition의 AWQ는 요청률 5에 묶여
(실측 처리율 4.63 req/s ≈ 상한) 1.48배에서 멈췄다.

여기서 얻은 조건이 하나 붙는다. **처리량을 분해하기 전에 서버가 포화 상태인지부터
봐야 한다.** 교재의 "전부 입력 쪽"은 서버가 포화된 상태의 분해이고, 도착률이 상한인
구간에서는 입력·출력이 함께 움직여 분해가 원인을 가리지 못한다.

---

## Part 4. "빨라졌다"의 원인을 계층으로 좁힌다

앞 파트에서 처리량이 올라간 것은 확인했지만, **왜** 올랐는지는 서빙 지표만으로는
알 수 없다. 프로파일링은 그 인과를 계층으로 내려가며 좁히는 작업이다.

### 세 개의 레이어

| 레이어 | 보는 것 | 도구 |
|---|---|---|
| **서빙** | 처리량, TTFT, ITL, GPU·메모리 활용률 | vLLM 지표, `nvidia-smi` |
| **프레임워크** | 오퍼레이터별 실행 시간(matmul/attention/layernorm), CPU-GPU 데이터 이동, 연산-I/O 오버랩 | PyTorch Profiler |
| **런타임** | 오퍼레이터가 확장된 실제 CUDA 커널 | Nsight Systems / Nsight Compute |

Nsight Systems는 **시스템 전체 타임라인**이고, Nsight Compute는 **단일 커널의
마이크로아키텍처 분석**이다. 역할이 다르므로 순서가 있다.

### 어떤 순서로 내려가나

```
Nsight Systems (전체 타임라인)
 ├─ GPU 활용률이 낮다 → PyTorch Profiler (CPU 모드) → 호스트 측 병목
 └─ GPU는 바쁜데 지연시간이 높다 → PyTorch Profiler (CUDA 모드)
        ├─ 단일 커널이 지배적 → Nsight Compute → 튜닝/교체/퓨전
        └─ 아니다 → launch 오버헤드·오버랩 부재·동기화 → Nsight Systems 재점검
```

증상(서빙 지표)에서 근본 원인(커널)까지 한 번에 뛰지 않고, 각 단계에서 "다음에
어디를 볼지"만 결정한다.

### 스터디에서 공유된 프로파일링 결과

같은 16GB 카드에서 베이스와 AWQ를 각각 프로파일링한 값이다. 여기서 나온 관찰이
Part 3의 인과 해석을 커널 수준에서 뒷받침한다.

**① 총 GPU 커널 실행 시간은 두 모델이 거의 같다 (약 7.1초).** 그런데 벤치 시간은
46.9초 → 21.5초로 2.18배 단축됐다(계산 확인). GPU가 실제로 계산하는 시간은 줄지
않았는데 전체 소요 시간만 줄었다는 뜻이다.

**② 진짜 줄어든 것은 호스트-GPU 동기화 대기다.**

| | 베이스 | AWQ | 배수 |
|---|---|---|---|
| `cudaEventSynchronize` 비중 | 85.2% (25.6초) | 74.6% (11.5초) | — |
| 호출당 평균 대기 | 13.92 ms | 5.67 ms | **2.45배** |
| 벤치 측정 TPOT | 15.3 ms | 7.0 ms | **2.19배** |

동기화 대기 단축(2.45배)과 클라이언트가 측정한 TPOT 개선(2.19배)이 **방향과 크기가
거의 일치**한다(계산 확인, 차이 약 12%). 디코딩 한 스텝이 끝나기를 기다리는 시간
자체가 짧아진 것이 체감 처리량 개선의 실제 경로다.

**③ 커널 구성은 달라지지만 개별 커널의 효율은 같다.** AWQ에서는 4비트 역양자화와
행렬곱을 함께 처리하는 **Marlin GEMM**이 등장한다. 다만 AWQ에서도 bf16 GEMM이 약
35% 남아 있다 — 어텐션 프로젝션이나 `lm_head` 같은 일부 레이어는 원본 정밀도로
남는 것으로 보인다(해석). Nsight Compute로 본 GEMM의 SM 점유율은 bf16과 Marlin이
모두 약 16.7%로 사실상 같았고, KV 캐시 쓰기 커널은 두 구성이 거의 동일했다(7.30 vs
7.85 μs). 가중치만 양자화했으므로 어텐션·캐시 경로가 그대로인 것과 일치한다.

### 직접 재보니 ① 가설이 기각됐다

같은 3계층을 4090에서 돌렸더니 **"커널 시간은 같고 대기만 줄었다"가 성립하지 않았다.**

| 도구 / 조건 | Qwen3-8B | Qwen3-8B-AWQ |
|---|---|---|
| PyTorch Profiler 50 iteration 커널 합 | 1.189s | **0.477s (2.5배 단축)** |
| 같은 구간 최대 커널 | bf16 wmma GEMM 78.7% | Marlin 38.2% + 18.6% |
| nsys `--cuda-graph-trace=node` 커널 합 | 15.141s | **9.459s** |
| `cudaEventSynchronize` 평균 | 19.34ms | 6.60ms (2.9배) |
| 같은 구간 TPOT | 30.64ms | 12.59ms (2.4배) |

**커널 시간 자체가 2.5배 줄었다.** 4비트 Marlin GEMM이 bf16 GEMM을 대체하면서 디코드
구간의 연산이 실제로 빨라진 것이다.

그러면 "커널 시간이 같다"는 관측은 어디서 나왔나. **nsys의 기본
`--cuda-graph-trace=graph` 설정에서는 CUDA graph 재생 안의 커널이 개별 집계되지
않는다.** 같은 오프라인 조건에서 기본값은 8.64 vs 8.50초로 "거의 같게" 보이지만,
`node` 단위로 다시 재면 15.14 vs 9.46초로 갈린다 — 기본 설정이 베이스 쪽 커널 약
6.5초를 숨긴 것이다. GPU 사용률 95%인 58초 벤치에서 커널 합이 8.4초로 나오는 것부터
이미 신호였다.

`cudaEventSynchronize` 단축(2.9배)이 TPOT 개선(2.4배)과 비슷한 것은 맞다. 다만 이
대기는 **GPU가 스텝을 끝내기를 기다리는 시간**이므로, 커널이 빨라진 결과를 보는
창이지 원인이 아니다.

### 직접 재보니 ② 도구가 결론을 바꾼다

- **AWQ에도 f16/bf16 GEMM이 남는다**는 관측은 재현됐다. 서버를 감싼 측정에서 AWQ의
  커널 1위가 cutlass f16 wmma GEMM 45.0%였고 Marlin은 18.9%였다 — 큰 M을 다루는
  **prefill 구간은 여전히 고정밀 GEMM**이고 Marlin은 디코드 GEMM 쪽이다
- **GEMM 점유율 비교는 확인하지 못했다.** WSL2에서 Nsight Compute가 일반 사용자·`sudo`
  모두 `ERR_NVGPUCTRPERM`으로 막힌다. 리눅스의
  `NVreg_RestrictProfilingToAdminUsers=0`에 해당하는 설정이 WSL2에는 없고, Windows 측
  NVIDIA 제어판이나 레지스트리(`RmProfilingAdminOnly`)를 바꾸고 **재부팅**해야 한다

그래서 인과 사슬을 이렇게 고쳐 쓴다. 양자화의 이득은 **메모리 재분배(KV 확보)와 커널
교체(Marlin) 두 갈래**로 오고, 어느 쪽이 얼마인지는 도구 설정을 맞춰야 갈린다. 앞선
"연산은 그대로고 대기만 줄었다"는 결론은 **기본 graph 트레이스가 만든 인상**이었을
가능성이 크다 (해석 — 카드·모델이 달라 직접 비교는 아니다).

### 운영상 제약

- **Nsight Compute는 관리자 수준 권한을 요구**한다. 컨테이너로 제공되는 클라우드
  GPU(Runpod 등)에서는 동작하지 않을 가능성이 크다. Nsight Systems는 될 수도
  있으나 스터디에서도 확인되지 않았다 (미검증)
- 측정은 GPU 노드에서 하고, 결과 파일(`.nsys-rep`)을 받아 로컬 macOS/Windows에
  설치한 Nsight GUI로 여는 방식이 편하다. 별도 가입은 필요 없다
- GPU 프로파일링 카운터는 배타적으로 잡히므로, DCGM 같은 상시 모니터링 에이전트를
  잠시 내려야 하는 경우가 있다. 다만 위 권한 오류는 카운터 충돌이 아니라 권한 문제라
  DCGM을 내려도 그대로였다

### 시계열 모니터링이 프로파일러보다 먼저 잡아낸 것

프로파일러가 사후 분석이라면 상시 모니터링은 벤치 도중의 시계열이다. 두 계층이 서로를
보완한 사례가 이번 실측에 있다.

| 구간 | GPU util | 전력 | PCIe TX 평균 |
|---|---|---|---|
| base / ShareGPT | **98.6%** | **218 W** | **3,233 MB/s** |
| base / Prefix | 96.7% | 286 W | 1,474 MB/s |
| AWQ / ShareGPT | 93.7% | **304 W** | 39 MB/s |
| AWQ / Prefix | 86.9% | 281 W | 36 MB/s |

- **PCIe TX의 80배 차이**(3,233 vs 39 MB/s)가 앞의 메모리 압박을 먼저 드러냈다.
  서빙 지표만 보면 "베이스가 느리다"에서 끝났을 문제다
- **GPU util 98.6%인 베이스가 AWQ(93.7%)보다 전력이 낮다**(218 vs 304 W). util%는
  SM이 점유된 시간의 비율일 뿐 연산량이 아니라는 것이 전력과 나란히 놓았을 때 드러난다
  — 앞선 실습에서 확인한 "util 94%인데 추측 디코딩이 이득"과 같은 성질이다
- **GeForce에서는 DCGM의 PROF(DCP) 필드가 전부 비활성**이다(`metric not enabled`).
  PCIe 대역폭을 보려면 NVML `nvmlDeviceGetPcieThroughput`을 직접 폴링하는 exporter가
  필요하다. NVLink가 없는 환경에서 PCIe는 관찰 대상이 맞다

---

## Part 5. 분산 서빙 — GPU를 늘리면 빨라지는가

### 같은 모델, 반대 결과

동일한 Qwen3-14B-AWQ를 인터커넥트가 다른 두 인스턴스에서 1/2/4 GPU로 측정했다.

| 인스턴스 | GPU | 연결 | 최적 구성 |
|---|---|---|---|
| g6e.12xlarge | L40S ×4 | **PCIe만** | **단일 GPU** (처리량·TTFT 모두) |
| p4d.24xlarge | A100 ×8 | **NVLink** | **4-GPU** (처리량 최고, TTFT 최저) |

단일 GPU 추론에서는 일반적으로 L40S가 A100보다 낫다. 그런데 분산에서는 뒤집힌다.

원인은 GPU 개수가 아니라 **인터커넥트**다. 텐서 병렬화는 레이어마다 GPU 간
activation을 동기화해야 하는데, NVLink에서는 이 통신 비용이 병렬화 이득보다 작다.
PCIe만 있는 g6e에서는 통신 오버헤드가 더 커서 GPU를 늘릴수록 손해다.

### 분산이 항상 이득은 아니다 — 두 층위

**첫째, 인터커넥트가 나쁘면 단일 GPU가 이긴다.** 위의 g6e가 그 경우다.

**둘째, 인터커넥트가 좋아도 처리량만 놓고 보면 복제가 낫다.** p4d에서 4-GPU
텐서 병렬이 처리량 1위였지만, GPU마다 독립 인스턴스를 하나씩 띄우면 그보다 훨씬
높다.

- 교재는 이를 "거의 3배(9,816 vs 3,926 TPS)"라고 쓰는데, **9,816 / 3,926 = 2.50배**다
  (계산 검증). 3배는 아니다
- 여기서 한 걸음 더 계산해보면: **9,816 ÷ 4 = 2,454 TPS/GPU**다. 복제 4개가 정확히
  선형 확장한 수치로 보이며, 그렇다면 TP=4는 단일 GPU 대비 **3,926 / 2,454 = 1.60배**에
  그친다 (추론 — 단일 GPU 처리량이 자료에 없어 역산한 값이다)
- 즉 "4장을 붙였는데 1.6배"와 "4장에 4개를 띄워 4배" 사이의 격차가 2.5배다

### 그래서 분산 서빙은 언제 쓰나

| | 수직 확장 (TP/PP) | 수평 확장 (복제 + LB) |
|---|---|---|
| 구조 | 여러 GPU가 **하나의 모델 인스턴스**를 처리 | GPU마다 **독립 인스턴스** |
| 얻는 것 | **큰 모델 적재**, 요청당 지연시간 단축 | **총 처리량**, 장애 격리, 단순함 |
| 못 얻는 것 | 통신 오버헤드, 복잡도 | 요청 하나의 지연시간은 안 줄어든다 |
| 쓸 때 | 모델이 GPU 1장에 안 들어감 / TTFT를 줄여야 함 | 그 외 대부분의 프로덕션 |

분산 서빙의 고유한 이득은 처리량이 아니라 **지연시간**이다. p4d에서 1→4 GPU로
가며 TTFT가 66 → 33 ms로 50% 줄었는데(계산 확인), 이는 복제를 아무리 늘려도 얻을
수 없다.

스터디에서 공유된 측정에서도 **TP=2가 오히려 느려지는 경우**가 나왔다. 모델이
GPU 1장에 넉넉히 들어가는데 TP를 쓴 상황이다. 같은 2장으로 PP=2 / TP=2 /
라우터+복제 2개를 나란히 재보면 순위가 갈릴 것으로 예상된다 — 라우터+복제가
가장 유리할 것이라는 게 스터디의 예상이다 (미검증).

### 다섯 가지 트레이드오프

| 트레이드오프 | 이번 범위에서 확인된 지점 |
|---|---|
| 처리량 ↔ 지연시간 | 배칭·큐잉이 처리량을 올리고 요청당 지연을 늘린다 |
| 메모리 효율 ↔ 모델 품질 | AWQ가 처리량 2.7배를 냈지만 정확도 손실 가능성은 별도 검토 대상 |
| 하드웨어 활용도 ↔ 유연성 | 특정 GPU에 맞춘 설정은 다른 카드에서 실패할 수 있다 |
| 수직 확장 ↔ 수평 확장 | 위 표 그대로 — 목적이 지연이냐 처리량이냐로 갈린다 |
| 정적 최적화 ↔ 적응형 서빙 | 이 실습 자체가 고정 파라미터 기반의 정적 최적화다 |

### 과최적화의 경계

교재는 튜닝 노브를 두 갈래로 나눈다 — **메모리·캐시 계열**
(`--gpu-memory-utilization`, `--max-model-len`, `--block-size`)과 **배칭·스케줄링
계열**(`--max-num-seqs`, `--max-num-batched-tokens`, `--max-paddings`).

그러면서 과도한 튜닝을 경계한다. 특정 GPU·트래픽에 맞춘 설정은 이식성이 떨어지고,
vLLM 버전이 올라가면 파라미터 자체가 바뀌어 유지보수 부담이 된다.

스터디가 덧붙인 흐름이 이 경고와 맞물린다. **최근 vLLM 옵션에는 `auto` 값이 늘고
있다** — 프레임워크가 하드웨어 스펙을 읽어 적합한 설정을 스스로 고르는 방향이다.
GPU마다 파라미터를 따로 관리하는 것이 현실적으로 어렵기 때문이다. 결국 노력은
"완벽한 값 찾기"보다 **"어떤 기법을 적용할지 판단하기"**에 써야 한다.

---

## Part 6. 서빙 엔진이 떠맡는 새로운 결정들

여기부터는 실습이 아니라 방향 소개다. 공통점이 하나 있다 — 서빙 엔진이 "모델을
빠르게 실행하는 것" 외에 **판단**을 떠맡기 시작했다.

### 시맨틱 캐싱과 라우팅 — 정확 일치에서 의미 일치로

기존 라우팅은 모델 복제본 사이의 로드 밸런싱이었고, 캐싱은 프리픽스가 정확히
일치해야 동작했다. 시맨틱 계층은 **임베딩과 벡터 검색**으로 같은 의도를 인식한다.

"시애틀에서 하와이까지 비행시간이 얼마나 걸리나요?"와 "시애틀에서 하와이까지
비행기로 몇 시간 걸리는지 알려줘"는 prefix 매칭이 되지 않지만 임베딩은 가깝다.
캐시된 응답을 그대로 반환하면 LLM 호출 자체가 사라진다.

라우팅 단위도 **복제본 수준에서 엔드포인트 수준으로** 올라간다.

| 기능 | 하는 일 |
|---|---|
| 시맨틱 캐싱 | 의미가 같은 요청에 캐시된 응답 반환 → 비용·지연 절감 |
| 복잡도 기반 모델 선택 | 경량 인코더로 프롬프트를 판단해 소형/대형/추론 활성화를 선택 |
| SLM 라우팅 | 8B~32B 태스크 특화 모델이 더 나은 경우 그쪽 엔드포인트로 |
| 도구 필터링 | MCP 도구 수십~수백 개 중 관련 도구만 모델에 전달 |
| 정책 집행 | PII 마스킹, 팀별 도구 권한, 예산 한도의 중앙 집행점 |

파이프라인은 ① PII 마스킹(NER 소형 모델 + 정규식) → ② 임베딩 생성과 벡터 검색
(캐시 히트 확인, 도구 후보 축소) → ③ 임베딩 기반 소형 분류기로 최종 모델 결정
순이다.

도구 필터링의 동기는 실용적이다. 사내 MCP 도구가 100개인데 전부 프롬프트에
실으면 토큰·지연·비용이 늘고, **권한이 큰 도구가 잘못 선택될 위험**도 커진다.

**LiteLLM**은 100개 이상 제공사를 OpenAI 형식 하나로 묶는 게이트웨이로, 폴백·LB·
캐싱·가드레일·예산 한도·관찰성을 제공한다. 시맨틱 캐시 계열 기능은
`Auto Routing`(베타)으로 재편됐다. 스터디는 LiteLLM을 **포워드 프록시 성격**
(사내 사용자가 외부 SaaS 모델로 나갈 때 중간에서 통제)으로, vLLM 앞단 라우터를
리버스 프록시 성격으로 구분해 이해하는 관점을 제시했다.

### 멀티모달 — 병목이 CPU로 이동한다

여기서 다루는 것은 **멀티모달 입력을 받아 텍스트를 생성하는 모델**이다. 확산 기반
이미지·영상 생성 모델은 아키텍처가 달라 범위 밖이다.

이미지는 프롬프트 안에서 플레이스홀더 토큰으로 표현되고
(`<|vision_start|>...<|image_pad|>...<|vision_end|>`), 처리 시점에 Vision Encoder가
만든 임베딩으로 대체된다. Encoder는 이미지를 패치로 나눠 LLM 히든 차원으로 투영한
뒤 패치 간 어텐션으로 공간·의미 관계를 인코딩한다.

문제는 시스템 쪽이다. 텍스트 토큰화는 가볍지만 **이미지 전처리(디코딩, 크롭,
리사이즈, 텐서 변환)는 CPU 집약적**이고 LLM 실행 전에 끝나야 한다. 요청률이 높아지면
CPU가 포화돼 **GPU가 유휴 상태로 기다린다** — 병목이 GPU에서 전처리 단계로 옮겨간다.

vLLM의 대응은 **프로세스 분리**다. API 서버와 입력 전처리·출력 후처리를 한
프로세스에, GPU 커널 스케줄링·실행을 다른 프로세스에 두고 비동기로 돌린다. CPU가
무거운 전처리로 바빠도 GPU 커널 실행이 막히지 않는다.

### 엣지 서빙 — 효율이 지표가 된다

동인은 ① 지연시간(로보틱스·자율주행·AR/VR은 클라우드 왕복 20~50ms도 허용 불가)
② 데이터 로컬리티(GDPR·HIPAA) ③ 비용(대역폭·전송료)이다.

클라우드가 성능을 좇는 반면 엣지의 지표는 **와트당 연산량(TOPS/W)**이다. 이를
가능하게 하는 요소는 NPU 같은 저전력 가속기, 모델 압축(여기서는 선택이 아니라
동작의 전제조건), CPU→NPU→GPU 이기종 분업, 발열 기반 열 인지 스케줄링,
엣지-클라우드 하이브리드 오프로딩이다.

이기종 분업은 파이프라인 병렬성과 구조가 비슷하되 **서로 다른 하드웨어에 걸친다**는
점이 다르다.

### Multi-LoRA — 어댑터를 공유한다

LoRA는 원본 가중치를 동결하고 작은 저랭크 레이어만 학습한다. Multi-LoRA 서빙은
**활성 어댑터를 모두 GPU 메모리에 올려두고** 하나의 베이스 모델 인스턴스로 함께
서빙한다. 필요할 때 하나씩 로드하는 방식이 아니다.

서로 다른 어댑터를 쓰는 요청도 연속 배칭으로 묶여야 하므로, 베이스 가중치 연산과
어댑터별 연산을 함께 처리한 뒤 결합하는 **Punica 같은 특화 커널**이 필요하다.

**언제 쓰지 말아야 하나**가 더 중요하다. 어댑터별 트래픽이 이미 하드웨어를
포화시킬 만큼 크다면, 각 어댑터를 베이스에 병합해 독립적으로 수평 확장하는 편이
낫다. Multi-LoRA는 **어댑터별 트래픽이 낮아 개별 서빙으로는 GPU가 남는 경우**를
위한 기법이다.

### 강화학습 — 서빙 엔진이 훈련 루프의 일부가 된다

RLHF 훈련 시간의 약 80%가 **샘플 생성 단계**에 쓰인다는 추정이 있다(OpenRLHF).
현재 정책인 액터 모델을 여러 레플리카로 띄워 후보 응답을 대량 생성하고, 보상
모델이 점수를 매기고, 정책 그래디언트로 액터를 갱신한 뒤 새 가중치를 서빙
레플리카에 다시 동기화하는 루프다. 서빙이 느리면 훈련 워커가 샘플을 기다리며 논다.

여기서는 처리량만큼 **결정론**이 중요하다. 레플리카나 실행 간의 미세한 비결정성이
일관되지 않은 보상 → 불안정한 그래디언트 → 재현 불가능한 훈련으로 누적된다.
배치 형태 변화 같은 구현 세부가 작은 편차를 만들 수 있다는 지적이 있어, 최근 서빙
엔진은 **배치 불변(batch-invariant) 결정론적 추론**도 목표로 삼는다.

### 직접 실측 — 게이트웨이, Multi-LoRA, VLM

Part 6의 세 항목은 4090 1장으로도 재현된다. 개념 설명이 실제로 어떤 비용과 위험을
동반하는지가 여기서 드러난다.

**게이트웨이(LiteLLM)** — 기능은 전부 동작했고 비용은 작다.

| 항목 | 실측 |
|---|---|
| 경유 오버헤드 | 직접 p50 8.8ms → 경유 14.5ms (**+5.7ms**) |
| 폴백 | 백엔드를 죽이자 **2,426ms** 만에 대체 모델이 응답, 복구 후 389ms |
| PII 마스킹 | `<PERSON>` `<EMAIL_ADDRESS>` `<CREDIT_CARD>` `<PHONE_NUMBER>` 4종 치환 |
| 복잡도 라우팅 | "hi" → 0.5B(75ms), 다단계 설계 질문 → 1.5B(324ms) |
| 지연 기반 라우팅 | 30회 중 **29:1**로 빠른 쪽에 쏠림 |

지연 기반 라우팅의 29:1 쏠림은 로드 밸런싱으로 기대하는 분산과 다르다. 전략 이름만
보고 고르면 안 되고 분포를 직접 세야 한다.

**시맨틱 캐시는 오답을 낸다** — 이번 실습에서 가장 중요한 발견이다.

| 순서 | 프롬프트 | 경로 | 유사도 | 응답 |
|---|---|---|---|---|
| 1 | What is the capital city of France? | upstream | — | Paris ✓ |
| 2 | Which city is the capital of France? | **캐시** | 0.939 | Paris ✓ |
| 3 | What is the capital of **Germany**? | **캐시** | 0.937 | **Paris ✗** |

- 문턱을 0.85에서 **0.95로 올려도** 사라지지 않았다. Germany는 미스가 됐지만 이번엔
  Italy가 Germany 항목에 0.969로 붙어 "Berlin"을 받았다. **"What is the capital of X?"
  라는 틀이 임베딩을 지배**해 X가 달라도 거리가 가깝다
- 히트는 9~83ms로 빠르지만, **미스일 때 임베딩 룩업 73ms가 그대로 오버헤드**로 남는다
- 부수 효과로 **캐시가 백엔드 장애를 가렸다.** 백엔드를 죽여도 캐시가 답하는 바람에
  페일오버 경로가 측정되지 않았다 — 장애를 못 느끼는 대신 오답을 받는다

의미 기반 캐싱은 "히트율이 올라간다"는 이득만 있는 것이 아니라 **정확도를 거래한다.**
사실 질의처럼 표면이 비슷하고 답이 다른 도메인에서는 문턱 조정으로 해결되지 않는다.

**Multi-LoRA는 상시 비용이 있다** — 어댑터를 쓰지 않아도 지불한다.

| 서버 | 요청 | 출력 TPS | TPOT | KV 토큰 |
|---|---|---|---|---|
| LoRA 비활성 | base | 609.0 | 22.65ms | 42,768 |
| `--enable-lora` | base만 | 565.9 (**−7.1%**) | 22.28ms | 33,712 (**−21%**) |
| `--enable-lora` | 어댑터 2종 혼합 | 572.8 | 23.96ms (**+5.8%**) | 33,712 |

`--enable-lora`를 켜는 것만으로 **KV가 21% 줄고 출력이 7% 떨어진다.** 어댑터를 섞어
보내는 추가 비용은 TPOT +5.8%로 그보다 작다. 앞서 정리한 "어댑터별 트래픽이 낮을 때만
이득"이라는 조건이 이 상시 비용에서 나온다.

**멀티모달의 CPU 병목은 조건부다.**

| 입력 (요청마다 새 이미지) | TTFT | GPU util |
|---|---|---|
| 텍스트 전용 | 26.4ms | 97.5% |
| 448px | 82.0ms (3.1x) | 90.5% |
| 896px | 204.5ms (7.7x) | 91.1% |
| 1792px | **859.0ms (32.5x)** | 89.5% |

- 해상도를 올리면 TTFT가 텍스트의 **32.5배**까지 벌어지고 **GPU util은 97 → 90%로
  떨어진다.** 전처리가 CPU 1코어 남짓(1.1~1.4코어)에 묶여 GPU를 놀리는 형태다 —
  "병목이 GPU에서 CPU 전처리로 이동한다"는 서술이 그대로 관측된다
- 다만 **같은 이미지를 반복하면 이 병목이 사라진다.** 멀티모달 프로세서 캐시와 prefix
  캐시가 전처리와 prefill을 모두 흡수해 해상도를 16배 올려도 TTFT가 27~44ms에 머물렀다
- 즉 CPU 병목은 **요청마다 이미지가 다를 때** 나타난다. 벤치마크에서 같은 이미지를
  재사용하면 문제 자체가 보이지 않는다

### 공통점

| 사례 | 서빙 엔진이 새로 떠맡은 결정 |
|---|---|
| 시맨틱 라우터 | 어떤 모델·도구를 쓸 것인가 |
| Multi-LoRA | 어떤 어댑터를 적용할 것인가 |
| 엣지-클라우드 하이브리드 | 어디서 실행할 것인가 |
| RL 서빙 | 재현 가능한 출력을 보장할 것인가 |

추론 전용 실행기였을 때는 없던 요구사항(정책 집행, 결정론)이 여기서 생긴다.

---

## 스스로 답해본 질문

**Q1. 처리량이 올랐다는 건 GPU가 더 빨리 계산해서인가, 덜 계산해서인가?**

교재 수치만 보면 "덜 계산해서"로 읽히는데, **직접 재보니 둘 다였다.**

교재 쪽 근거는 이렇다. Prefix Repetition의 2.37배에서 출력 처리량은 227.64 → 223.31로
오히려 낮고, 늘어난 것은 입력 토큰 통과량(246.74 → 899.82)뿐이다. AWQ의 2.7배도
18.15 GiB가 KV로 이동해 동시성이 2.65배가 된 결과다.

그런데 4090 실측에서 두 가지가 갈렸다.

- **분해 구조가 재현되지 않았다.** 도착률(요청률 5)이 상한인 구간에서는 입력·출력이
  같은 비율로 움직인다(1.50x / 1.48x). "전부 입력 쪽"은 **서버가 포화된 상태**의
  분해다 — 분해하기 전에 포화 여부를 먼저 봐야 한다
- **커널 시간도 실제로 줄었다.** 50 iteration 커널 합 1.189 → 0.477초(2.5배)로,
  bf16 GEMM이 4비트 Marlin GEMM으로 바뀌면서 연산 자체가 빨라졌다. "커널 시간은
  같다"는 관측은 nsys 기본 graph 트레이스가 베이스 쪽 커널 6.5초를 숨긴 결과였다

그래서 답은 **"덜 계산한 것"과 "빨리 계산한 것"이 겹쳐 있다**이고, 둘을 가르려면
① 총/출력 처리량 분해 ② 포화 여부 확인 ③ 프로파일러 설정 확인이 모두 필요하다.
하나라도 빠지면 원인을 잘못 짚는다.

**Q2. GPU를 더 붙이면 무조건 더 빠른가?**

아니다. 두 층위에서 아니다.

① 인터커넥트가 PCIe뿐이면 GPU를 늘릴수록 통신 오버헤드가 병렬화 이득을 넘어선다
(g6e에서 단일 GPU가 최적). ② NVLink가 있어도, 처리량만 목표라면 텐서 병렬보다
독립 복제가 2.5배 유리하다.

분산 서빙의 고유한 이득은 **모델 적재 한계 극복과 지연시간 단축**(TTFT 66 → 33ms)
이다. 이건 복제로는 얻을 수 없다. 질문을 "GPU를 늘릴까"가 아니라 "지연시간이
문제인가, 처리량이 문제인가"로 바꿔야 답이 나온다.

**Q3. 여기서 찾은 "최적 설정"은 다른 환경에서도 최적인가?**

아니다. 그리고 그 사실이 이번 범위에서 세 번 반복된다 — 서두의 "최적화는 움직이는
목표", 중간의 과최적화 경고, 결론부의 "이 실습 자체가 정적 최적화였다"는 자인이다.

같은 모델·같은 설정인데 인터커넥트만 달라도 최적 GPU 수가 뒤집혔고, 같은 양자화가
46GB 카드에서는 KV 2.65배, 16GB 카드에서는 2.2배로 다르게 나타났다. 배워야 할
것은 수치가 아니라 **측정·해석 방법론**이다.

**Q4. 병목이 GPU가 아닐 때 어떻게 진단하고 대응하는가?**

진단은 계층을 내려가며 좁힌다. Nsight Systems로 "GPU가 바쁜가"부터 판정하고,
바쁘지 않으면 PyTorch Profiler CPU 모드로 호스트 측을, 바쁜데 느리면 CUDA 모드로
지배적 오퍼레이터를, 단일 커널이 지배하면 Nsight Compute로 내려간다.

대응 패턴은 반복적으로 같다 — **크리티컬 패스에서 CPU 작업을 빼내 GPU 실행과
비동기로 돌린다.** 멀티모달 전처리 병목에 대한 vLLM의 프로세스 분리와, "커널 사이
유휴 gap을 스트림 오버랩으로 메운다"는 처방이 같은 구조다.

실측에서 배운 것은 **계층을 내려가기 전에 시계열을 먼저 보라**는 쪽이다. 이번에
베이스 측정이 손상된 것은 프로파일러가 아니라 **PCIe TX 3.2 GB/s라는 상시 모니터링
지표**가 먼저 잡았다. 서빙 지표(P99 TPOT가 평균의 4배)는 이상을 알려주지만 원인을
가리키지 못했고, GPU util은 오히려 98.6%로 정상처럼 보였다.

또 **도구 설정 자체가 병목 판정을 바꾼다.** nsys 기본값에서는 커널이 숨겨져 "GPU는
안 바쁜데 대기가 길다"로 읽히고, `--cuda-graph-trace=node`로 바꾸면 정반대가 된다.
병목은 이동할 뿐 아니라 **측정 설정에 따라 다르게 보인다.**

**Q5. 최적화 기법을 적용하지 말아야 할 때의 기준은?**

기법마다 역효과 조건이 명시된다. 흥미로운 것은 **"트래픽 패턴"이라는 하나의 변수가
정반대 결정을 만든다**는 점이다.

| 기법 | 안 쓰는 조건 |
|---|---|
| Multi-LoRA | 어댑터별 트래픽이 이미 GPU를 포화시킴 → 병합 후 독립 서빙 |
| 시맨틱 캐싱 | 반복·유사 질의가 없음 → 임베딩·검색 오버헤드만 남음 |
| 분산 서빙(TP) | 모델이 1장에 들어가고 인터커넥트가 느림 → 복제가 낫다 |
| 열 인지 스케줄링 | 최대 성능이 안정성보다 우선인 환경 |

Multi-LoRA는 트래픽이 **적어야** 공유가 이득이고, 시맨틱 캐싱은 유사 트래픽이
**많아야** 이득이다. 그래서 "이 기법이 좋은가"가 아니라 "내 트래픽이 이 기법의
전제를 만족하는가"를 먼저 물어야 한다.

**Q6. "서빙 엔진의 역할이 확장된다"는 말의 근거는?**

Part 6의 네 사례가 각각 다른 종류의 결정을 엔진 쪽으로 옮긴다 — 모델·도구 선택,
어댑터 선택, 실행 위치 선택, 출력 재현성 보장이다.

이전 세대 추론 서버에는 없던 요구사항이 여기서 생긴다. 정책 집행(PII 마스킹, 도구
권한, 예산 한도)은 보안·거버넌스 요구고, 결정론은 훈련 파이프라인의 요구다. 둘 다
"모델을 빠르게 실행한다"는 목표에서는 도출되지 않는다.

---

## 정리

이번 범위는 **"측정하고 해석하는 방법"** 자체가 내용이었고, 직접 재보니 그 방법 쪽에서
더 많이 배웠다.

```
목표를 숫자로 고정한다 (처리량 우선, 지연은 허용 범위)
  → 실제 트래픽을 닮은 데이터셋을 고른다 (데이터셋이 최적화 방향을 정한다)
    → 지표를 사전에 확정한다 (총/출력 처리량을 나눠야 원인이 보인다)
      → 베이스라인을 남긴다 (메모리 배분 로그가 이미 병목을 말한다)
        → 양자화부터 적용한다 (메모리 재분배 + 커널 교체 두 갈래로 온다)
          → 프로파일링으로 원인을 좁힌다 (도구 설정이 결론을 바꾼다)
            → 확장은 목적에 따라 다르다 (지연이면 TP, 처리량이면 복제)
```

확인된 사실 위주로 추리면:

1. **메모리 배분이 처리량을 정한다** — 가중치를 줄이면 KV 캐시·KV 토큰·최대 동시성이
   **같은 배수로** 함께 움직인다. 교재(L40S 46GB)는 2.65배, 4090 24GB 실측은 2.46배로
   카드가 달라도 구조가 같다 (계산 확인). 다만 가중치 절감분이 전부 KV로 가지는 않는다
   — 9.56 GiB 중 8.56 GiB만 돌아왔고 나머지는 활성화·graph 예약분이다
2. **메모리는 너무 적게 잡아도, 너무 많이 잡아도 측정이 깨진다** — 24GB 카드에서 15 GiB
   모델을 `--gpu-memory-utilization 0.90`으로 올리면 VRAM 97.9% 포화에서 PCIe 페이지
   이동(평균 3.2 GB/s)이 생겨 출력 처리량이 18~22% 깎인다. KV를 20% 줄인 0.85 쪽이
   오히려 빠르다. 공정한 AWQ 배수는 3.94배가 아니라 **2.25배**였다
3. **"덜 계산한 것"과 "빨리 계산한 것"이 겹쳐 있다** — 교재 수치에서는 처리량 증가가
   전부 입력 쪽이었지만, 도착률이 상한인 부하에서는 입력·출력이 같은 비율로 움직여
   재현되지 않았다. 동시에 커널 시간 자체도 2.5배 줄었다(bf16 GEMM → 4비트 Marlin).
   분해는 **서버가 포화 상태일 때만** 원인을 가린다
4. **도구 설정이 병목 판정을 바꾼다** — nsys 기본 `--cuda-graph-trace=graph`는 CUDA
   graph 안의 커널을 개별 집계하지 않아 베이스 쪽 커널 약 6.5초를 숨긴다. 기본값에서는
   "커널 시간은 같고 대기만 줄었다"로 읽히고, `node` 단위로 바꾸면 15.14 vs 9.46초로
   갈린다. Nsight Compute는 WSL2에서 권한 벽에 막혀 이 층까지는 내려가지 못했다
5. **시계열 모니터링이 프로파일러보다 먼저 원인을 짚었다** — PCIe TX 80배 차이가 2번의
   출발점이었다. 같은 구간에서 GPU util 98.6%인 베이스가 AWQ보다 전력이 낮았다
   (218 vs 304 W) — util%는 연산량이 아니다. GeForce에서는 DCGM PROF 필드가 전부
   비활성이라 NVML 직접 폴링이 필요하다
6. **분산은 GPU 개수가 아니라 인터커넥트가 정한다** — PCIe만 있으면 단일 GPU가 최적이고,
   NVLink가 있어도 처리량만 보면 복제가 2.50배 유리하다. 분산의 고유 이득은 TTFT 50%
   단축이다 (교재의 "거의 3배"는 계산상 2.50배, "모델이 메모리의 65% 이상"은 59.8%)
7. **시맨틱 캐시는 히트율을 정확도와 거래한다** — 문턱 0.85·0.95 모두에서 "capital of
   Germany/Italy"가 이전 답(Paris/Berlin)을 받았다. "What is the capital of X?"라는 틀이
   임베딩을 지배해 문턱 조정으로 해결되지 않는다. 캐시가 백엔드 장애까지 가려 페일오버가
   측정되지 않았다
8. **적용하지 말아야 할 조건이 기법의 일부다** — `--enable-lora`는 어댑터를 쓰지 않아도
   KV 21%·출력 7%를 상시로 지불한다. 멀티모달 CPU 병목은 요청마다 이미지가 다를 때만
   나타나고(1792px TTFT 32.5배), 같은 이미지를 반복하면 캐시가 흡수해 보이지 않는다

---

## 이어서 볼 것

- **Nsight Compute까지 내려가기** — WSL2 권한 벽(`ERR_NVGPUCTRPERM`)을 Windows 측
  설정으로 풀고 GEMM 점유율·메모리 스톨을 직접 확인. 이번에 남은 유일한 미확인 층이다
- **`auto` 파라미터의 실제 동작** — vLLM이 하드웨어를 읽고 무엇을 어떻게 정하는지
  코드로 확인. 특히 `--gpu-memory-utilization`을 자동으로 정해주는지
- **활성화 양자화** — 이번에 확인한 것은 가중치 양자화다. Marlin 커널로 디코드 GEMM은
  바뀌었지만 prefill은 여전히 f16 GEMM이었다
- **시맨틱 캐시의 오탐 억제** — 문턱만으로 안 되는 것을 확인했으니, 재순위(rerank)나
  질의 정규화 같은 2차 검증이 실제로 듣는지
- **llm-d와 KServe** — 캐시 인지 라우팅과 PD 분리의 K8s 구현. 스터디 후반 예정
- **Diffusion 언어 모델** — 자기회귀 디코더와 다른 생성 구조
- **배치 불변 결정론적 추론** — 같은 입력에 같은 출력을 보장하는 구현 조건

---

## 참고 자료

**교재**
- Hands-On LLM Serving and Optimization (2026)

**벤치마크·데이터셋**
- vLLM `bench serve` 문서: https://docs.vllm.ai/en/latest/cli/bench/serve.html
- ShareGPT 데이터셋: https://huggingface.co/datasets/anon8231489123/ShareGPT_Vicuna_unfiltered

**프로파일링**
- NVIDIA Nsight Systems: https://developer.nvidia.com/nsight-systems
- NVIDIA Nsight Compute: https://developer.nvidia.com/nsight-compute
- PyTorch Profiler 레시피: https://docs.pytorch.org/tutorials/recipes/recipes/profiler_recipe.html
- vLLM Profiling 가이드: https://docs.vllm.ai/en/latest/contributing/profiling.html

**모니터링**
- DCGM Exporter: https://github.com/NVIDIA/dcgm-exporter
- node_exporter: https://github.com/prometheus/node_exporter

**시맨틱 라우팅·게이트웨이**
- vLLM Semantic Router: https://github.com/vllm-project/semantic-router
- vLLM Production Stack: https://github.com/vllm-project/production-stack
- LiteLLM 라우팅·로드밸런싱: https://docs.litellm.ai/docs/routing-load-balancing
- LiteLLM 아키텍처: https://docs.litellm.ai/docs/proxy/architecture
- Model Context Protocol: https://modelcontextprotocol.io/

**멀티모달**
- vLLM Multimodal Inputs: https://docs.vllm.ai/en/latest/features/multimodal_inputs.html

**엣지 서빙**
- LiteRT (TensorFlow Lite 후속): https://ai.google.dev/edge/litert
- ONNX Runtime: https://onnxruntime.ai/
- ExecuTorch: https://docs.pytorch.org/executorch/stable/index.html
- Apache TVM: https://tvm.apache.org/

**Multi-LoRA**
- LoRA 논문: https://arxiv.org/abs/2106.09685
- Punica: Multi-Tenant LoRA Serving: https://arxiv.org/abs/2310.18547
- S-LoRA: https://github.com/S-LoRA/S-LoRA
- vLLM LoRA Adapters: https://docs.vllm.ai/en/latest/features/lora.html

**강화학습 서빙**
- OpenRLHF: https://github.com/OpenRLHF/OpenRLHF
- vLLM RLHF 문서: https://docs.vllm.ai/en/latest/training/rlhf.html
- Thinking Machines, "Defeating Nondeterminism in LLM Inference":
  https://thinkingmachines.ai/blog/defeating-nondeterminism-in-llm-inference/

**영상**
- [임커밋] 시각화로 이해하는 Diffusion 언어 모델
