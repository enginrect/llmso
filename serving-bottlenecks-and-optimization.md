# LLM 서빙의 병목과 필수 최적화 기법

## Part 1. 병목은 어디에 있는가

### 이 장이 답하려는 질문

> **"TFLOPS가 높은 GPU = 좋은 LLM 서빙 GPU인가?"**

답은 아니다. 그리고 그 이유를 설명하기 위해 이 장은 **산술 강도(arithmetic intensity)** 와
**루프라인 모델(roofline model)** 이라는 도구를 새로 꺼낸다.

미리 결론을 정리하면 병목이 국면마다 다르다.

| 국면 | 지배적 제약 |
|---|---|
| 모델을 올릴 때 | **GPU VRAM 용량** |
| Prefill | 입력이 길면 **compute-bound**가 될 수 있음 |
| Decode | 대체로 **memory bandwidth-bound** |
| 긴 컨텍스트 · 동시 사용자↑ | **KV 캐시 메모리** |
| Multi-GPU | **인터커넥트**(NVLink/NVSwitch/RDMA) |

즉 GPU를 고를 때 봐야 할 건 하나가 아니다.

```
연산 능력(FLOPS) + VRAM 용량 + 메모리 대역폭 + 인터커넥트
  × 모델 크기 × 정밀도 × KV 캐시 × prefill/decode 비율
```

### 왜 이게 사업 문제인가

#### 1. 고객 경험 — 지연시간과 만족도는 비선형이다

지연시간과 만족도는 반비례하지만 **선형이 아니다.** 20초를 1초로 줄이면 판이 바뀌지만,
0.1초를 0.01초로 줄이는 건 사람이 인지하지 못한다.

그래서 이미 충분히 빠른 구간이라면 **지연시간을 조금 내주고 처리량을 얻는 트레이드오프**가
비용 면에서 유리하다.

> **다만 이 논리를 오용하면 안 된다.** 이 트레이드오프는 0.1초 ↔ 0.01초 같은 미세 구간의 이야기지,
> "1초를 20초로 늘려도 된다"는 근거가 아니다. 20초면 사용자는 그냥 떠난다.

고객 경험의 또 다른 축은 **품질**이다. 같은 계열이면 파라미터가 클수록 벤치마크가 좋다
(Llama-3-8B < Llama-3-70B). 여기서 최적화의 진짜 가치가 나온다.

```
최적화 전:  지연시간 맞추려면 8B가 한계
최적화 후:  같은 하드웨어 · 같은 지연시간 · 같은 처리량으로 32B/70B 운용 가능
            → 응답 품질이 통째로 올라간다
```

#### 2. 비용 — 진짜 승부처는 훈련이 아니라 추론

GPT-4 훈련에 5천만 달러가 들었다는 보도 때문에 훈련이 더 비싸 보이지만, 실제로는
**추론용 하드웨어 소비가 이미 훈련을 넘어섰고 격차는 계속 벌어지는 추세**다.

이유는 단순하다.

```
훈련: 대부분 일회성 선투자 (+ 간헐적 재훈련·파인튜닝)
추론: 배포 후 모든 쿼리마다 계속 발생, 사용량에 비례해 누적
```

게다가 대부분의 기업은 파운데이션 모델을 처음부터 훈련하지 않는다. 파인튜닝하거나 RAG로 보강한다.
반면 추론 수요는 멈추지 않고, **에이전트 워크플로우가 이를 가속**한다 — 워크플로우 하나가
여러 LLM 호출과 임베딩 호출을 부르기 때문이다([아키텍처 노트](serving-engine-and-production-architecture.md)에서 본 그대로).

#### 3. 확장성 — 피크와 하드웨어 가용성

평소 안정적이던 시스템도 블랙프라이데이 같은 시기에 **400% 이상 수요 급증**을 맞을 수 있다.
최적화가 부족하면 병목·지연 악화·요청 실패로 이어진다.

그리고 현실적인 제약이 하나 더 있다. **고성능 GPU가 모든 리전에서 항상 구해지지 않는다.**
최적화된 모델은 저사양 칩에서도 돌아가므로, 특정 GPU에 종속되지 않는 것 자체가
신규 시장 확장의 조건이 된다.

### GPU 스펙 읽는 법

LLM과 직결되는 속성은 넷이다.

| 속성 | 무엇을 결정하나 |
|---|---|
| **연산 성능(FLOPS)** | 행렬곱·어텐션 처리량. 정밀도별로 다름(FP32/BF16/FP8/INT8) |
| **메모리 용량(VRAM)** | 모델 가중치 + KV 캐시를 담을 수 있는가 |
| **메모리 대역폭** | HBM ↔ 연산 유닛 데이터 전송 속도 |
| **인터커넥트** | GPU 간·노드 간 통신 대역폭 |
| (+ 전력) | 랙당 배치 밀도와 운영 비용 |

#### H100 SXM vs H100 NVL — 무엇을 고를 것인가

| 항목 | H100 SXM | H100 NVL |
|---|---|---|
| FP16/BF16 Tensor Core | **1,979 TFLOPS** | 1,671 TFLOPS |
| FP8 Tensor Core | **3,958 TFLOPS** | 3,341 TFLOPS |
| GPU Memory | 80 GB | **94 GB** |
| Memory Bandwidth | 3.35 TB/s | **3.9 TB/s** |
| Form Factor | SXM (전용 소켓) | PCIe |
| Interconnect | **NVLink 900 GB/s** | NVLink 600 GB/s |

**SXM은 연산이 강하고, NVL은 메모리 용량·대역폭이 강하다.** 어느 쪽이 나은가는 워크로드가 정한다.

#### 피자 가게 비유

| GPU 요소 | 비유 | 부족하면 |
|---|---|---|
| 연산력(FLOPS) | 오븐의 화력 | 굽는 데 오래 걸려 고객이 대기 = 연산 병목 |
| 메모리 대역폭 | 반죽을 오븐에 공급하는 속도 | **오븐이 논다 = GPU 연산력 낭비** |
| 메모리 용량 | 반죽 냉장고 크기 | 아예 모델을 못 올린다 = OOM |

세 요소의 균형을 워크로드에 맞추는 게 핵심이고, **현재 대부분의 병목은 두 번째(대역폭)** 다.

#### 인터커넥트 — 노드 안

| 구성 | GPU 간 대역폭 | 제약 |
|---|---|---|
| **H100 PCIe** | 128 GB/s | 가장 저렴. GPU 간 통신이 적은 워크로드용 |
| **H100 NVL (NVLink Bridge)** | 600 GB/s | **2개까지만** 연결. GPU 4개면 1-2는 600, 1-3은 여전히 128 |
| **H100 SXM + NVLink** | 총 900 GB/s | 최대 8개. 단 **900을 7개 링크로 나눠 각 128 GB/s** |
| **+ NVSwitch** | **모든 쌍이 900 GB/s** | 최고 성능, 비싸다 |

여기서 자주 헷갈리는 지점이 SXM의 900 GB/s다. 이건 **한 GPU의 총 대역폭**이고,
8-GPU 구성에서 나머지 7개와 점대점으로 나누면 링크당 900 ÷ 7 ≈ 128 GB/s가 된다.
**NVSwitch를 넣어야** 모든 쌍이 온전한 900 GB/s로 붙는다.

> **왜 PCIe를 안 쓰고 NVLink인가?**
> PCIe는 표준 제정 속도가 느리다. 7세대가 나와도 NVLink 규격에는 한참 못 미친다.
> 그래서 NVIDIA가 독자 규격으로 계속 속도를 올려왔다. 다만 독점 규격이라
> UALink 같은 개방형 표준과의 경쟁 구도도 함께 만들어지고 있다.

#### 인터커넥트 — 노드 간

한 노드에 담을 수 있는 GPU는 물리·전력·냉각 제약으로 **보통 8개**가 상한이다.
그 이상은 노드를 넘어야 하고, 그 순간 속도가 급락한다.

| 구간 | 대역폭 |
|---|---|
| 노드 내 NVLink/NVSwitch | 900 GB/s |
| 노드 내 NVLink Bridge | 600 GB/s |
| 노드 내 PCIe | 128 GB/s |
| **노드 간 (IB 400G)** | **50 GB/s** |

400 Gbps ÷ 8 = 50 GB/s. NVLink 대비 **18배 느리다.**

실무 원칙이 여기서 나온다. **텐서 병렬화의 기본 단위가 8인 이유**가 이것이다.
`--tensor-parallel-size 8`까지는 NVLink 안에서 끝나지만 9부터는 노드를 넘고,
all-to-all 통신이 50 GB/s 구간을 타면서 병목이 된다.

#### 전력 — 눈에 안 띄는 제약

TDP는 배포 환경에 따라 의미가 완전히 다르다.

| 환경 | 전력의 위상 |
|---|---|
| 일반 클라우드 사용자 | 추상화됨. 가격·가용성에 암묵적으로 반영될 뿐 |
| 클라우드 사업자·프라이빗 DC | **1급 설계 제약.** 랙당 GPU 수를 전력·냉각이 결정 |
| 엣지/온디바이스 | 시스템 전체를 규정. 정밀도와 실행 전략이 전력 중심으로 결정됨 |

극단적으로는 랙에 인입 전력이 충분히 공급되지 않아 GPU가 제 성능을 못 내는 경우도 생긴다.

#### 모델 규모별 GPU 선택

| 모델 | 권장 | 근거 |
|---|---|---|
| **소형** (예: 파인튜닝한 8B) | **A10 24GB** | 모델을 올리기 충분, 가장 저렴, 구세대라 구하기 쉬움. 단 **FP8 미지원** |
| **중형** (예: 14B) | **L40S 48GB** | **FP8 지원**이 크게 유리. 단일 GPU로 끝내 분산 오버헤드 회피 |
| **대형** (예: DeepSeek-R1 671B) | **H200 × 8 + NVLink, FP8** | 단일 GPU로 불가. MoE라 expert 분산 최적화도 가능 |

A10의 FP8 미지원은 실전에서 중요하다. FP8로 양자화한 모델을 준비했는데 하드웨어가
FP8 텐서 코어를 지원하지 않으면 기대한 성능이 나오지 않는다.

### 병목 ① — 모델 로딩

#### 왜 반드시 GPU 메모리여야 하나

```
디스크 → CPU 메모리 → GPU 메모리
```

"CPU 메모리에 두고 필요할 때 올리면 안 되나?" → 대역폭 차이 때문에 안 된다.

| 저장소 | 대역폭 |
|---|---|
| SSD | 0.5 ~ 14 GB/s |
| CPU 메모리 (DRAM) | 50 ~ 200 GB/s |
| **GPU 메모리 (HBM)** | **300 GB/s ~ 3 TB/s** |

가중치가 CPU 메모리에만 있으면 매 요청마다 GPU로 전송하는 단계가 추가되고,
이건 실시간 추론에서 용납 불가능한 지연이다. **그래서 GPU 메모리 용량이 1차 제약**이 된다.

#### 모델 크기 추정

필요한 건 두 가지뿐이다.

```
모델 크기 = 파라미터 개수 × 파라미터당 바이트 수(정밀도가 결정)
```

| 정밀도 | 비트 | 바이트 |
|---|---|---|
| FP32 | 32 | 4 |
| FP16 / BF16 | 16 | 2 |
| INT8 / FP8 | 8 | 1 |

파라미터 수는 보통 모델 이름에 있고(`Llama-2-7b`), 정밀도는 `config.json`의 `torch_dtype`에 있다.

```
Llama-2-7b (BF16) = 70억 × 2 bytes = 14,000,000,000 bytes ≈ 14 GB
```

실제 모델 파일 합계(약 13.5GB)와 거의 일치한다.

#### KV 캐시 추정 — 여기가 진짜 함정

"16GB GPU에 14GB 모델이면 되겠네"가 안 되는 이유가 KV 캐시다.

```
토큰당 KV 캐시 = 2 × 레이어 수 × 어텐션 헤드 수 × 헤드 차원 × 정밀도 바이트
```

Llama-2-7b(MHA, 32층, 32헤드, 헤드 차원 128, FP16) 기준으로 계산하면:

```
2 × 32 × 32 × 128 × 2 = 524,288 bytes = 0.5 MB / 토큰
```

여기에 동시 처리량을 곱한다.

```
총 KV 캐시 = 토큰당 크기 × (최대 배치 크기 × 최대 시퀀스 길이)
           = 0.5MB × 16 × 4,096
           = 32 GB      ← 모델(14GB)보다 크다
```

**KV 캐시가 모델보다 두 배 이상 커질 수 있다.** 이게 앞 노트들부터 반복된 "KV 캐시 관리가
throughput·latency·concurrency를 결정한다"의 정량적 근거다.

이 계산을 뒤집으면 GPU에서 가능한 배치 크기를 역산할 수 있다.

| GPU | 모델 로드 후 여유 | 이론적 최대 배치 |
|---|---|---|
| A10 24GB | 10 GB | 10×1024 ÷ (0.5×4096) = **5** |
| L40S 48GB | 34 GB | 34×1024 ÷ (0.5×4096) = **17** |

L40S가 비싸지만 동시 처리량이 3배 이상이라 **결과적으로 비용 효율이 더 낫다.**
(단 activation 몫이 따로 필요하므로 이론값을 다 쓰지는 못한다. 실질적으로 4 / 16 정도.)

#### 실전 경험칙

```
GPU 메모리 ≈ 모델 크기 × 2  를 시작점으로 잡는다
```

40GB 모델이면 80GB GPU에서 출발한다는 뜻이다. prefix caching 같은 기법은 여기서
메모리를 더 요구하므로, 여유는 낭비가 아니라 성능 여지다.

GPU 메모리 사용량은 유휴 → 실행으로 가며 이렇게 움직인다.

```
[유휴]   모델 가중치만 (고정)
[prefill] KV 캐시 급증
[decode]  토큰마다 KV 캐시 계속 증가 → 생성 끝나는 시점이 피크
```

이 피크를 넘는 여유가 없으면 OOM이다.

### 병목 ② — 모델 실행: 산술 강도와 루프라인

#### 산술 강도란

```
산술 강도(arithmetic intensity) = 연산 횟수(FLOPS) / 데이터 이동량(Bytes)
```

- **낮다** = 연산은 적은데 데이터를 많이 읽고 쓴다 → **memory-bound**
- **높다** = 적은 데이터로 많이 계산한다 → **compute-bound**

여기서 "데이터 이동"은 모델 로딩이 아니라 **실행 중** HBM에서 연산 유닛까지 오는 경로다.

```
HBM (off-chip, 가장 느림·가장 큼)
  → L2 캐시 → L1/공유 메모리 (SRAM, 빠름·작음)
    → 레지스터 (가장 빠름)
```

이 경로에서 가장 느린 구간이 HBM이므로, **GPU 메모리 대역폭이 데이터 이동의 기준**이 된다.

#### 칩의 크로스오버 포인트

L40S로 계산해보면:

```
362 TFLOPS(FP16) ÷ 864 GB/s = 419 FLOPS/Byte
```

**419가 이 칩의 분기점**이다. 워크로드의 산술 강도가 이보다 낮으면 GPU 연산력을 다 못 쓴다.

```
     달성 성능 (TFLOPS)
        │        ┌──────────── 362 (연산 한계, compute-bound)
        │       ╱
        │      ╱  ← 메모리 대역폭에 막힌 구간 (memory-bound)
        │     ╱
        └────┴──────────────── 산술 강도 (FLOPS/B)
            419
```

산술 강도 210이면 GPU 연산력의 절반만 쓰고 있다는 뜻이고, 1,000이면 이미 연산 한계에 걸려
데이터를 더 빨리 공급해도 소용없다.

#### 행렬곱의 산술 강도

Transformer 연산의 대부분은 행렬곱이다. 입력 [M,K] × 가중치 [K,N] → 출력 [M,N]일 때:

```
연산 횟수     = 2 × M × N × K
데이터 이동   = 2 × (M×K + K×N + M×N)     (FP16 기준 2바이트)

산술 강도 = (M×N×K) / (M×K + K×N + M×N)
```

M=N=K로 두고 계산하면 행렬이 커질수록 강도가 오른다.

| 행렬 크기 | 산술 강도 | L40S(419) 기준 |
|---|---|---|
| 64 | 21.3 | memory-bound |
| 512 | 170.7 | memory-bound |
| 4,096 | 1,365.3 | **compute-bound** |

#### 그래서 Prefill과 Decode는 어떻게 갈리는가

입력 텐서는 `[배치, 시퀀스 길이 s, 히든 차원 h]`다. 배치 1로 두면 `[s, h]`가 된다.

- **Prefill**: 프롬프트 토큰을 한꺼번에 처리 → **s가 크다**
- **Decode**: 토큰을 하나씩 생성 → **s = 1로 고정**

h=4096으로 계산해보면 결론이 선명하다.

| 시퀀스 길이 | Prefill 산술 강도 | Decode 산술 강도 | 판정 (L40S) |
|---|---|---|---|
| 64 | 62.1 | **≈1.0** | 둘 다 memory-bound |
| 512 | 409.6 | **≈1.0** | 둘 다 memory-bound |
| 4,096 | 1,365.3 | **≈1.0** | prefill은 compute-bound, decode는 memory-bound |

> **Decode는 시퀀스가 아무리 길어져도 산술 강도가 1 근처에 머문다.**
> s가 1로 고정이기 때문이다. 즉 **decode는 구조적으로 영원히 memory bandwidth-bound**다.

이게 첫 노트부터 반복된 "prefill은 compute-bound, decode는 memory-bound"의 수식적 증명이다.
그리고 최적화 방향이 여기서 갈린다.

```
compute-bound  → 연산량(FLOPS) 자체를 줄이는 기법
memory-bound   → 불필요한 데이터 이동을 줄이는 기법
```

Part 2의 모든 기법이 이 둘 중 하나에 속한다.

### 메모리 벽과 다른 가속기들

#### 왜 여전히 NVIDIA인가

경쟁자는 많다 — AMD MI300X, Intel Gaudi2, Google TPU, AWS Inferentia, Huawei Ascend,
그리고 Groq·Cerebras·SambaNova 같은 스타트업들. 그럼에도 NVIDIA가 지배하는 이유는 넷이다.

1. **범용성** — 특화 칩은 효율적이지만 **모델 아키텍처가 빠르게 바뀌는 상황에 대응이 어렵다**
2. **CUDA 생태계** — 전환 비용이 크다. ROCm·JAX·Neuron 등은 각자 독자 스택
3. **온칩 SRAM 칩의 비용** — 지연시간은 뛰어나지만 SRAM이 비싸 모델 하나에 칩이 많이 필요
4. **구성 유연성** — FP8/FP4 등 다양한 정밀도, 높은 대역폭, 인터커넥트

#### 메모리 벽

지난 20년간 연산 성능(FLOPS)은 폭발적으로 올랐지만 **메모리 대역폭과 인터커넥트 대역폭은
훨씬 완만하게 개선**됐다. 그래서 연산이 아무리 빨라져도 데이터를 못 가져와서 못 쓴다.
이게 **메모리 벽(memory wall)** 이다.

완화 시도는 두 방향이다.

**(1) 온칩 SRAM을 키운다** — 파라미터를 아예 연산 유닛 옆에 둬서 왕복 자체를 없앤다.
아이디어는 명쾌하지만 **SRAM이 비싸고 모델을 여러 칩에 쪼개야** 해서 비용 효율이 떨어진다.
그럼에도 지연시간이 처리량보다 중요한 워크로드에서는 가치가 있다는 인식이 업계에 퍼지고 있다.

**(2) 랙 스케일로 묶는다** — **GB200 NVL72**가 대표적이다.

```
B200      Blackwell 세대 GPU
GB200     Grace CPU + Blackwell GPU를 NVLink-C2C로 결합한 빌딩 블록
NVL72     NVLink Switch로 GPU 72개를 하나의 NVLink 도메인으로 묶은 랙
```

랙 뒷면이 구리 배선으로 거미줄처럼 연결되어, **72개 GPU가 병목 없이 서로 통신**한다.
랙을 넘으면 이더넷/IB로 18배 이상 느려지므로, "랙 하나를 거대한 GPU 하나처럼" 쓰겠다는 발상이다.

이 구조는 **MoE 모델에 특히 유리하다.** MoE는 expert 간 통신량이 많아 노드를 넘기면 손해인데,
NVLink 도메인 안이라면 GPU가 달라도 문제가 없다. (MoE는 교재 후반부에서 다룬다.)

---

## Part 2. 어떻게 뚫는가

Part 1이 "GPU가 논다"를 증명했다면, Part 2는 **"GPU에 더 많은 일을 시키되 불필요한 계산·메모리·
데이터 이동은 줄인다"** 를 네 갈래로 실행한다.

| 기법 | 공략 대상 |
|---|---|
| **배칭·스케줄링** | GPU 유휴 시간 |
| **어텐션·커널 최적화** | 어텐션 연산량과 HBM↔SRAM 이동 |
| **모델 압축** | 모델 자체의 크기와 연산량 |
| **프리픽스 캐싱** | 반복되는 prefill 재계산 |

### 배칭 — 가중치를 한 번만 읽는다

#### 배칭이 효과적인 진짜 이유

흔히 "여러 요청을 한꺼번에 처리해서 빠르다"고 설명하지만, 본질은 다른 데 있다.

```
[배칭 없음]  요청1 → 가중치를 HBM에서 SRAM으로 로드 → 토큰 1개 생성
             요청2 → 가중치를 또 로드          → 토큰 1개 생성
             요청3 → 가중치를 또 로드          → 토큰 1개 생성
             (가중치 전송 3회)

[배칭]       요청1,2,3 → 가중치를 한 번 로드 → 토큰 3개 동시 생성
             (가중치 전송 1회)
```

**모델 가중치는 모든 요청이 공유한다.** 그래서 배치를 키워도 가중치를 다시 읽을 필요가 없다.
한 번의 메모리 읽기로 더 많은 연산을 뽑아내는 것 — 이게 **산술 강도를 인위적으로 끌어올리는**
행위다.

효과가 국면마다 다른 것도 이 때문이다.

| 국면 | 배칭 효과 |
|---|---|
| **Prefill** | **제한적.** 이미 토큰을 병렬 처리해 GPU를 포화시키고 있음 |
| **Decode** | **크다.** 토큰 하나씩 만드는 구조라 GPU가 놀고 있었음 |

#### Static → Dynamic → Continuous

**Static batching** — 배치가 꽉 찰 때까지 대기. 온라인 서빙에 부적합하다.
10개가 찰 때까지 기다리는데 9번째와 10번째 사이에 한 시간이 비면 앞선 요청들이 그만큼 기다린다.

**Dynamic batching** — 여기에 **최대 대기 시간**을 추가한다.

```
배치가 꽉 참       → 즉시 실행
최대 대기 시간 도달 → 요청이 하나뿐이어도 즉시 실행
```

SLA가 "1분 내 응답"이면 max delay를 30초쯤으로 잡는 식이다. 전통적 ML 서빙에는 충분하지만
LLM에는 부족하다 — **요청마다 출력 길이가 달라서 배치 전체가 가장 느린 요청에 묶인다.**

**Continuous batching** — 배치 안의 요청이 끝나는 즉시 대기열의 새 요청을 그 자리에 넣는다.

```
[Static/Dynamic]  요청1 ████████
                  요청2 ███          ← 먼저 끝나도 슬롯이 논다
                  요청3 ██████
                       └ 전부 끝나야 다음 배치

[Continuous]      요청1 ████████
                  요청2 ███ → 요청4 █████   ← 빈 자리를 즉시 채움
                  요청3 ██████ → 요청5 ███
```

**vLLM·SGLang에서 continuous batching은 기본값이고 끌 수 없다.**
`max_num_seqs=1`로 두면 사실상 무력화되지만, 그건 배칭 효과를 없애는 설정이다.

한 가지 조건이 있다. 이 회전이 잘 돌려면 **각 처리 단계가 짧아야** 한다.
그래서 마이크로 배칭 개념이 함께 나온다.

#### 튜닝 파라미터 3종

Dynamic과 달리 max delay time은 필요 없다. 대신 셋을 조율한다.

| 개념 | vLLM 옵션 | 의미 |
|---|---|---|
| 최대 배치 크기 | `--max-num-seqs` | 동시에 처리할 **요청 개수** 상한 |
| 최대 배칭 토큰 수 | `--max-num-batched-tokens` | 한 iteration에서 배치 전체가 쓰는 **총 토큰 수** 상한 |
| 최대 모델 길이 | `--max-model-len` | 요청 하나가 넘을 수 없는 **컨텍스트** 상한 |

왜 토큰 단위 제어가 따로 필요한가. **"20토큰 요청 10개"와 "10만 토큰 요청 2개"는
완전히 다른 워크로드**인데, 요청 개수만으로는 이 차이를 반영할 수 없기 때문이다.

국면별 실질 제약도 다르다.

```
Prefill → max-num-batched-tokens 가 지배 (입력이 길어서)
Decode  → max-num-seqs 가 지배 (요청 개수가 병렬성)
```

주의: 토큰 상한을 너무 낮게 잡으면 prefill에서 GPU에 충분한 토큰을 공급하지 못해
연산력을 포화시키지 못한다.

#### Chunked Prefill

Continuous batching에도 약점이 남는다. **prefill과 decode는 성격이 다른 워크로드**인데
한 배치에 섞이기 때문이다.

누가 1,000페이지 PDF를 넣으면 그 prefill 하나가 뒤의 모든 요청을 밀어버린다.
prefill을 우선하면 이미 decode 중이던 요청이 통째로 대기하고, 함께 배칭해도
토큰 하나 디코딩은 긴 prefill을 끝내는 것보다 훨씬 빨라서 별 도움이 안 된다.

해법은 **긴 prefill을 잘게 쪼개는 것**이다.

```
[before]  ████████████████████ prefill  →  ▪ decode  ▪ decode
          (긴 prefill 하나가 전부를 막음)

[after]   ███ ▪ ███ ▪ ███ ▪ ███ ▪
          (prefill 조각과 decode를 번갈아 배치)
```

이상적으로는 **prefill 조각이 decode 하나와 비슷한 크기**가 되도록 나눈다.

효과는 트레이드오프다.

| 지표 | 변화 |
|---|---|
| ITL (토큰 간 지연) | **개선** — decode가 긴 prefill에 막히지 않음 |
| TTFT (첫 토큰) | **악화** — prefill이 여러 단계로 나뉨 |
| E2E 지연 | 개선 없음, 오버헤드로 약간 나빠지기도 |
| 처리량 | **개선** — 유휴 틈을 메워 GPU 활용률 상승 |

```bash
--enable-chunked-prefill        # 기본 활성화
--max-num-batched-tokens 2048   # 이 값이 사실상 청크 크기
```

별도의 "청크 크기" 옵션은 없다. `--max-num-batched-tokens`가 그 역할을 한다.
너무 크면 청킹을 안 하는 것과 같고, 너무 작으면 GPU를 포화시키지 못한다.

### 어텐션 최적화

#### KV 캐시를 줄이는 것이 왜 중요한가

decode 단계에서는 **매 iteration마다 KV 캐시가 HBM ↔ SRAM을 왕복**한다.
디코드가 10번이면 10번 가져오고 10번 내려보낸다. 그리고 토큰이 쌓일수록 그 양이 계속 커진다.

KV 캐시가 작아지면 세 가지가 동시에 좋아진다.

```
메모리 대역폭 부담 ↓  (decode 병목 완화)
GPU 메모리 점유 ↓     (배치를 더 키울 수 있음 = 처리량 ↑)
같은 메모리로 더 긴 컨텍스트 서빙 가능
```

#### MHA → MQA → GQA → MLA

| 방식 | 구조 | KV 캐시 | 문제 |
|---|---|---|---|
| **MHA** | 쿼리마다 전용 K/V 헤드 | 기준(100%) | 가장 비효율적 |
| **MQA** | **모든 쿼리가 K/V 헤드 1개를 공유** | 헤드 수만큼 절감(32~64배) | **정확도 저하가 큼** |
| **GQA** | 쿼리 헤드를 그룹으로 묶어 그룹마다 K/V 공유 | **그룹 크기만큼 절감** (2배~8배) | MHA와 MQA의 절충 — **현재 다수 채택** |
| **MLA** | 헤드를 줄이는 대신 **압축(latent)해서 캐싱** | GQA(그룹 2.25) 수준 | DeepSeek 도입, 성능은 MHA보다 강하다고 주장 |

MQA가 얼마나 공격적인지 보면 이해가 쉽다. 7B 모델은 보통 어텐션 헤드가 32개인데,
MQA는 그 32개가 **단 하나의 K/V 뱅크를 공유**한다. 메모리는 크게 줄지만
정확도 손실이 커서 실전에서 쓰기 어려웠고, 그래서 GQA가 나왔다.

> **GQA의 절감률은 고정값이 아니다.** `num_attention_heads ÷ num_key_value_heads`,
> 즉 그룹 크기가 그대로 배수가 된다. [첫 노트](model-serving-and-llm-basics.md)에서 Qwen2.5-0.5B의 config로 확인한 값이
> 14 ÷ 2 = **7배**였고, Llama-3-8B는 32 ÷ 8 = 4배다.
> 모델 카드에 GQA라는 단어가 없어도 이 두 값만 보면 알 수 있다.

가장 최근에는 **하이브리드**도 등장했다 — Kimi K3는 KDA(Kimi Delta Attention)와
Gated MLA를 3:1로 결합한다. 큰 흐름은 이렇게 정리된다.

```
기본 어텐션은 O(L²)이라 롱 컨텍스트에 너무 비싸다
  → 압축하거나 범위를 줄인다 (Linear Attention, Sliding Window, MLA)
    → 압축하면 정보가 희석된다
      → DeltaNet / Gated DeltaNet으로 중간중간 보정
        → 그래도 손실이 있다
          → 평소엔 Linear로 가다가 중간중간 전역 어텐션을 섞는다
```

#### config.json으로 확인하는 법

모델이 무엇을 쓰는지는 두 필드만 비교하면 안다.

```jsonc
// MHA — 둘이 같다
"num_attention_heads": 32,
"num_key_value_heads": 32,

// GQA — KV 헤드가 적다
"num_attention_heads": 32,
"num_key_value_heads": 8,     // → 4개의 어텐션 헤드가 KV 1개를 공유
```

첫 노트에서 다룬 Qwen2.5-0.5B도 `num_attention_heads: 14` vs `num_key_value_heads: 2`였다.
그때 config에서 GQA를 읽어냈던 것이 여기서 다시 연결된다.

> 어텐션 방식의 진화가 주는 실질적 의미는 이것이다.
> **GPU를 한 대 더 사지 않고도 메모리 효율을 크게 얻는다.**

#### GPU 커널 — OS 커널이 아니다

여기서 말하는 커널은 **GPU 커널**이다. OS 커널과 구조적으로 비슷한 위치에 있다.

```
[OS]   유저 프로세스 → 커널 → 하드웨어(CPU/메모리/디스크)
[GPU]  PyTorch 코드 → GPU 커널 → GPU 연산 유닛/메모리
```

행렬곱·소프트맥스 같은 연산을 실제로 수행하는 작고 특화된 프로그램이다.

**커널 퓨전(Kernel Fusion)** — 여러 연산을 하나로 합쳐 HBM 왕복을 없앤다.

```
[퓨전 전]  연산1 → HBM에 쓰기 → HBM에서 읽기 → 연산2 → HBM에 쓰기 → ...
[퓨전 후]  한 번 읽어서 → 레지스터/SRAM에서 전 과정 계산 → 한 번 쓰기
```

**FlashAttention** — 이 발상을 어텐션에 특화시킨 것이다.

```
[표준]  MatMul → HBM 왕복
        Mask    → HBM 왕복
        Softmax → HBM 왕복
        MatMul  → HBM 왕복        (병목 구간을 단계마다 왕복)

[Flash] 타일 단위로 한 번 가져와 → SRAM 안에서 전 과정 → 최종 출력만 HBM에
```

큰 행렬을 **타일링(tiling)** 으로 쪼개 SRAM에 담고, 온라인 소프트맥스 같은 기법으로
중간 결과를 HBM에 구체화(materialize)하지 않는다. **계산 결과는 동일하고 데이터 이동만 바뀐다.**

FlashAttention 2·3은 아이디어를 유지하면서 GEMM과 소프트맥스를 오버랩하는 등의 기법을 더해
신세대 GPU(H100 등)에서 활용률을 더 끌어올린다.

```bash
vllm serve <model> --attention-backend FLASH_ATTN
```

명시하지 않아도 vLLM이 GPU 세대에 맞춰 자동 선택한다 —
Hopper면 FlashAttention 3, Ampere 이하면 FlashAttention 2나 FlashInfer 쪽으로.
**기본값으로 시작하고, 다른 최적화를 먼저 해본 뒤 커널을 실험하는 게 순서**다.

**PagedAttention** — 커널이 아니라 **메모리 관리** 층의 혁신이다.

전통적 방식은 요청마다 최악의 경우를 가정해 연속 메모리를 미리 할당한다.
4,096 토큰을 예약했는데 200만 쓰면 나머지는 요청이 끝날 때까지 낭비된다.
vLLM 팀 측정으로 **KV 캐시의 실사용률이 20.4~38.2%**에 불과했다.

해법은 OS 페이징을 그대로 가져온 것이다.

```
KV 캐시를 고정 크기 블록으로 분할
  → 블록 테이블(논리 → 물리 매핑)로 접근
    → 연속 메모리 불필요, 흩어진 블록도 활용
      → 내부 단편화는 요청당 최대 한 블록, 외부 단편화는 소멸
```

논문 기준 **메모리 낭비가 거의 0에 가까워진다.** 확보된 메모리는 곧바로 동시 요청 수 증가로 이어진다.
continuous batching과 마찬가지로 지금은 사실상 표준 기능이다.

### 모델 압축

#### 세 가지 방법

| 기법 | 방식 | 실무 위상 |
|---|---|---|
| **양자화(Quantization)** | 파라미터 정밀도를 낮춘다 | **가장 실용적.** 훈련 파이프라인 수정 거의 불필요 |
| **증류(Distillation)** | 큰 교사 모델의 지식을 작은 학생 모델로 전이 | 잠재력은 가장 크지만 비용·난이도가 높음 |
| **가지치기(Pruning)** | 불필요한 가중치·헤드를 제거 | 아직 연구 단계, 셋 중 가장 덜 대중적 |

#### 양자화 — 오차 두 종류

정밀도를 낮추면 두 가지 오차가 생긴다.

| 오차 | 원인 | 예 |
|---|---|---|
| **반올림 오차** | 대상 포맷에 정확히 표현 불가 | FP32의 7.6 → INT8에서 8 (오차 0.4) |
| **클램핑 오차** | 표현 범위 초과로 절단 | FP8 범위 ±448일 때 1,000 → 448 |

클램핑이 더 위험하다. 다만 실제 가중치 분포가 대체로 정규분포에 가까워서
**양 끝 극단값을 잘라도 영향이 제한적**인 경우가 많다. 그럼에도 최신 기법은 하드 클램핑 대신
**스케일링**으로 값 범위를 압축해 표현 범위 안에 넣는다.

#### 부동소수점 포맷

```
총 비트 = 부호(1) + 지수부(범위) + 가수부(정밀도)
```

| 포맷 | 총 비트 | 지수 | 가수 | 범위 |
|---|---|---|---|---|
| FP32 | 32 | 8 | 23 | ±10³⁸ |
| **BF16** | 16 | **8** | 7 | **FP32와 동일** |
| FP16 | 16 | 5 | 10 | ±6×10⁻⁵ ~ 65504 |

BF16이 학습에서 선호되는 이유가 여기 있다. **지수부를 FP32만큼 유지**해서 범위가 같고,
정밀도만 희생하므로 FP32→BF16 변환에 클램핑 위험이 없다.

정수형(INT8/INT4)과 부동소수점형(FP8/FP4)의 차이도 중요하다.

```
INT  균일 분포 — 전 구간에 값이 고르게 배치
FP   비균일 분포 — 0 근처에 촘촘, 큰 값 근처는 성김 (로그적)
```

가중치·활성화의 실제 분포에 따라 어느 쪽이 정확도 유지에 유리한지 달라진다.

#### 양자화가 세 가지를 동시에 준다

```
1. 모델 크기 감소     7B FP16 14GB → INT8 7GB
                      → 단일 GPU/노드에 들어가면 노드 간 통신 회피
                      → 남은 공간이 KV 캐시로 = 동시 요청 ↑
2. 데이터 이동량 감소  decode(memory-bound) 지연 직접 개선
3. 연산 속도 향상      비트를 절반으로 줄이면 FLOPS가 대략 2배
                      (H100: FP16 1,979 → FP8 3,958 TFLOPS)
```

#### Weight-only vs Weight-and-Activation

| | **W4A16** (GPTQ/AWQ) | **W8A8** (INT8→FP8) |
|---|---|---|
| 크기·이동량 감소 | 75% | 50% |
| 연산 FLOPS | **변화 없음** | **2배** |
| Prefill (compute-bound) | 변화 없음 | 개선 |
| Decode (memory-bound) | 개선 (**저배치에 강함**) | 개선 (**고배치에 강함**) |
| 적합 | 긴 생성, 지연시간 민감, 저배치 | 긴 컨텍스트, 고처리량, 고배치 |

W4A16이 연산을 못 살리는 이유는 **weight-only라 활성화는 여전히 16비트**이기 때문이다.
실행 시 저비트를 고비트로 되돌리는 **역양자화(dequantize)** 까지 필요하다.
(Marlin·Machete 같은 혼합 정밀도 커널이 이 역양자화를 피하게 해준다.)

실전 원칙은 이렇다.

> **W8A8만으로 지연시간 SLA를 만족한다면 W4A16을 쓰지 말고, 대신 배치를 최대한 키워
> 인스턴스당 처리량을 올려 비용을 줄인다.**

#### 하드웨어 호환성 — 놓치기 쉬운 함정

FP8 양자화 모델을 준비했는데 GPU가 FP8 텐서 코어를 지원하지 않으면 의미가 없다.

| 아키텍처 | FP8 | FP4 |
|---|---|---|
| Blackwell (B200) | O | **네이티브 지원** |
| Hopper (H100/H200) | **O** | 네이티브 아님(소프트웨어 혼합 정밀도로 실용적) |
| Ampere 이하 (A100/A10) | **X** | 부실 |

양자화 방식을 고르기 전에 GPU 스펙부터 확인해야 한다는 뜻이다.
Part 1에서 A10이 FP8 미지원이라고 짚었던 것이 여기서 실전 제약으로 돌아온다.

#### 정확도 트레이드오프

다행히 GPTQ W4A16, AWQ, FP8(W8A8)은 실측 정확도 손실이 미미하다는 것이 여러 연구로 확인됐다.
패턴도 있다.

```
모델이 클수록  → 양자화에 덜 민감 (여유가 많아서)
모델이 작을수록 → 정확도 손실 위험 ↑
KV 캐시 양자화 → 정확도엔 덜 침습적이지만 성능 개선 폭도 작다
```

역으로 이용할 수도 있다. **FP8 12B 모델**이 **FP16 8B 모델**과 비슷한 지연시간을 내면서
처리량과 정확도는 더 좋을 수 있다.

정확도 검증에는 `lm_eval` 같은 하니스를 쓴다.

#### PTQ vs QAT

| | **PTQ** (Post-Training) | **QAT** (Quantization-Aware Training) |
|---|---|---|
| 시점 | 훈련 완료 후 | 훈련 중 양자화를 시뮬레이션 |
| 난이도 | 낮음 (변환 + 캘리브레이션) | 높음 (훈련 파이프라인 필요) |
| 8비트 정확도 | 좋음 | 좋음 |
| **4비트 정확도** | 보통 허용 불가 | **허용 가능** |
| 유연성 | 다양한 하드웨어에 재배포 용이 | 양자화 스킴에 종속 |

실무에서는 PTQ가 압도적으로 대중적이다. QAT는 **4비트급 공격적 압축**처럼
단순 반올림으로는 모델이 망가지는 경우의 선택지다.
OpenAI GPT-OSS가 FP4(MXFP4) QAT를 써서 120B를 단일 80GB GPU에,
20B를 16GB 엣지 디바이스에 올린 것이 대표 사례다(FP4는 파라미터당 0.5바이트).

#### 증류

양자화·가지치기가 원본을 줄이는 것과 달리, 증류는 **완전히 새로운 작은 모델을 훈련**한다.
큰 "교사" 모델의 지식을 작은 "학생" 모델로 옮긴다.

중요한 제약: **교사 모델에 대한 완전한 접근이 필요**하다. API로 출력 토큰만 받아서는 안 된다
(로짓이나 손실까지 써야 하기 때문).

| | 양자화 | 증류 |
|---|---|---|
| 정확도 하락 | 낮음 (보통 3% 이하) | 더 큼 |
| 속도 향상 | 1.5~3배 | 훨씬 큼 |
| 난이도 | 낮음 (가중치만 있으면 됨) | 높음 (훈련 비용이 원본의 최대 10%) |

**실전 순서**는 명확하다.

```
1. 이미 증류된 모델이 공개돼 있는가? → 있으면 먼저 평가
2. 정확도 기준을 만족하면 채택 → 그 위에 양자화를 추가 적용
3. 증류 모델이 없으면 → 양자화부터 (증류는 비싸고 손실도 크다)
```

둘은 경쟁이 아니라 **보완 관계**다.

#### 가지치기

모델이 과도하게 파라미터화되어 있다는 전제에서, 불필요한 가중치나 헤드를 제거한다.
NVIDIA GPU(Ampere·Hopper)는 **2:4 구조적 희소성**을 스파스 텐서 코어로 가속해
50% 희소성에서 행렬곱을 최대 2배까지 끌어올릴 수 있다.
다만 프로덕션 적용은 아직 셋 중 가장 미성숙하다.

### 프리픽스 캐싱

#### 왜 요청 전체 해싱은 안 되나

전통적 서버 측 캐싱은 요청 전체를 해시해 결과를 저장한다(Triton이 그렇게 한다).
그런데 **LLM은 자유형 텍스트를 받는다.** 사람들은 같은 질문을 수십 가지 방식으로 표현하므로
캐시 히트율이 바닥이다.

**프리픽스 캐싱**은 전체 대신 **앞부분(prefix)만 매칭**한다. 앞부분이 일치하면
그 구간의 KV 캐시를 재계산하지 않고 그대로 재사용한다.

```
캐싱 OFF → 요청이 끝나면 KV 캐시 즉시 삭제
캐싱 ON  → GPU 메모리 여유가 있는 한 유지, 부족하면 LRU로 축출
```

#### 구현체

| 프레임워크 | 구현 | 활성화 |
|---|---|---|
| **SGLang** | **RadixAttention** — 라딕스 트리로 prefix 추적. 트리는 CPU 메모리, 각 노드가 GPU의 KV 캐시에 매핑 | 기본 내장 |
| **vLLM** | 자체 구현 — KV 캐시 블록을 **해시 기반**으로 매칭(라딕스 트리 아님) | `--enable-prefix-caching` (기본 True) |

#### 어디서 빛나는가

**멀티턴 채팅** — 대화 기록 전체가 매 턴 새 프롬프트에 붙어 전송된다.
캐싱이 없으면 매번 전체를 다시 prefill해야 하고, 대화가 길어질수록 TTFT가 나빠진다.

**긴 컨텍스트 서빙** — 컨텍스트가 4K → 128K → 1M으로 커지면서 prefill이 TTFT를 지배한다.
프롬프트 앞부분을 고정적으로 구성하면 매번 캐시 히트를 얻는다.

**에이전트 워크플로우** — 같은 질문을 여기저기 물어보고 지식을 덧붙이며 반복하므로
동일한 프롬프트가 포함될 확률이 특히 높다.

#### 캐시 히트를 깨뜨리는 것들

여기서 실무적으로 중요한 주의사항이 나온다. **접두사는 완전히 같아야 한다.**

```
"Hi, what is the weather like today?"
"Hi, what is the weather like now?"     → "Hi, what is the weather like " 까지 공유 ✓
"What is the weather like today?"        → "Hi" 가 없어 처음부터 재계산 ✗
```

**공백 하나만 달라도 다른 값으로 판단**된다. 그래서 프롬프트 구성 순서가 성능에 직결된다.

```
권장 구조
  <system>   안정적인 시스템 프롬프트 (항상 동일)
  <context>  정적 컨텍스트
  <user>     사용자마다 달라지는 부분
```

RAG에서는 특히 조심해야 한다. **검색된 청크의 순서가 매번 바뀌면 캐시 히트율이 떨어진다.**
같은 문서를 가져와도 배치 순서가 다르면 다른 프리픽스가 된다.

#### 확장 — Cache-Aware Routing

인스턴스가 여러 개면 문제가 생긴다. **프리픽스 KV 캐시는 특정 인스턴스에 로컬로 존재**하므로
라운드 로빈으로 뿌리면 캐시가 있는 곳으로 안 갈 수 있다.

**Cache-aware routing**은 프리픽스와 인스턴스 사이에 친화도(affinity)를 만들어
해당 캐시를 가진 인스턴스로 보낸다. 효과가 두 가지다.

```
1. 새 인스턴스에서 prefill을 처음부터 다시 하지 않아도 됨
2. 모든 인스턴스가 모든 프리픽스를 캐싱할 필요가 없어짐
   → GPU 메모리 절약 + 잦은 축출/재계산 방지
```

앞 노트들부터 나온 Cache-Aware Routing이 여기서 완성된다.
(더 자세한 라우팅은 교재 후반부에서 다룬다.)

#### 멀티테넌시 보안 — 놓치기 쉬운 함정

여러 고객이 같은 엔드포인트를 공유할 때 문제가 생긴다.

> 고객 A와 B의 프롬프트 접두사가 우연히 같으면, **응답 지연시간을 관찰해서
> 다른 고객의 데이터 존재 여부를 추론**할 수 있다.

해법은 시스템 프롬프트와 컨텍스트 사이에 **고객별 고유 ID를 삽입**하는 것이다.

```
<system>  You are a helpful assistant.
<id>      {user id or session id}      ← 여기서 프리픽스가 갈린다
<context> ...
<user>    ...
```

이러면 서로 다른 고객은 시스템 프롬프트까지만 공유하고 그 이후는 절대 섞이지 않는다.
**다만 이건 캐시 공유율을 낮추는 대가를 치른다.** 팀 이름 같은 걸 앞쪽에 넣으면
그만큼 공유 가능한 구간이 줄어든다. 격리와 공유율 사이에서 위치를 정해야 한다.

---

## 스스로 답해본 질문

### 병목 진단

**Q1. "TFLOPS가 높은 GPU = 좋은 LLM 서빙 GPU인가?"**

아니다. FLOPS는 **연산 유닛이 데이터를 받았을 때** 낼 수 있는 속도일 뿐이고,
LLM 서빙의 상당 구간은 애초에 데이터를 못 받아서 막힌다.

국면별로 지배적 제약이 다르다. 모델을 올리는 순간엔 **VRAM 용량**이 결정한다
(못 담으면 FLOPS가 아무리 높아도 시작조차 못 한다). decode는 산술 강도가 1 근처라
**메모리 대역폭**이 지배한다. 멀티 GPU로 가면 **인터커넥트**가, 긴 컨텍스트·고동시성이면
**KV 캐시 메모리**가 병목이 된다.

같은 H100 라인에서도 SXM은 연산이 강하고 NVL은 메모리 용량·대역폭이 강하다.
"어느 쪽이 우월한가"가 아니라 "내 워크로드가 무엇에 막히는가"의 문제다.
A10이 FP8을 지원하지 않는다는 것 같은 기능 지원 여부도 FLOPS 숫자에는 안 나온다.

**Q2. 우리 워크로드는 GPU 연산력을 다 쓰고 있는가, 메모리 대역폭이 병목인가?**

**산술 강도를 계산해 칩의 크로스오버 포인트와 비교**하면 답이 나온다.
L40S의 분기점은 362 TFLOPS ÷ 864 GB/s = 419 FLOPS/B다.

행렬곱 기준으로 보면 행렬이 커질수록 강도가 오른다(64→21, 512→171, 4096→1365).
그래서 LLM에서는 **행렬(시퀀스 길이)이 충분히 큰가**가 판정 기준이 된다.

```
Prefill:  s가 크면 419를 넘어 compute-bound가 될 수 있다
Decode :  s = 1로 고정 → 산술 강도가 항상 ≈1.0 → 영원히 memory-bound
```

즉 우리 워크로드가 prefill 비중이 크고 프롬프트가 길다면 연산이 병목일 수 있고,
짧은 프롬프트에 긴 생성이라면 거의 확실히 대역폭이 병목이다.
그리고 배치 크기 1로 고정하면 어느 쪽이든 GPU를 포화시키지 못한다.

**Q3. 같은 하드웨어로 지연시간·처리량을 유지하면서 비용/품질을 어떻게 유리하게 바꾸는가?**

세 갈래가 있다.

**품질을 올린다** — 서빙을 최적화해 확보한 여유로 8B 대신 32B/70B를 같은 지연시간에 돌린다.
같은 계열에서는 파라미터가 클수록 벤치마크가 좋으므로, 최적화가 곧 품질 향상이 된다.

**비용을 내린다** — 같은 하드웨어로 처리량을 올리면 동시 서빙 고객이 늘어 수평 확장 시
필요한 노드 수가 줄어든다. 또는 최적화된(양자화된) 모델로 **저사양 칩**에서 동등한 성능을 내면
단가 자체가 내려간다.

**제약을 푼다** — 특정 고급 GPU에 종속되지 않으면 리전별 GPU 가용성 문제를 우회할 수 있고,
피크 트래픽에도 확장 여지가 생긴다.

기술적으로 이걸 가능하게 하는 것들이 결국 KV 캐시 축소(GQA/MLA), 양자화, 프리픽스 캐싱,
그리고 인터커넥트를 고려한 배치 설계다.

### 최적화 기법

**Q1. GPU에 더 많은 일을 시키면서, 동시에 불필요한 계산·메모리·데이터 이동은 어떻게 줄이는가?**

네 층위에서 동시에 공략한다.

```
[더 많은 일] 배칭·스케줄링
   가중치를 한 번 읽어 여러 요청을 처리 → 산술 강도를 인위적으로 상승
   continuous batching으로 유휴 슬롯 제거, chunked prefill로 틈새 충전

[계산 줄이기] 어텐션 알고리즘
   MQA/GQA/MLA로 K/V 자체를 줄이거나 압축

[이동 줄이기] 커널
   커널 퓨전으로 HBM 왕복 제거, FlashAttention으로 SRAM 안에서 완결

[메모리 줄이기] 압축 + 관리
   양자화로 크기·이동량·연산량을 동시에 축소
   PagedAttention으로 단편화 제거, 프리픽스 캐싱으로 재계산 회피
```

**셋은 서로를 돕는다.** 양자화로 메모리를 아끼면 배치를 더 키울 수 있고,
배치가 커지면 산술 강도가 올라 GPU를 더 쓴다. KV 캐시를 줄여도 같은 효과가 난다.

**Q2. Prefill(compute-bound)과 Decode(memory-bound)를 하나의 배치에 어떻게 공존시키는가?**

순진하게 섞으면 둘 다 손해다. prefill을 우선하면 decode 중인 요청이 통째로 대기하고,
같은 iteration에 넣어도 토큰 하나 디코딩은 긴 prefill보다 훨씬 빨라서 균형이 안 맞는다.

해법이 **chunked prefill**이다. 긴 prefill을 **decode 하나와 비슷한 크기의 조각으로 쪼개서**
번갈아 배치한다. 그러면 두 워크로드의 작업 단위가 비슷해져 한 배치 안에서 자연스럽게 섞인다.

대가는 명확하다. **ITL과 처리량은 좋아지지만 TTFT는 나빠진다** — prefill이 여러 단계로
나뉘기 때문이다. 그래서 유스케이스가 선택 기준이 된다. 챗봇처럼 TTFT가 핵심이면 신중해야 하고,
긴 컨텍스트 요청과 대화형 요청이 섞이는 환경이면 이득이 크다.

조각 크기는 `--max-num-batched-tokens`로 조절하는데, 너무 크면 청킹 효과가 없고
너무 작으면 GPU를 포화시키지 못하는 중간값 찾기 문제다.

더 근본적인 해법은 아예 **다른 GPU로 분리**하는 P/D disaggregation이고, 이건 교재 후반부(고급 최적화) 주제다.

**Q3. 모델 품질을 얼마나 희생해야 얼마만큼의 서빙 성능을 얻는가?**

기법마다 교환비가 다르다.

| 기법 | 정확도 손실 | 성능 이득 |
|---|---|---|
| GQA/MLA | 거의 없음 (아키텍처 선택) | KV 캐시 대폭 축소 |
| FlashAttention/PagedAttention | **없음** (결과 동일) | 이동·단편화 제거 |
| 양자화 W8A8 / W4A16 | 보통 3% 이하 | 1.5~3배 |
| 증류 | 양자화보다 큼 | 훨씬 큼 (10배 축소도 가능) |

즉 **먼저 손실이 0인 것부터 쓰는 게 순서**다. 커널과 메모리 관리 최적화는 정확도를
전혀 건드리지 않으므로 고민할 필요가 없다. 그다음이 양자화, 마지막이 증류다.

양자화 안에서도 패턴이 있다. **모델이 클수록 양자화에 덜 민감**하므로,
작은 모델을 그대로 쓰는 것보다 **큰 모델을 양자화해서 쓰는 편이 정확도와 처리량 모두
유리한 경우**가 있다(FP8 12B ≈ FP16 8B 지연시간).

그리고 손실을 정확히 알려면 결국 **측정해야 한다.** `lm_eval` 같은 하니스로
내 유스케이스에 맞는 벤치마크를 돌려보는 것 외에 일반해는 없다.

---

## 정리

이번 노트는 **진단 → 처방** 구조가 그대로 드러났다.

```
진단
  GPU를 고를 때 FLOPS만 보면 안 된다
    → 국면마다 병목이 다르기 때문 (용량 / 대역폭 / 인터커넥트 / KV 캐시)
      → 이걸 정량화하는 도구가 산술 강도와 루프라인
        → 계산해보니 decode는 s=1 고정이라 산술 강도가 항상 ≈1
          → decode는 구조적으로 영원히 memory-bound
            → 즉 "GPU가 논다"가 기본 상태다

처방
  GPU를 놀지 않게 한다        → 배칭 (가중치 한 번 읽고 여러 요청 처리)
  옮길 데이터를 줄인다         → GQA/MLA (KV 캐시 축소), 양자화
  옮기는 횟수를 줄인다         → 커널 퓨전, FlashAttention
  옮길 공간을 아낀다           → PagedAttention (단편화 제거)
  아예 다시 계산하지 않는다     → 프리픽스 캐싱
```

가장 분명해진 연결은 **배칭의 이유**였다. "여러 개를 한꺼번에 처리해서 빠르다"가 아니라
**"모델 가중치를 한 번만 HBM에서 읽어오면 되기 때문"** 이라는 것. 이 한 문장으로
왜 배칭이 decode에서만 크게 효과적인지, 왜 그것이 산술 강도를 올리는 행위인지가 동시에 설명된다.

그리고 Part 1에서 계산한 KV 캐시 32GB > 모델 14GB라는 숫자가 Part 2의 절반을 정당화한다.
GQA도, MLA도, PagedAttention도, KV 캐시 양자화도, 프리픽스 캐싱도 전부 이 32GB를 겨냥한다.

다음 노트는 고급 최적화와 서빙 프레임워크다. 이번에 미뤄둔 P/D disaggregation,
MoE, speculative decoding이 거기서 나온다.

---

## 이어서 볼 것

- **Speculative decoding** — 아키텍처 노트의 레이어 3에서 이름만, 이번에도 이름만 나왔다
- **MoE 아키텍처** — 랙 스케일 인터커넥트가 왜 MoE에 유리한지 (교재 후반부)
- **P/D disaggregation** — chunked prefill의 근본 해법
- **KV 캐시 양자화 + 양자화된 어텐션 커널** 조합 — 둘을 같이 써야 이득이 온다는 조건
- **MLA의 압축 메커니즘** — "헤드를 줄이는 대신 latent를 캐싱한다"의 실제 수식
- Kimi K3의 하이브리드 어텐션(KDA + Gated MLA) 구조

---

## 참고 자료

**교재**
- Hands-On LLM Serving and Optimization (2026)

**하드웨어**
- NVIDIA H100 Tensor Core GPU: https://www.nvidia.com/en-us/data-center/h100/
- NVIDIA GB200 NVL72: https://www.nvidia.com/en-us/data-center/gb200-nvl72/
- NVLink & NVSwitch for LLM inference: https://developer.nvidia.com/blog/nvidia-nvlink-and-nvidia-nvswitch-supercharge-large-language-model-inference/
- AI and Memory Wall (arXiv): https://arxiv.org/pdf/2408.14158

**최적화 기법**
- vLLM 공식 문서: https://docs.vllm.ai
- SGLang 문서: https://docs.sglang.ai
- FlashInfer: https://github.com/flashinfer-ai/flashinfer
- A Visual Guide to Quantization: https://newsletter.maartengrootendorst.com/p/a-visual-guide-to-quantization
- lm-evaluation-harness: https://github.com/EleutherAI/lm-evaluation-harness

**영상**
- LLM Inference Engineering 시리즈 — 메모리 벽 / GPU 내부 / 커널과 메모리 / **양자화** / **병렬 처리** / MoE / **프로덕션 서빙**
- 딥러닝 큐레이터 임커밋 — GQA, DeepSeek MLA
- Visual AI — GQA & MQA로 메모리 75% 절감

**스터디 멤버 공개 자료**
- vLLM 양자화 벤치마크(BF16 / FP8-dynamic / GPTQ-Int4) 재현 코드:
  https://github.com/icebreaker70/vllm-quantization-hands-on-benchmark
