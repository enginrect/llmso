# 고급 LLM 최적화와 서빙 프레임워크

## Part 1. 추측 디코딩 — decode의 memory-bound를 연산으로 옮긴다

### 왜 이게 성립하는가

decode는 산술 강도가 시퀀스 길이와 무관하게 1 근처에 고정된다 — 앞서 계산으로
확인한 사실이다. 토큰 하나를 만들 때마다 모델 가중치 전체를 HBM에서 읽어오는데
연산은 그 토큰 하나 분량뿐이니, **GPU 연산 유닛이 논다.**

추측 디코딩의 발상은 이 노는 연산을 쓰자는 것이다. 작은 **draft 모델**이 토큰 K개를
미리 추측하고, 큰 **target 모델**이 그 K개를 **한 번의 forward pass로 병렬 검증**한다.
가중치를 한 번만 읽고 토큰 여러 개를 확정하니, 토큰당 메모리 이동이 줄어든다.

스터디 자료의 수치로 계산해보면 (target 가중치 140GB, draft 7GB, 토큰 3개 생성):

```
target 단독:    가중치 로드 3회      = 140 × 3 = 420 GB 이동
추측 디코딩:    (target+draft) 1회   = 140 + 7 = 147 GB 이동
```

420 ÷ 147 = **2.86배**의 메모리 이동 절감이다 (계산 검증). decode 병목이 메모리
대역폭이므로 이 절감이 곧 속도가 된다.

### 수락/거부 규칙 — 왜 품질이 안 깨지는가

이 기법의 가장 중요한 성질은 **최종 출력 분포가 target 모델 단독 출력과 수학적으로
동일**하다는 것이다 (rejection sampling).

1. draft가 K개 토큰을 확률과 함께 제안
2. target이 한 번의 forward로 각 토큰의 확률을 계산
3. 토큰별 판정:
   - **target 확률 ≥ draft 확률** → 그대로 수락
   - **target 확률 < draft 확률** → `target/draft` 비율의 확률로 수락 (예: 0.25/0.35 ≈ 71%)
4. **거부되는 순간 그 뒤의 draft 토큰은 전부 폐기** — autoregressive라 앞 토큰이 바뀌면
   뒤는 무의미하다. target이 수정된 분포(거부된 토큰 확률을 빼고 재정규화)에서 직접 샘플링해 이어간다

이 "수락 확률 + 대체 샘플링"을 더하면 target의 원래 분포가 **정확히 복원**된다.
그래서 draft가 아무리 작아도 품질은 target 그대로다 — 카카오 발표에서
draft를 125M(target 대비 수백분의 1)까지 줄여도 문제없었고, 수락률이 0.6~0.8
나왔다는 사례가 이를 뒷받침한다.

> **수락률(acceptance rate)이 이득을 결정한다.** 거부가 잦으면 draft 연산 + 검증 연산을
> 쓰고도 토큰은 하나씩 나오는 셈이라 오히려 손해다. 수락률을 높이는 길은
> draft를 target과 정렬시키는 것 — 같은 계열 모델, 또는 target에서 증류.

### Draft를 고르는 4가지 방법

| 방법 | 방식 | 장점 | 단점 |
|---|---|---|---|
| 기존 소형 모델 | 같은 계열의 작은 모델 (공격적 양자화 권장) | 구현 간단 | 정렬이 안 맞으면 수락률 낮음 |
| 증류 | target에서 draft를 직접 학습 | 수락률↑ | 별도 학습 필요 |
| **셀프 드래프팅** (Medusa, EAGLE) | target 자체에 예측 헤드/모듈 추가 | **별도 모델 불필요, 메모리 절약, 정렬 우수** | 추가 학습 필요 |
| **N-gram** | 프롬프트 내 반복 패턴을 테이블 매칭 | **오버헤드 거의 0, 최간단** | 자유 생성에는 약함 |

셀프 드래프팅의 동기가 실용적이다 — draft 모델도 결국 HBM에 올라가야 하는 별도
가중치다. 안 그래도 부족한 GPU 메모리에 모델 두 개를 얹느니, target 안에 가벼운
예측 헤드만 붙이자는 것. Medusa는 여러 개의 예측 헤드로 여러 스텝 앞을 병렬
예측하고(이름도 머리 여럿인 메두사에서), **EAGLE은 토큰이 아니라 target의 은닉
상태(hidden state)를 예측**해 더 안정적이다. EAGLE-2는 동적 draft tree, EAGLE-3는
여러 레이어의 feature를 융합해 정확도를 더 올렸고, 현재 가장 성능 좋은 셀프
드래프팅 기법으로 꼽힌다.

N-gram은 프롬프트 앞부분의 n-토큰 시퀀스를 테이블로 만들어 매칭만 한다. 계산이
없으니 오버헤드가 거의 0이고, **JSON/SQL 같은 구조화 출력이나 템플릿 채우기**처럼
출력이 입력을 많이 재사용하는 상황에 잘 맞는다. 교재는 다른 기법을 시도하기 전
**가장 먼저 켜볼 것**을 권한다.

### K 튜닝

K(draft 토큰 수)가 크면 속도 상한이 높아지지만, 수락률이 낮으면 버려지는 토큰만
는다. 실무 권장:

- 시작은 2~4, **최적은 보통 4~8**
- 구조화 출력·함수 호출처럼 예측 가능성이 높으면 **16~32까지**
- 위치별 수락률을 모니터링 — 예: K=6에서 [0.8, 0.7, 0.6, 0.5, 0.10, 0.02]라면
  5·6번째 추측은 이득이 없으니 K를 줄인다

### 한계 — 조건부 기법이라는 것

1. **decode에만 유효하다.** 긴 입력의 prefill처럼 이미 compute-bound인 구간에는 효과가 없다
2. **큰 배치가 효과를 지운다.** 배칭이 병목을 memory→compute로 옮기면, 추측의 추가 연산이
   처리량을 오히려 깎는다. 지연시간은 여전히 개선될 수 있으나 throughput은 손해
3. 두 모델을 한 GPU에서 효율적으로 돌리는 엔지니어링이 어렵다 → 최근 트렌드가
   셀프 드래프팅 + n-gram으로 이동한 이유
4. 정적 K의 한계 → adaptive K 연구 활발

> **스터디에서 공유된 실측이 이 한계에 해당한다.** VRAM 16GB 데스크탑
> (Qwen3-8B-FP8)에서는 **동시성 1에서도 vanilla vLLM이 이미 GPU 연산 98%**였고,
> 그 상태에서 추측 디코딩을 켜자 전부 역효과가 났다. PCIe 대역폭은 한참 남는 것을
> 확인했으니(TX/RX 수 GB/s 수준) 병목은 GPU 연산 쪽이다. 추측 디코딩은 "메모리
> 병목의 일부를 GPU 연산으로 옮기는 거래"이므로, **옮겨 받을 연산 여유가 없으면
> 성립하지 않는다.** 교재의 기준 실험이 A100-80GB인 이유이기도 하다. GPU 세대가
> 올라갈수록 연산/대역폭 비율이 커져서 실무용 GPU에서는 대부분 효과가 나온다.

### 교재 실험 결과 (A100, Qwen3-32B — 인용)

vanilla / n-gram(K=6) / improved n-gram(K=4, 매칭범위 2~128) / EAGLE-3 4구성을
동시성 1과 16에서 비교한 교재 수치다.

| 조건 | 결과 |
|---|---|
| **동시성 1** (저부하) | improved n-gram +16%, **EAGLE-3 약 2배** (56.5 vs 28.9 tok/s — 1.96배, 계산 확인) |
| **동시성 16** (고부하) | n-gram 두 설정 모두 **vanilla보다 느림**. EAGLE-3는 우세하나 격차 축소 |
| TTFT vs ITL (동시성 16) | EAGLE-3는 ITL을 크게 얻는 대신 **TTFT가 오히려 증가** (추측 헤드 오버헤드) |

이론 그대로다 — ① 저부하(memory-bound)에서 드래프팅은 거의 공짜, ② 고부하
(compute-bound 전환)에서는 오버헤드가 손해로 작용, ③ TTFT↔ITL 트레이드오프는
SLA가 무엇에 민감한지에 따라 선택이 갈린다. 그리고 최적 기법과 K는 데이터셋
의존적이라 **실제 트래픽으로 수락률을 재면서 튜닝**해야 한다.

### 직접 실측 — 순위가 뒤집혔다 (RTX 4090, Qwen3-8B-FP8)

같은 4구성을 4090에서 재현했다 ([실습 기록](labs/advanced-optimization-and-serving-frameworks.md)).

| 조건 | 결과 (vanilla 대비) |
|---|---|
| 동시성 1 | **improved n-gram 1.79x** > n-gram 1.65x > EAGLE-3 1.13x |
| 동시성 16 | **improved n-gram 1.69x**, n-gram 0.99x, **EAGLE-3 0.50x** (TTFT 4.35초) |

- 교재(A100·32B)에서 약 2배 우세였던 EAGLE-3가 여기선 **최하위**다. 수락률은 41%로
  4구성 중 1위인데도 — 타깃이 8B로 작아 speculator 자체 forward 비용이 상대적으로
  커진 탓(해석). 결정 변수는 수락률이 아니라 **"수락 이득 − speculator 비용"**이다.
- vanilla 동시성 1의 GPU util이 94%로 수치상 "포화"였지만 improved n-gram이 +79%를
  냈다. nvidia-smi의 util%는 SM이 바쁜 시간의 비율일 뿐이라, memory-bound decode에서는
  추측 디코딩 전제 판정 지표로 못 쓴다 — 위 16GB 사례의 98%와 이번 94%는 숫자가
  비슷해도 실제 연산 여유가 달랐다.

---

## Part 2. 병렬화 — 모델이 GPU 하나에 안 들어갈 때

### 문제의 크기부터

추론에서도 분산이 필요해지는 이유는 두 가지다.

- **가중치**: 100B를 넘는 모델은 80GB GPU 한 장을 넘는다. 극단적으로 가중치 1.6TB면
  B200(192GB) 8장 노드(1.5TB)로도 안 들어간다
- **KV 캐시**: 16비트·128K 컨텍스트면 **요청당 43GB**. 동시 요청이 쌓이면 캐시만으로
  최신 GPU의 VRAM을 넘긴다

그리고 GPU가 여러 장이 되는 순간, 지금까지 없던 고려사항이 등장한다 — **통신**.
단일 GPU에서는 모두가 같은 메모리를 쓰지만, 분산에서는 한쪽 연산 결과를 다른 쪽이
받을 때까지 기다린다. "무엇을, 얼마나, 어떤 매체로 전송하는가"가 성능을 결정한다.

### 통신 패턴 두 개만 기억하면 된다

집합 통신(collective communication, NCCL 등 xCCL 라이브러리)에는 여러 패턴이
있지만, 추론에서 결정적인 것은 둘이다.

| 패턴 | 쓰는 곳 | 특성 |
|---|---|---|
| **all-reduce** | **Tensor Parallelism** | 모든 GPU의 부분합을 모아 전체에 다시 뿌림 |
| **all-to-all** | **Expert Parallelism (MoE)** | 모든 GPU가 서로 데이터를 교차 전송 — 가장 혼잡 |
| point-to-point | Pipeline Parallelism | 이웃 GPU에 1:1 전달 — 가장 가벼움 |

그리고 이 둘(all-reduce, all-to-all)의 공통 결론: **NVLink 도메인을 넘기면 안 된다.**

| 링크 | 대역폭 |
|---|---|
| NVLink (B200, 노드 내) | 1.8 TB/s |
| NVLink (H100, 노드 내) | 900 GB/s |
| 노드 간 IB/RoCEv2 400Gbps | 50 GB/s |

B200 기준 노드 내와 노드 간의 격차는 1,800 ÷ 50 = **36배**다 (계산 확인 —
H100 기준으로는 18배였다). 통신량이 많고 저지연이 필요한 패턴을 이 느린 구간에
태우면 성능이 무너진다.

### TP — 너비로 쪼갠다

Tensor Parallelism은 **레이어 하나를 여러 GPU에 수평 분할**한다. 데이터는 같고
가중치가 다르다 — 가중치 100개를 4장에 나누면 각자 25개씩 들고 부분 행렬곱을
수행한 뒤 결과를 합친다.

이게 성립하는 이유는 첫 노트에서 확인한 구조적 사실 그대로다. MLP의 원소별 연산과
어텐션의 헤드별 연산은 **서로 독립**이라 쪼개서 계산해도 되고, 합칠 때만 통신하면
된다. 문제는 그 "합칠 때"의 빈도다:

```
트랜스포머 레이어 1개 = 어텐션 all-reduce 1회 + MLP all-reduce 1회 = 2회
80레이어 모델        = 토큰당 2 × 80 = 160회의 all-reduce        (계산 확인)
```

**토큰 하나에 160번**의 전 GPU 동기화. NVLink에서는 감당되지만 노드 간 링크에서는
불가능하다. 그래서 **Megatron rule**: TP 그룹은 한 노드 안의 NVLink GPU(보통 8장)를
넘지 않는다. 스터디에서 덧붙인 단서 — **같은 노드라도 PCIe 연결이면 TP는 비권장**이다.
NVLink가 전제다.

보상은 명확하다. 8-way TP면 각 GPU가 가중치의 1/8, 연산의 1/8만 담당한다.

### PP — 깊이로 쪼갠다

Pipeline Parallelism은 **레이어 블록을 GPU별로 순차 배정**한다. 80레이어를 4장에
나누면 GPU1이 L1~20, GPU2가 L21~40… 순으로 배치가 조립 라인처럼 흐른다.

통신 비용 차이가 크다:

```
TP (8 GPU, 80층):  토큰당 all-reduce 160회 — 전 GPU 동기화
PP (4 스테이지):    토큰당 point-to-point 3회 — 경계에서만       (계산 확인)
```

160 vs 3. 그래서 **PP는 느린 노드 간 링크에서도 동작한다.** 전송을 다음 배치
연산과 겹치면(overlap) 통신 시간이 연산 뒤에 숨어 추가 지연도 없다.

대가는 **파이프라인 버블**이다. 배치 하나만 흘리면 각 GPU는 자기 차례 외엔 논다:

```
idle 비율 = (p−1) / (m+p−1)     p=스테이지 수, m=마이크로배치 수

m=1,  p=4  →  75% 유휴          (계산 확인)
m=4,  p=4  →  43%
m=32, p=4  →  9%
```

해법이 **마이크로배칭** — 배치를 잘게 쪼개 파이프라인에 연속 투입하면 중간이 계속
차 있다. fill/drain의 고정 비용(p−1)은 그대로지만 m이 커질수록 비중이 줄어든다.
서빙에서는 continuous batching이 같은 역할을 한다. 특히 **decode는 토큰을 하나씩
만드니 배치 하나로는 스테이지 하나만 일하고 나머지가 전부 논다**(1/4 busy) —
여러 요청을 계속 흘려 넣어야 파이프가 찬다.

### 선택 흐름 — 교재의 의사결정 플로우

```
모델이 GPU 1장에 들어가나?
 ├─ Yes → 분산하지 마라. 통신 오버헤드가 없는 단일 GPU가 최선
 ├─ No  → 양자화부터 (FP8이면 절반, W4A16이면 1/4)
 │        └─ 그래도 안 되면 → 노드 내 멀티 GPU
 │             ├─ NVLink 있음 → TP (이상적)
 │             └─ PCIe만     → PP 위주, TP 최소화
 └─ 노드 하나(8장)로도 안 되면 → 멀티노드
      └─ 노드 내부 = TP, 노드 간 = PP   (예: TP=8 × PP=2 = 16장)
```

깔린 원리 두 가지. ① GPU 메모리 대역폭(H100 ~3TB/s)이 GPU 간 인터커넥트(NVLink
900GB/s)보다 3배 이상 빠르므로 **GPU를 늘려도 선형으로 빨라지지 않는다** — 하위
GPU 여러 장보다 상위 GPU 한 장이 낫다. ② H100 1장 노드 8개 ≠ H100 8장 노드 1개.
후자만 NVLink로 묶인다.

vLLM 설정은 한 줄이다:

```bash
vllm serve <model> --tensor-parallel-size 8 --pipeline-parallel-size 2
# 총 GPU = TP × PP (곱). TP는 어텐션 헤드 수의 약수여야 한다
# 멀티노드는 --distributed-executor-backend ray (단일 노드 기본은 mp)
```

### EP — 전문가를 나눈다 (MoE)

MoE는 FFN을 여러 **전문가(expert)** 로 쪼개고, **라우터**가 토큰마다 소수의
전문가만 활성화하는 구조다. 라우터 자체도 학습되는 소형 신경망이다 —
`G(x) = Softmax(x·W_g)`로 각 전문가에 확률을 매기고 top-K만 계산에 참여시킨다.

왜 효과가 큰가. 첫 노트에서 확인했듯 **FFN이 전체 파라미터의 약 2/3를 차지**한다.
그 구간의 활성 연산을 top-K/전체 비율로 줄이면(예: 8개 중 2개 = 1/4), 연산과 함께
**HBM에서 읽어올 가중치 이동량도 같이 준다.** 최신 대형 모델이 죄다 MoE인 이유다.

| 모델 | 구성 | 특징 |
|---|---|---|
| DeepSeek-V3 | routed 256 + **shared 1**, top-8 | fine-grained + 공유 전문가 |
| Llama 4 Maverick | routed 128 + shared 1 | |
| Kimi K2 (1T) | routed 384 + shared 1 | |
| **Qwen3-235B** | routed 128, **shared 없음** | **의도적으로 공유 전문가 생략** |

- **Shared expert**: 문법·구두점처럼 모든 토큰에 공통인 패턴을 전담하는, **라우터를
  거치지 않고 항상 켜져 있는** 전문가. DeepSeek 계열이 도입
- **Fine-grained**: 전문가를 잘게 쪼갤수록 조합 다양성이 폭증한다 — 16개 중 2개는
  C(16,2)=120가지지만, 64개 중 8개는 C(64,8) ≈ **44.3억 가지**다 (계산 확인)

**Expert Parallelism**은 이 전문가들을 여러 GPU에 분산 배치한다. 문제는 통신 패턴:
토큰이 라우팅된 전문가가 다른 GPU에 있으면 보내고(dispatch) 받아와야(combine) 한다.
**MoE 레이어당 all-to-all 2회**, 게다가 어떤 토큰이 어디로 갈지는 배치마다 달라
**통신 패턴을 고정할 수 없다.** 노드를 넘어가면 MoE 레이어 시간의 70% 이상, E2E
추론 지연의 약 40%가 이 통신이 된다.

그래서 대응이 두 갈래다 — ① EP도 가능한 한 NVLink 안에서, ② 통신 자체를 최적화한
전용 라이브러리 (**DeepEP**, DeepSeek 공개). 스터디에서 든 비유가 좋았다: FlashAttention이
어텐션의 HBM 왕복을 알고리즘으로 줄였듯, DeepEP는 dispatch/combine 경로를 전용
설계로 줄인다. vLLM에서는 `--enable-expert-parallel`로 켜고, 전문가별 부하 불균형
(hot expert)은 EPLB(Expert Parallel Load Balancer)가 동적으로 재조정한다.

### DP — 통째로 복제한다

Data Parallelism은 모델을 쪼개는 게 아니라 **완전한 복제본을 여러 개** 두고
로드밸런서가 요청을 분산하는, 소프트웨어의 수평 확장 그대로다. 스터디에서 든 비유로는
K8s의 stateless Deployment — 학습과 달리 **추론에서는 gradient 동기화가 없으므로
복제본 간 통신이 0바이트**다. 처리량과 가용성을 사고, 개별 요청의 지연시간은
그대로라는 것까지 LB의 상식과 같다.

다만 LLM의 라우팅은 웹서버보다 정교해야 한다. 요청마다 비용 편차가 크고 GPU가
비싸기 때문이다. 라운드로빈 → least connections → 지연시간 기반을 지나, LLM
특화의 종착지는 **cache-aware routing** — 같은 프리픽스의 요청을 이미 그 KV 캐시를
가진 복제본으로 보낸다. 프리픽스 캐싱 노트에서 본 그 라우팅이 여기서 DP의 핵심
요소로 재등장한다 (NVIDIA Dynamo의 KV Router, llm-d 라우터가 이 역할).

vLLM은 DP 배포를 3가지 모드로 지원한다 — **Internal LB**(진입점 하나, 내부
코디네이터가 분배 — 나머지 노드는 `--headless`로 순수 연산 워커), **Hybrid LB**
(노드별 API 서버 + 외부 LB), **External LB**(rank마다 독립 엔드포인트). MoE에서는
EP와 결합되어 `EP 크기 = TP × DP`로 전문가가 샤딩된다.

**네 병렬화의 관계**: DP가 가장 바깥 레이어다. 안쪽에서 TP(너비)·PP(깊이)·EP(전문가)로
쪼갠 "샤딩된 유닛"을 만들고, 그 유닛 전체를 DP로 복제한다.

---

## Part 3. Prefill-Decode 분리 — 간섭을 물리적으로 없앤다

### 하나의 프로비저닝으로는 타협뿐이다

prefill은 compute-bound, decode는 memory bandwidth-bound — 정반대의 하드웨어 자원을
요구하는 두 국면이 같은 GPU를 공유하면 **간섭(interference)** 이 생긴다. chunked
prefill로 완화는 되지만, 한쪽에 맞춘 설정은 반드시 다른 쪽을 희생시킨다. 어느 쪽도
100% 만족시키지 못하는 타협이다.

PD 분리는 이를 물리적으로 끝낸다 — **prefill 전용 GPU 풀과 decode 전용 GPU 풀**로
나누고, prefill이 만든 KV 캐시를 decode 풀로 넘긴다.

얻는 것 네 가지:

1. **TTFT/ITL 독립 최적화** — 입력 위주 워크로드면 prefill 풀만, 출력 위주면 decode
   풀만 비대칭으로 증설. 간섭이 없어져 ITL도 안정적·예측 가능해진다
2. **워크로드별 독립 튜닝** — prefill은 텐서 코어를 포화시키는 배치로, decode는
   대역폭 병목이 풀리는 배치로. 병렬화 전략(TP/PP)도 단계별로 다르게
3. **하드웨어 선택의 자유** — H100과 H200은 FLOPS가 같고 메모리(80 vs 141GB)와
   대역폭(3.35 vs 4.8TB/s)만 다르다. prefill=H100, decode=H200(또는 저비용 L40S)
   같은 이종 조합으로 비용 최적화
4. **비대칭 스케일링** — 요청 진입점인 prefill은 버스트에 공격적으로, decode는
   예측 가능하게

논문 수치(인용): DistServe는 동일 SLA를 **12.6배 타이트한 자원 예산**으로 달성,
Splitwise는 **80% 비용으로 1.4배 처리량**(비용 대비 1.75배 효율 — 계산 확인).

### 핸드오프 비용 — KV 캐시 전송이 전제조건이다

분리의 대가는 명확하다. prefill이 만든 KV 캐시를 decode 풀로 **네트워크 너머로**
보내야 한다. 이 비용이 분리의 이득을 넘으면 하지 않느니만 못하다.

전송량을 직접 계산해봤다.

```
8B급 모델(32층, KV헤드 8, head_dim 128, fp16):
  토큰당 KV = 2×32×8×128×2 = 131,072 B = 0.125 MB
  1,024토큰 입력  → 약 0.13 GB          (교재의 0.1~0.15GB와 일치)
  10,240토큰 입력 → 약 1.3 GB            (입력 길이에 선형)
  × 처리량 16 req/s → 초당 약 25 GB 전송 필요

OPT-66B(64층, hidden 9216, fp16), 512토큰:
  2×64×9216×2 × 512 = 1.13 GiB          (스터디 자료 수치와 일치, 계산 검증)
  × 초당 10회 전송 → 약 90 Gb/s
```

이 요구량을 링크별 감당 능력에 대보면 판정이 나온다:

| 링크 | 대역폭 | 25GB/s 감당 |
|---|---|---|
| NVLink (노드 내) | 900GB/s+ | 여유 |
| InfiniBand NDR (노드 간, RDMA) | 50~100GB/s | 감당 |
| PCIe (RDMA 없음) | ~10GB/s | **불가 — 병목** |

즉 **PD 분리의 전제조건은 KV 캐시 발생량을 감당하는 고속 인터커넥트**다. 25GbE
이더넷 수준이면 90Gb/s 요구를 못 받아 오히려 손해다.

노드 간 전송 자체는 4가지 최적화로 오버헤드를 줄인다 — ① 청크 단위 스트리밍 전송,
② 비동기 전송을 prefill 연산과 오버랩(PP의 "send hides inside"와 같은 원리),
③ KV 캐시가 레이어별로 독립이므로 **완료된 레이어부터 먼저 전송**해 decode를 일찍
시작, ④ KV 캐시 압축·양자화. 다 합치면 분리 오버헤드를 **요청당 전체 지연의 1%
미만**까지 줄일 수 있다는 실증이 있다 (Wang et al., 2025).

> 양자화 노트에서 다룬 양자화가 FFN 가중치 쪽이었다면, 여기서는 **KV 캐시(어텐션
> 쪽) 양자화**가 전송량 절감 수단으로 재등장한다. 같은 기법이 다른 병목에 다시 쓰인다.

### 실제 구현 — NIXL과 llm-d

전송 계층의 사실상 표준이 **NIXL**(NVIDIA Inference Xfer Library — NIC와 다른
것이다)이다. GPU Direct RDMA로 **CPU와 OS 메모리 복사를 거치지 않고 GPU 메모리 간
직접 전송**한다 (zero copy). 필요한 KV 블록 인덱스만 골라 pull하는 방식이라 메모리
오버헤드도 없다. 스택은 NIXL → UCX → IB/RoCE/TCP 계층이고, 백엔드로 GDS·NVMe-oF·
S3까지 지원한다.

llm-d의 P/D 분리 흐름(공개 발표 기준)이 구체적이다:

1. Envoy Gateway가 요청을 받고 스케줄러(EndPointPicker)가 P/D 분리 여부와 워커 선택
2. decode Pod의 사이드카가 prefill Pod에 **max_tokens=1로 원격 prefill 요청** —
   토큰 하나만 만들고 끝내는 건 KV 캐시 계산이 목적이기 때문
3. prefill 완료 후 decode Pod가 **NIXL로 KV 캐시를 pull**
4. decode Pod가 이어서 토큰 생성 → 응답

같은 역할을 하는 시스템으로 NVIDIA Dynamo, Mooncake(Kimi — K8s 없이 베어메탈에서도
동작), SGLang(96×H100 클러스터 사례) 등이 있다.

### 무조건 좋은 게 아니다

공개 벤치마크에서 P/D 노드 비율이 워크로드에 안 맞으면 **완전히 실패(타임아웃)**
하거나 오히려 나빠졌다 — prefill-heavy 부하에 decode 노드를 과하게 배정하면
prefill이 병목이 되어 무너지고, decode-heavy 부하에서는 분리 이득 자체가 거의 없다.
MoE에서는 prefill 풀과 decode 풀의 **EP 크기까지 다르게** 가져간다(예: prefill
EP32 조밀 / decode EP144 성김 — decode 쪽은 continuous batching으로 큰 배치를
모아야 희소성이 산다).

교재의 판정: **고급 TTFT·ITL 튜닝이 필요한 대형 모델에만 PD 분리를 쓰고, 소형
모델은 단순한 통합 서빙을 유지하라.** 분리 운영의 복잡도를 상쇄할 만큼 워크로드가
커야 한다.

---

## Part 4. 고급 KV 캐싱 — 긴 컨텍스트 시대의 캐시 관리

### RAG vs CAG, 그리고 비용의 반전

추론·코딩 에이전트가 긴 컨텍스트를 계속 키우면서, KV 캐시는 모델 내부 구현
디테일이 아니라 **서빙 스택의 일급 시민**이 됐다. 저장하고, 스케줄링하고, 디바이스
간에 옮기고, 축출하는 대상이다.

긴 컨텍스트를 다루는 두 전략을 비교한다:

- **RAG**: 쿼리 시점에 관련 청크만 검색해 프롬프트에 주입 — 컨텍스트와 TTFT를
  관리 가능한 수준으로 유지
- **CAG**: 관련 지식 대부분을 **KV 캐시로 미리 계산해 재사용** — 프리픽스 캐싱이
  CAG의 소박한 구현이다. 컨텍스트 윈도우가 100k~1M으로 커지고 "lost in the middle"
  문제가 완화되면서 실용권에 들어왔다

교재의 예시로 비용을 직접 계산해봤다. 시스템 프롬프트 500토큰, RAG는 500토큰 청크
10개 검색, CAG는 100,000토큰 컨텍스트 전체를 캐싱, 사용자 프롬프트 500토큰.
캐싱된 입력 단가를 일반 입력의 1/10($0.125 vs $1.25 / 1M토큰)로 잡으면:

```
RAG:  캐시 500 + 비캐시 5,500   → (500×0.125 + 5,500×1.25)/10⁶  = $0.0069
CAG:  캐시 100,500 + 비캐시 500 → (500×1.25 + 100,500×0.125)/10⁶ = $0.0132
```

**CAG의 TTFT는 약 10배 빠른데(비캐시 prefill이 5,500 vs 500토큰), 비용은 약 1.9배
비싸다** (계산 검증). 캐싱된 토큰도 공짜가 아니고(벤더별 10~25% 단가, Gemini는
저장 시간당 요금, Claude는 최초 쓰기 요금까지), CAG는 캐싱 총량 자체가 압도적이기
때문이다. "빠른 것"과 "싼 것"이 갈린다 — 지연시간이 최우선이면 CAG, 비용 효율이면
RAG가 여전히 유리할 수 있고, 캐시 재사용 빈도에 따라 케이스별로 계산해야 한다.

### 셀프호스팅이면 계산이 달라진다 — LMCache

위 계산은 외부 API 단가 기준이다. 자체 호스팅이면 **KV 캐시 관리를 직접 최적화**해
CAG를 훨씬 싸게 만들 수 있다. 그 대표 프레임워크가 LMCache다(vLLM·SGLang 지원,
TensorRT-LLM은 예정).

**① 계층형 오프로딩** — GPU 메모리가 부족하면 저장 계층을 늘린다:

```
GPU HBM (비싸고 작음) → CPU DRAM (약 3배 공간) → SSD (최대 50배) → 네트워크 스토리지
```

효과가 구조적이다. 일반 프리픽스 캐싱은 GPU 메모리에 장문 컨텍스트 캐시 하나가
겨우 들어가서, 테넌트 4곳을 서빙하려면 **모델 복제본 4개 + 캐시 인지 라우팅**이
필요했다. 오프로딩이 있으면 **인스턴스 하나가 캐시 4개를 CPU에 들고 GPU와 스왑**
하면 된다 — 단일 테넌트 요청량이 인스턴스를 포화시키지 못하는 상황이면 그대로
4배 비용 절감이다.

**② 압축** — CacheGen이 KV 캐시 분포 기반으로 비트스트림 인코딩해 크기를 줄인다.
전송이 빨라지고, GPU에 남는 공간만큼 배치가 커진다.

**③ 블렌딩(CacheBlend)** — RAG의 제약을 겨냥한다. RAG 청크는 순서·조합이 매번
달라 **프리픽스 매칭이 깨진다**. 그렇다고 청크별 KV 캐시를 단순히 이어붙이면
안 된다 — self-attention은 청크 사이의 교차 토큰 관계까지 계산하므로 concat은
품질을 깨뜨린다. CacheBlend는 교차 관계 보존에 필요한 **일부(기본 15%)만 선택적
재계산**해서, 전체 재계산 비용 없이 비-프리픽스 캐시를 재사용한다. RAG와 CAG가
양자택일이 아니게 되는 지점이다.

**KV 전송도 지원한다** — 기존에는 NIXL을 감싼 방식(1p1d, XpYd)이었는데 문서가
이를 legacy·비추천으로 옮겼고, 자체 **MP 모드**(LMCache를 별도 데몬으로 분리,
vLLM과 ZMQ 통신 — 워커가 죽어도 캐시 유지)로 대체하는 흐름이다.

### 교재 실험이 말해주는 조건 (A100, Qwen3-14B — 인용)

- **콜드(캐시 적중 0)에서는 LMCache가 항상 더 느리다** — 캐시 처리 오버헤드.
  재사용 없는 워크로드(짧은 프리픽스, 랜덤 요청)면 켜지 않는 게 기본값
- 웜업 후: GPU 캐시에 다 들어가는 동안은 막상막하 → vanilla vLLM은 캐시가 넘쳐
  LRU 축출이 시작되는 지점(실험에서는 10번째 요청)부터 **재계산으로 회귀(6~7초)**,
  LMCache는 CPU에서 가져와 **약 1초** 유지
- 동시 5요청: vanilla는 KV 캐시 + activation을 같은 GPU 메모리로 감당하며 초당
  약 4,700토큰에서 병목, LMCache는 여유 — **처리량 약 16배 차이**

프리픽스 캐싱 노트의 교훈이 그대로 이어진다 — **캐시는 재사용이 실재하는
워크로드에서만 이득이 된다.** 시스템 프롬프트·테넌트 지식처럼 공유 구간이 크면 이득이 크고,
랜덤 프롬프트면 오버헤드만 남는다.

**직접 실측 (RTX 4090, Qwen3-14B-FP8)** — GPU 캐시(29,248 토큰)보다 29% 큰
working set을 같은 순서로 순차 재접근시키자 vanilla vLLM은 **prefix cache 히트
0/88,027**이 됐다. LRU가 각 문서를 재사용 직전에 축출해 웜과 콜드의 TTFT가 같아지는
최악 케이스다(교재의 "10번째부터 회귀"보다 극단). LMCache는 같은 조건에서 CPU로부터
약 16GB/s로 KV를 복원해 **웜 TTFT 2.7배(169→63ms), 동시 5요청 2.4배**를 냈고,
콜드는 +8% 손해였다 — 위 명제의 양면을 모두 실측으로 확인
([실습 기록](labs/advanced-optimization-and-serving-frameworks.md)).

---

## Part 5. 서빙 프레임워크 — vLLM의 속과 3개의 대안

### 왜 전문 프레임워크인가

TensorFlow Serving, TorchServe, Triton 같은 범용 서빙은 이미지·정형 데이터용으로
설계됐다 — 입력 크기가 짧고 텐서 형태가 고정이며 지연이 예측 가능한 세계다. LLM은
다르다: ① 자기회귀 생성으로 세션이 수 초~수 분 열려 있고, ② 입력이 수 토큰~백만
토큰으로 폭증하며 KV 캐시가 병목이고, ③ 가변 길이 요청에 정적 배칭이 무력하고,
④ 토큰 단위 스트리밍과 수백 ms TTFT를 요구받고, ⑤ 비싼 GPU를 낭비 없이 짜내야
한다. 이 다섯을 풀려고 나온 것이 전문 LLM 서빙 프레임워크다.

구조적으로 두 티어로 나뉜다:

```
오케스트레이션 티어   NVIDIA Dynamo, llm-d(K8s 네이티브)
                     — 라우팅/스케줄만 한다. "No engine, no token" — 토큰 생성은 안 함
엔진 티어            vLLM(PyTorch Foundation, 멀티벤더) · SGLang · TensorRT-LLM(NVIDIA 전용) · TGI(HF)
                     — 스케줄러 / KV 캐시 / 커널의 3단 구조로 실제 추론 수행
```

### vLLM 아키텍처 — 두 진입점, 여섯 계층

사용 방식은 둘이다. **LLM 클래스**(인프로세스 파이썬, 오프라인/배치용)와
**API 서버**(`vllm serve`, OpenAI 호환 HTTP, 프로덕션용).

내부는 계층 구조다:

```
LLMEngine        공개 API·요청 생명주기 관리 (진입점)
 └ EngineCore    중앙 오케스트레이터 — "내부 루프"
    └ Scheduler          자원 배분의 교통 관제사 (모델 무관 최적화 담당)
       └ SchedulerOutput  "작업 지시서" — 스케줄러↔실행기 사이의 계약
          └ ModelExecutor   워커 프로세스들 조율
             └ GPUWorker      프로세스별 디바이스·모델 생명주기
                └ GPUModelRunner  실제 forward pass 실행
```

**관심사 분리가 설계의 뼈대다.** Scheduler는 모델을 모른 채 시스템 최적화(배칭,
캐싱, 공정성)를 결정하고, GPUWorker는 스케줄링을 모른 채 연산만 한다. 초기화 시
`tensor_parallel_size=4`면 워커 프로세스 4개가 스폰되고, 메인↔워커는 양방향
메시지 큐(rpc_broadcast_mq / worker_response_mq)로 통신한다. 모델 구현체는 내부
레지스트리에서 이름으로 조회된다(Qwen → Qwen3NextForCausalLM 식).

요청 하나의 흐름은 4단계다: **Processor**(토큰화·검증 → Request 객체) →
**Scheduler**(다음 배치와 토큰 결정 — 최적화가 여기서 적용) →
**Executor→GPUWorker**(forward pass) → **Output Processor**(스트리밍 응답).
스케줄러가 결정을 내리면 사실상 끝난 것이고, 나머지는 실행이다.

### Scheduler 딥다이브 — 토큰 단위로 스케줄링한다

Scheduler는 5가지 책임을 진다 — 자원 오케스트레이션(WAITING/RUNNING 큐),
**토큰 단위 스케줄링**, 최적화 통합 허브, 동적 부하 분산, 생명주기 관리(FCFS·
우선순위·선점).

핵심 차별점은 **요청 단위가 아니라 토큰 단위**라는 것. prefill/decode를 단계로
가르지 않고, 매 스케줄링 스텝마다 "어느 요청의 토큰 몇 개를 이번 forward에
넣을지"를 전역 토큰 예산 안에서 결정한다. 구현 수준에서는 두 값의 간극을 좁히는
문제다:

```python
num_new_tokens = (request.num_tokens_with_spec       # 처리해야 할 총 토큰(추측 포함)
                  + request.num_output_placeholders
                  - request.num_computed_tokens)      # 이미 처리된 토큰
```

**이 간극을 좁히는 자리가 곧 모든 최적화의 공통 접점이다** — chunked prefill은
num_new_tokens에 상한을 걸고, 프리픽스 캐싱은 num_computed_tokens를 캐시 조회로
건너뛰어 올리고, 추측 디코딩은 num_tokens_with_spec에 draft 토큰을 얹는다.
서로 다른 기법들이 같은 산수 하나로 합류한다.

스케줄링 사이클: 상태 수집·예산 갱신 → **RUNNING 우선 처리**(이미 KV 블록을 점유
중이므로 — 이때 chunked prefill·프리픽스 캐싱·추측 디코딩 적용, 자원 부족 시
저우선 선점) → 남는 예산으로 WAITING 활성화 → 후처리(LoRA·인코더·draft 확정) →
SchedulerOutput 조립.

> **우선순위 결정과 토큰 스케줄링의 분리.** 큐가 "누구부터"를 정하고, 토큰 산수가
> "몇 개씩"을 정한다. 이 분리 덕에 우선순위 정책과 실행 최적화를 서로 독립적으로
> 조합할 수 있다.

### 계층화된 최적화 — futureproof의 설계

vLLM의 설계 철학: **최적화는 그것이 속한 계층에서 한다.** 모델과 하드웨어가 너무
빨리 바뀌므로, 특정 모델·하드웨어에 최적화를 하드코딩하면 시스템이 금방 낡는다.

| 계층 | 범위 | 예 |
|---|---|---|
| Scheduler | 시스템 전반, **모델 무관** | 배칭, 프리픽스 캐싱, 공정성 |
| ModelExecutor | 모델 아키텍처별 | Transformer 융합 어텐션 커널 |
| 모델 레이어 | 컴포넌트별 | KV 재사용, FlashAttention, 연산자 융합 |
| CustomOp | 하드웨어별 | CUDA 커널, 텐서 코어, 양자화 연산자 |

새 GPU가 나오면 CustomOp만, 새 아키텍처가 나오면 ModelExecutor 쪽만 갈아끼우면
된다. 위로 갈수록 범용, 아래로 갈수록 특화 — 한 계층의 변경이 다른 계층으로 새지
않는다.

### 나머지 셋 — 성격이 뚜렷하다

**TensorRT-LLM** — NVIDIA가 만든 자사 GPU 전용 라이브러리. 모델을 튜닝된 TensorRT
엔진으로 컴파일해 Tensor Core·CUDA를 극한까지 쓴다. in-flight batching, FP8/FP4
양자화, Triton·Dynamo와 긴밀 통합. 범용성이 아니라 **NVIDIA 하드웨어에서의 최대
실전 성능**이 목표다. Ada(SM89)부터 Blackwell까지 지원하고, 도커 이미지가 55GB급
이라는 실무 디테일도 있다.

**SGLang** — vLLM의 직접 경쟁자. **RadixAttention**(radix tree로 여러 호출에 걸친
KV 캐시 재사용)이 대표 기능이고, 구조화 출력(JSON/정규식/EBNF)과 에이전틱·멀티턴
워크플로우에 강하다. 하드웨어도 NVIDIA 외에 AMD·CPU·TPU·Ascend까지 — 멀티벤더
이식성이 강점이다. DeepSeek 재현(96 GPU) 사례로 대규모 검증도 됐다. 성능은 vLLM과
경합하지만 커뮤니티·생태계는 아직 vLLM이 더 크다.

**llama.cpp** — "어디서나, 저비용" 철학의 경량 C/C++ 스택. GGUF 포맷 + 2~8비트
양자화 + CPU(SIMD)/Metal/CUDA/Vulkan 백엔드로, GPU 없이도 돈다. OpenAI 호환 서버
내장, Ollama가 이를 감싼 상위 래퍼다. 목표 자체가 다르다 — 플릿 처리량이 아니라
**로컬 지연시간·풋프린트·프라이버시**. 로컬 개발, 온프레미스 어시스턴트, 엣지가
주 용도다.

### 선택 기준 — 기능이 아니라 SLO에서 출발한다

교재의 평가 방법론 6단계: ① SLO부터 명문화(TTFT p95/p99, TPS, 토큰당 비용) →
② 실제 프롬프트 분석(prefill/decode 비중, 컨텍스트 길이, 도구 호출 여부) →
③ 동일 모델·양자화·부하로 "사과 대 사과" 비교 → ④ 운영성 측정(콜드 스타트,
관측성, 업그레이드 마찰) → ⑤ 하드웨어·벤더 종속 고려 → ⑥ **변화를 계획**.

| 상황 | 선택 |
|---|---|
| 빠른 프로덕션, 넓은 모델 커버리지, 파이썬 워크플로우 | **vLLM** |
| 에이전틱·다단계, 엄격한 JSON/문법 출력, 멀티벤더 | **SGLang** |
| NVIDIA 올인, 달러당 최대 토큰 | **TensorRT-LLM** |
| 로컬/온프레미스/엣지, 저비용, 프라이버시 | **llama.cpp** |

교재가 강조하는 것은 순위가 아니다. **서빙 생태계는 K8s보다 빠르게 변한다** —
vLLM은 1~2주마다 버전이 올라간다. 3~6개월 주기 재평가, 프레임워크 추상화 레이어,
탈출 계획(exit plan)을 갖춰서 **앱 코드를 다시 쓰지 않고 엔진을 갈아끼울 수 있는
유연함** 자체가 서빙 엔지니어의 목표다. vLLM이 내부를 계층화한 것과 같은 원칙을,
프레임워크 선택이라는 한 단계 위에서 반복한다.

### 직접 실측 — 동일 조건 비교 (RTX 4090, Qwen3-8B)

방법론 ③("사과 대 사과")대로 4종을 같은 모델·같은 부하로 쟀다
([실습 기록](labs/advanced-optimization-and-serving-frameworks.md)).

- **BF16 3종(vLLM·SGLang·TensorRT-LLM)의 단일 스트림 TPOT는 19.6~20.1ms로 동률.**
  decode가 memory-bound인 이상 토큰당 시간은 엔진이 아니라 대역폭이 정한다.
- 차이는 다른 축에서 났다 — SGLang은 random 동시성 8 처리량 **+29%**(스케줄링),
  TensorRT-LLM은 **첫 요청 워밍업 43초**(그래프 컴파일 — 재시작 잦은 환경의 실질 비용),
  llama.cpp는 동시 부하에서 **32요청 중 5개 연결 끊김**(단일 스트림 특화의 이면).
- llama.cpp의 TPOT 11.8ms는 Q8_0 가중치(~8.7GB vs BF16 16GB)의 효과다. dtype을
  통일하지 않으면 프레임워크 비교가 양자화 비교로 오염된다 — 방법론 ③의 반면교사를
  수치로 확인했다.

---

## 스스로 답해본 질문

### 고급 최적화

**Q1. "compute-bound vs memory-bound" 축이 이 범위 전체를 관통하는 단일 기준이라는
걸 어떻게 설명할 수 있는가? PD 분리된 decode 풀에서 추측 디코딩을 켜면 두 기법이
서로를 갉아먹지 않는가?**

각 기법을 이 축 위에 놓으면 전부 한 문장이 된다:

```
추측 디코딩   memory-bound인 decode의 노는 연산을 쓴다   → 저배치·연산 여유가 전제
PD 분리      compute-bound(prefill)와 memory-bound(decode)의 간섭 제거
MoE 희소성   배치가 작으면 전문가당 토큰이 부족           → 큰 배치가 경계를 옮겨줘야 산다
배칭         memory-bound를 compute-bound 쪽으로 민다
```

마지막 질문. PD 분리된 decode 풀은 순수 memory-bound 환경이라
얼핏 추측 디코딩의 최적 무대처럼 보인다. 그런데 decode 풀은 continuous batching으로
**큰 배치를 모아야** 존재 이유(전문가 희소성, 대역폭 상각)가 성립하고, 큰 배치는
병목을 compute 쪽으로 밀어 추측 디코딩의 전제를 잠식한다. 즉 두 기법은 같은 자원
(남는 연산)을 두고 경쟁한다. 단일 절만 읽어서는 답이 없고, decode 풀의 실제 배치
크기가 임계 배치에 얼마나 가까운지 — 즉 지금 어느 축 위에 있는지 — 를 측정해야
결론이 나온다. GPU 연산 여유가 남는 저부하 시간대에는 공존하고, 포화 시간대에는
추측을 끄는 동적 운용이 자연스러운 귀결이다.

**Q2. 네 기법(추측 디코딩·TP/PP·PD 분리·고급 KV 캐싱)이 공통적으로 "핸드오프
비용"을 치른다는 패턴은 무엇을 의미하는가?**

넷의 비용 목록 — 거부된 draft 토큰(추측), all-reduce 160회(TP), KV 캐시 전송
90Gb/s(PD 분리), 콜드스타트 오버헤드(LMCache) — 은 우연이 아니다. 이 기법들은
모두 **일을 나눠서 빨라지는** 구조이고, 나누면 반드시 이음새가 생기며, 이음새에는
반드시 교환 비용이 붙는다. 속도는 공짜로 얻어지는 게 아니라 병목을 다른 자리로
옮기는 것이다.

이게 결론부의 "계층을 함께 튜닝하라"와 논리적으로 이어진다 — 새로 생긴 병목은
원래 기법이 있던 계층이 아니라 **다른 계층**(네트워크, 스케줄러, 캐시 관리)에
나타나기 때문이다. 커널만, 엔진만, 라우팅만 최적화하면 한 계층의 이득을 다른
계층의 새 병목이 상쇄한다. 4계층 스택(커널 → 엔진 → 캐시 관리 → 라우팅/오케스트
레이션)을 통째로 보는 것이 선택이 아니라 필연인 이유다.

**Q3. 모든 최적화가 "특정 조건에서만" 이득인데, 그 조건을 실시간으로 감지해 자동
전환하는 메커니즘은 왜 이 범위에서 다뤄지지 않는가?**

정적 K의 한계, Dynamo/llm-d의 실시간 신호 라우팅이 언급은 되지만, 정작 모든 실습
예제는 사람이 값을 고정해 튜닝하는 방식이다. 이건 교재의 구조적 선택으로 읽힌다 —
**동적 자동화는 "어떤 신호를 보고 무엇을 바꿀지"를 아는 사람만 만들 수 있고**,
그 신호(수락률, 배치 크기, 캐시 적중률, 큐 깊이)와 손잡이(K, 배치 예산, 오프로딩
계층)를 몸으로 익히는 게 이 범위의 목적이기 때문이다. 실제 자동화는 그 신호들을
실시간으로 읽는 주체 — 스케줄러와 라우터 — 의 몫이고, 그래서 vLLM 내부(토큰 단위
스케줄링이 바로 조건 감지·적응의 실체다)와 오케스트레이션 계층으로 논의가 넘어
간다. 정적 튜닝은 동적 자동화의 학습 곡선이다.

### 서빙 프레임워크

**Q4. 왜 vLLM의 Scheduler는 "모델을 모른 채" 최적화를 결정하고, GPUWorker는
"스케줄링을 모른 채" 연산만 하는가? 이 분리가 없었다면 무엇이 문제였을까?**

답은 futureproof다. 모델 아키텍처와 하드웨어는 몇 달 단위로 바뀐다. 스케줄링
로직이 특정 모델 구조를 알고 있으면 새 모델마다 스케줄러를 고쳐야 하고, 연산
로직이 배칭 정책을 알고 있으면 정책 실험마다 커널을 다시 검증해야 한다. 분리돼
있으면 새 GPU는 CustomOp만, 새 아키텍처는 ModelExecutor만, 새 스케줄링 아이디어는
Scheduler만 바꾸면 된다. SchedulerOutput이라는 명시적 계약("이 요청들에 이 토큰
수만큼, 이 메모리 블록으로")이 두 세계의 유일한 접점이라, 양쪽이 독립적으로
진화한다. 관심사 분리는 코드 미학이 아니라 변화 속도에 대한 생존 전략이다.

**Q5. vLLM이 요청 단위가 아니라 토큰 단위로 스케줄링한다는 것은 구체적으로 무엇이
다르며, num_computed_tokens와 num_tokens_with_spec의 차이를 좁히는 것이 왜 모든
최적화의 공통 메커니즘이 되는가?**

요청 단위 스케줄러는 "요청 A를 실행한다/안 한다"의 이진 결정만 한다 — prefill이
긴 요청 하나가 배치를 통째로 점유하는 문제를 피할 수 없다. 토큰 단위는 "요청 A의
토큰 512개, 요청 B의 토큰 1개, 요청 C의 토큰 8개(draft 포함)"처럼 **하나의 forward
pass를 토큰 예산으로 합성**한다. prefill과 decode가 자연스럽게 섞이고(chunked
prefill), 캐시 히트만큼 건너뛰고(프리픽스 캐싱), 추측 토큰이 얹힌다(speculative).

이 모두가 "처리된 토큰(num_computed_tokens)과 처리해야 할 토큰(num_tokens_with_
spec)의 간극"이라는 하나의 산수로 표현되기 때문에, 기법들이 서로 모른 채 같은
프레임워크에 공존한다. 그리고 **우선순위(큐)와 토큰 산수(예산)가 분리**되어 있어
FCFS든 우선순위 선점이든 어떤 정책과도 조합된다.

**Q6. "어떤 프레임워크가 최고인가"가 아니라 "내 조건에 맞는가"를 물어야 한다면,
어떤 축이 넷 중 하나를 가리키는가? "3~6개월마다 재평가하라"는 정답이 없다는 뜻인가,
정답이 계속 바뀐다는 뜻인가?**

축은 네 개면 충분하다 — ① 하드웨어 벤더(NVIDIA 올인이면 TensorRT-LLM, 멀티벤더면
SGLang/vLLM), ② 워크로드 성격(에이전틱·구조화 출력이면 SGLang의 RadixAttention·
문법 제약이 직결), ③ 배포 위치(로컬·엣지·프라이버시면 llama.cpp), ④ 그 외 대부분
(넓은 모델 커버리지, 빠른 프로덕션)이면 vLLM.

재평가 주기의 의미는 후자다 — **정답이 계속 바뀐다.** 오늘의 선택이 틀려서가
아니라, 3개월 뒤의 생태계가 다른 답을 만들기 때문이다(vLLM만 해도 1~2주 단위
릴리스다). 그래서 처방이 "더 열심히 고르라"가 아니라 "**갈아끼울 수 있게 만들라**"
(추상화 레이어, 탈출 계획)로 나온다. 선택의 정확도보다 선택의 가역성이 더 중요한
환경이라는 진단이다.

---

## 정리

이번 범위는 **GPU 한 장을 넘어선 이후**를 다뤘고, 그 전체가 하나의 흐름으로
이어졌다.

```
decode가 느리다
  → 노는 연산으로 토큰을 추측·병렬 검증한다 (speculative decoding — 단, 연산 여유가 전제)
    → 모델이 한 장에 안 들어간다
      → 너비(TP)·깊이(PP)·전문가(EP)로 쪼개고 통째로 복제(DP)한다
        — all-reduce와 all-to-all은 NVLink 안에서만, 노드 간은 point-to-point만
          → prefill과 decode가 같은 GPU에서 간섭한다
            → 풀을 물리적으로 분리하고 KV 캐시를 넘긴다 (PD 분리 — 인터커넥트가 전제)
              → 긴 컨텍스트가 KV 캐시를 폭증시킨다
                → 캐시를 계층화·압축·블렌딩한다 (LMCache — 재사용이 전제)
                  → 이 전부를 실제로 돌리는 것이 서빙 프레임워크다
                    → vLLM: 토큰 단위 스케줄링 + 계층화된 최적화 = 변화에 견디는 구조
```

확인된 사실 위주로 추리면:

1. **추측 디코딩은 분포를 보존하는 거래다** — rejection sampling으로 target 단독
   출력과 수학적으로 동일하고, 대가는 메모리 이동 절감(140GB×3 → 147GB, 2.86배 —
   계산 검증)이다. 단 GPU 연산 여유가 전제라, 이미 포화된 환경(스터디 공유 실측:
   16GB GPU는 동시성 1에서도 98%)에서는 역효과가 난다
2. **통신 패턴이 배치를 결정한다** — TP의 all-reduce는 토큰당 160회(80층 기준),
   PP의 point-to-point는 3회(4스테이지). 그래서 TP·EP는 NVLink 안(노드 간 대비
   18~36배 빠름), PP만 노드를 넘는다. 파이프라인 버블은 (p−1)/(m+p−1)로 정량화되고
   마이크로배칭으로 75%→9%까지 줄어든다 (계산 검증)
3. **PD 분리는 전제조건이 까다롭다** — KV 전송 요구량(8B·16req/s면 ~25GB/s, 계산 검증)
   을 인터커넥트가 감당해야 성립하고, 워크로드에 P/D 비율이 안 맞으면 실패한다.
   맞으면 자원 예산 12.6배(DistServe) 급의 이득
4. **CAG는 빠르지만 항상 싸지 않다** — 같은 시나리오에서 TTFT 10배 개선에 비용
   1.9배 (계산 검증). 셀프호스팅 + 계층형 오프로딩(CPU 3배, SSD 50배 공간)이
   그 계산을 바꾼다
5. **vLLM의 본질은 두 가지 분리다** — 관심사 분리(Scheduler는 모델 무관, 4계층
   최적화)와 우선순위/토큰 산수 분리. 둘 다 "변화가 빠른 환경에서 살아남는 구조"
   라는 같은 목적을 향한다
6. **프레임워크 선택은 가역성이 정확도보다 중요하다** — 생태계가 주 단위로 바뀌므로,
   추상화 레이어와 탈출 계획을 갖추고 3~6개월마다 재평가한다
7. **같은 실험도 하드웨어가 결론을 바꾼다 (직접 실측)** — 교재(A100·32B)에서 2배
   우세였던 EAGLE-3가 4090·8B-FP8에선 최하위(고동시성 0.50x)였고 improved n-gram이
   전 구간 최선(1.79x/1.69x)이었다. 동일 BF16이면 프레임워크 3종의 단일 스트림
   TPOT는 동률(19.6~20.1ms) — 벤치 숫자는 조건 명세 없이 이식되지 않는다

---

## 이어서 볼 것

- **vLLM 성능 튜닝 실전** — 이번에 본 내부 구조(스케줄러 파라미터, 토큰 예산)를
  실제로 조정하는 다음 범위의 주제
- **llm-d와 KServe** — PD 분리·캐시 인지 라우팅의 K8s 구현. 스터디 후반 실습 예정
- **NVIDIA Dynamo의 KV Router** — 오케스트레이션 티어의 라우팅 신호 설계
- **DeepEP 내부** — all-to-all 통신을 알고리즘으로 줄이는 방식 (FlashAttention의
  통신판이라는 비유가 맞는지 코드로 확인해볼 것)
- **EAGLE-3 논문** — hidden state 예측과 다층 feature 융합의 구체적 구조
- **Spec-Bench** — 추측 디코딩 기법들을 동일 조건에서 비교하는 벤치마크 플랫폼

---

## 참고 자료

**교재**
- Hands-On LLM Serving and Optimization (2026)

**추측 디코딩**
- vLLM Speculative Decoding 문서: https://docs.vllm.ai/en/latest/features/spec_decode.html
- vllm-project/speculators (draft 모델 학습·통합): https://github.com/vllm-project/speculators
- Spec-Bench (기법 비교 벤치마크): https://github.com/hemingkx/Spec-Bench

**병렬화·분산**
- vLLM Parallelism and Scaling: https://docs.vllm.ai/en/latest/serving/parallelism_scaling.html
- vLLM Data Parallel Deployment: https://docs.vllm.ai/en/latest/serving/data_parallel_deployment.html
- vLLM Expert Parallel Deployment: https://docs.vllm.ai/en/latest/serving/expert_parallel_deployment.html
- Megatron-LM 논문 (TP의 노드 내 제한 근거): https://arxiv.org/abs/1909.08053
- DeepEP (MoE all-to-all 전용 라이브러리): https://github.com/deepseek-ai/DeepEP

**PD 분리·KV 전송**
- NIXL (NVIDIA Inference Xfer Library): https://github.com/ai-dynamo/nixl
- llm-d P/D Disaggregation: https://llm-d.ai/docs/well-lit-paths/foundations/pd-disaggregation
- NVIDIA Dynamo: https://github.com/ai-dynamo/dynamo
- Mooncake (Kimi KV 캐시 시스템): https://github.com/kvcache-ai/mooncake

**KV 캐싱**
- LMCache: https://github.com/lmcache/lmcache
- LMCache 블로그: https://blog.lmcache.ai/

**서빙 프레임워크**
- vLLM Architecture Overview: https://docs.vllm.ai/en/stable/design/arch_overview/
- Aleksa Gordić, "Inside vLLM: Anatomy of a High-Throughput LLM Inference System": https://www.aleksagordic.com/blog/vllm
- TensorRT-LLM: https://github.com/NVIDIA/TensorRT-LLM
- SGLang: https://github.com/sgl-project/sglang
- llama.cpp: https://github.com/ggml-org/llama.cpp

**영상**
- [Kakao] 빠르고 비용 효율적으로 LLM 서빙하기: Speculative Decoding
- [PY] LLM 추론의 공학적 원리 시리즈 — 추측적 디코딩 / 병렬 처리 / MoE / 프로덕션 서빙
- [Visual AI] Mixture of Experts(MoE) 아키텍처
- [CNCF] From Model Serving To Distributed Inference: How llm-d Evolves AI Platforms

**스터디 멤버 공개 자료**
- Runpod 멀티 GPU(TP/DP) 서빙 실습 가이드: https://ken-0913.github.io/myblog/posts/llm/llm-tp-dp-serving-lab/
