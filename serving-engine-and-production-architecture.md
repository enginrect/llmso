# 서빙 엔진 해부와 프로덕션 서빙 아키텍처

## Part 1. 서빙 엔진을 분해하다

### 왜 굳이 직접 만들어보는가

이 파트의 접근은 특이하다. vLLM이나 Triton을 바로 쓰는 대신 **from first principles로 서빙 엔진을
직접 조립**한다. 이유가 명확하다.

- 오픈소스 서빙 프레임워크가 너무 많아서 선택이 어렵다
- 기본 원리를 알면 합리적인 판단을 할 수 있다

즉 **"서빙 엔진이 구체적으로 무슨 일을 하는지 분해해서 보여주는"** 파트다.
새 엔진이 나올 때마다 "이게 좋대"에 휘둘리지 않으려면, 핵심 기능이 무엇인지 먼저 알아야 한다.

한 줄로 요약하면 이렇다.

> 모델 서빙은 단순히 모델의 `generate()` 함수를 호출하는 것이 아니라,
> **API 처리 · 요청 추적 · 배치 · 스트리밍 · 프로세스 격리 · 메모리 관리 · 라우팅 · 확장 · 장애 복구를
> 함께 설계하는 시스템 엔지니어링**이다.

### 단일 모델 서빙: 6개 구성 요소

| 구성 요소 | 역할 |
|---|---|
| **API Server** | HTTP 요청 수신·파싱 (FastAPI 등) |
| **LLM Engine** | 전체 흐름을 조율하는 **오케스트레이터**. 기동 시 모델 로딩 포함 전 컴포넌트 초기화 |
| **Workload Manager** | 요청을 큐에 넣고 대기 상태를 추적, **다음 배치에 무엇을 묶을지 결정**(= 스케줄러) |
| **Model Executor** | 워커 프로세스를 초기화·관리하고 배치 실행을 트리거 |
| **Model Worker** | **GPU에서 실제 추론 수행** (별도 프로세스) |
| **Queue** | Executor ↔ Worker 사이의 작업/결과 전달 통로 |

요청 하나가 흐르는 경로는 이렇다.

```
1. Request intake     API 서버가 요청을 받아 페이로드에서 개별 프롬프트 추출
2. Prompt queuing     API 서버 → LLM 엔진 → 워크로드 매니저로 전달
3. Prompt tracking    워크로드 매니저가 각 프롬프트에 고유 ID를 부여해 Sequence 객체로 감싸고,
   and batching       모든 활성 시퀀스를 추적하며 다음 배치 구성을 결정
4. Batch execution    LLM 엔진이 배치를 가져와 Model Executor → Worker로 전달
5. Model inference    워커가 한 번의 추론 호출로 전체 배치를 처리, (텍스트, 프롬프트 ID) 쌍 반환
6. Response mapping   LLM 엔진이 프롬프트 ID로 각 결과를 원래 웹 요청과 다시 연결
```

### 왜 프로세스를 분리하는가 — 이 장의 첫 번째 핵심

Model Worker를 **별도 프로세스**로 띄우는 게 처음엔 과한 설계처럼 보인다. 이유는 GPU 경제학이다.

```
GPU는 비싸고, 놀리면 손해다.

토크나이징 · 전처리 · 후처리 같은 CPU 작업이 GPU와 같은 프로세스/스레드에서 돌면
→ GPU가 그 CPU 작업이 끝날 때까지 기다린다
→ 비싼 자원이 논다
```

그래서 역할을 물리적으로 가른다.

| 프로세스 | 담당 | 배치 위치 |
|---|---|---|
| **CPU 중심** — API Server, LLM Engine, Workload Manager | 요청 관리·오케스트레이션 | **GPU 없는 노드에 올려도 된다** |
| **GPU 전용** — Model Worker | 모델 forward 연산 | GPU 노드 |

GPU는 계산에만, CPU는 요청 관리에만 집중시켜 **GPU 활용률(utilization)을 극대화**하는 것.
작은 예제에서는 과잉으로 보이지만 **실제 프로덕션 GPU 서빙 시스템의 표준 패턴**을 그대로 반영한 구조다.

### 배칭 — 사용자 경계를 지우면 생기는 문제

배칭은 여러 프롬프트를 묶어 한 번에 Model Worker로 보내 GPU 활용률을 올리는 기법이다.
여기서 중요한 건 **묶는 방식**이다.

```
요청 A: [프롬프트 a1, a2]
요청 B: [프롬프트 b1, b2, b3]

배치(batch_size=4): [a1, a2, b1, b2]  ← 사용자 경계와 무관하게 묶인다
남은 것:            [b3]              ← 다음 배치로 대기
```

**사용자 경계를 무시하고 배치 크기만큼 채우는 게 핵심**이다. GPU는 어차피 병렬 연산을 하니
누구의 요청인지는 상관없다. 대신 문제가 하나 생긴다.

> 결과를 돌려줄 때는 **누구의, 몇 번째 프롬프트였는지 정확히 복원**해야 한다.

그래서 **Sequence 객체**가 등장한다. 고유 ID, 완료 여부, 토큰 수, 스트리밍 큐 참조 등을 들고 있고,
추론 결과가 나올 때 **시퀀스 ID를 함께 반환**해서 원래 요청과 순서를 다시 조립한다.

Workload Manager가 관리하는 자료구조도 이때 늘어난다.

```
incoming_queue    새로 들어온 Sequence 대기열
active_sequences  현재 GPU 배치에 포함된 Sequence
sequence_map      ID로 Sequence를 조회
```

**배치 크기의 트레이드오프**도 분명하다. 크게 잡으면 GPU 효율은 오르지만,
배치가 찰 때까지 먼저 온 요청이 기다려야 한다. batch_size를 2,000~3,000으로 잡으면
그만큼의 대기가 사용자 지연으로 돌아온다. 처리량과 지연시간 사이에서 균형점을 찾아야 한다.

### 스트리밍 — 사용자가 떠나지 않게

여기까지의 구조는 **EOS 토큰이 나올 때까지 전부 기다렸다가 한 번에 반환**한다.
챗봇에서는 사용자가 그 사이에 떠난다.

해결책은 디코드 스텝마다 결과를 즉시 흘려보내는 것이다.

- **SSE(Server-Sent Events)** 로 전달
- 토큰이 생성될 때마다 **이벤트 큐**에 넣고
- **비동기(async)** 인터페이스로 클라이언트에 즉시 전송

```
prefill → decode(A) → 즉시 전송
        → decode(B) → 즉시 전송
        → decode(C) → 즉시 전송 ... → EOS
```

앞 노트에서 정리한 대로, 스트리밍은 **체감 TTFT를 낮출 뿐 백엔드의 총 decode 비용은 그대로**다.

### 그리고 vLLM이 이걸 전부 대체한다

이 파트의 반전은 마지막에 온다. 앞에서 손으로 만든 배칭·스케줄링·스트리밍·큐 관리를
**vLLM은 기본으로 제공한다.**

| 항목 | 직접 구현 | vLLM |
|---|---|---|
| 배치 구성 | Workload Manager: FIFO + 고정 batch_size | 내부 스케줄러 (**continuous batching**) |
| 토큰 생성 | 워커가 별도 프로세스에서 스텝마다 forward | **PagedAttention + KV 캐시** |
| 결과 매핑 | `sequence_map`, request_id 수동 추적 | 입력 순서 그대로 outputs 반환 |
| 스트리밍 | asyncio.Queue + 백그라운드 스레드 직접 구현 | 내부 처리 (별도 API) |
| 코드량 | 워크로드 매니저 + 익스큐터 + 워커 (**수백 줄**) | **20줄 내외** |

여기서 이 파트가 하려는 말이 완성된다.

> 프레임워크를 쓰라는 게 아니라, **프레임워크가 무엇을 대신해주는지 알고 쓰라**는 것.
> 그래야 새 엔진이 나왔을 때 "이건 좋고 저건 부족하다"를 스스로 판단할 수 있다.

이게 추상적인 조언이 아닌 이유는, **기본값이 내 워크로드에 안 맞을 때 직접 만져야 하는 노브들**이
정확히 우리가 손으로 구현해본 것들이기 때문이다.

| 튜닝 파라미터 | 우리가 직접 만든 대응물 |
|---|---|
| 최대 동시 시퀀스 수 (`max_num_seqs`) | `WorkloadManager.batch_size` |
| 배치 토큰 수 (`max_num_batched_tokens`) | 배치에 몇 토큰까지 넣을지 |
| `gpu_memory_utilization` | 어디에도 없었음 — vLLM이 KV 캐시용으로 선점하는 비율 |
| KV 캐시 크기 | 우리 스트리밍 경로엔 캐시 자체가 없었음 |
| Chunked prefill | 긴 프롬프트가 다른 요청을 막지 않게 쪼개기 |
| 스케줄링 정책 | `get_next_batch()`의 FIFO |

배칭이 지연시간과 처리량에 어떻게 작용하는지 몸으로 알면 `max_num_seqs`를 어느 쪽으로
움직여야 하는지 감이 온다. 반대로 모르면 기본값을 그대로 쓰거나, 벤치마크 글에서 본 숫자를
맥락 없이 복사하게 된다.

#### 배포 형태는 두 가지다

vLLM이나 SGLang 같은 프레임워크는 용도에 따라 두 가지로 배포된다.

| 방식 | 특징 | 적합 |
|---|---|---|
| **라이브러리로 임베드** | 커스텀 서빙 앱 안에 넣어 실행 흐름을 세밀하게 제어 | 긴밀한 통합이 필요할 때 (교재 예제가 이 방식) |
| **독립 웹 서버로 실행** | REST/스트리밍 API를 노출해 외부 클라이언트·다른 서비스가 호출 | **실무에서 더 흔한 형태** |

Part 2에서 Ray Serve나 SageMaker 엔드포인트를 볼 때 등장하는 게 후자다.

#### Static vs Continuous Batching

```
[Static]     배치가 시작되면 전원이 끝날 때까지 새 요청이 못 들어온다
  요청1 ████████
  요청2 ███              ← 먼저 끝나도 슬롯이 놀고 있음
  요청3 ██████
       └─ 여기까지 대기 ─┘ → 그제야 다음 배치

[Continuous] 슬롯이 비는 즉시 새 요청을 투입
  요청1 ████████
  요청2 ███ → 요청4 █████   ← 빈 자리를 바로 채움
  요청3 ██████ → 요청5 ███
```

vLLM, SGLang, TensorRT-LLM(여기선 "in-flight batching")이 모두 이를 구현한다.

### 멀티 모델 서빙

#### 왜 필요한가

모델마다 전용 서버를 두면 이런 일이 생긴다.

```
모델 A 전용 GPU 서버 → 사용률 10%   (영상 처리, 요청이 드묾)
모델 B 전용 GPU 서버 → 사용률 90%   (텍스트, 요청 폭주)
```

**비싼 GPU가 한쪽에서 놀고 한쪽에서 터진다.** 한 서버가 여러 모델을 동적으로 로드/언로드할 수
있으면 GPU 사용률을 고르게 끌어올릴 수 있다.

#### 구성 요소가 5개로 재편된다

| 구성 요소 | 역할 |
|---|---|
| **API Server** | HTTP 엔드포인트 노출, 요청/응답 처리 |
| **Model Manager** | **모델 캐시를 관리하고 워커의 라이프사이클을 조정** (중심 역할) |
| **Model Store** | 모델 **메타데이터**를 저장·조회 |
| **Model Engine** | 메타데이터를 보고 적절한 **워커 인스턴스를 생성**(팩토리) |
| **Model Worker** | 모델을 메모리에 로드하고 추론 실행 |

여기서 **Model Manager와 Model Engine의 역할 분담**이 포인트다. Manager는 "무엇을 메모리에
올려둘지"(캐시 정책)만 결정하고, 실제 인스턴스 생성은 Engine에 위임한다.

요청이 흐르는 경로는 7단계다.

```
1. Client request     model_id + input_data로 예측 요청
2. Model lookup       Model Manager가 캐시 확인
3. Metadata fetch     캐시에 없으면 Model Store에 메타데이터 질의
                      { id, name, type, framework, version, description }
4. Worker creation    메타데이터를 Model Engine에 넘겨 워커 생성 → 모델을 메모리에 로드
5. Worker registration 캐시에 등록. 캐시가 가득 차 있으면 LRU로 하나 축출
6. Inference          워커에 입력 데이터를 넘겨 추론 실행
7. Response           결과 반환
```

같은 모델을 두 번째로 요청했을 때 또 로드하면 순수한 낭비다. 그래서 **모델 캐시**가 핵심이고,
메모리가 부족해지면 **LRU로 가장 오래 안 쓴 모델을 언로드**해 VRAM/HBM을 회수한다.

> Model Manager는 **어떤 모델을 RAM/VRAM/HBM에 올려둘지 결정하는 컴포넌트**다.
> 멀티 모델 서빙에서 비용과 성능을 가르는 자리에 있다.

메타데이터를 별도 컴포넌트로 분리한 것도 의미가 있다. 예제에서는 로컬 JSON이지만,
실무에서는 이 자리가 **모델 레지스트리나 메타데이터 서비스**로 대체된다.
`framework` 필드 하나로 어떤 워커를 띄울지가 결정되므로, 새 프레임워크 지원을 추가하는 일이
**메타데이터 + 워커 클래스 추가**로 국소화된다.

#### 설계 목표 2가지

**1. Cross-framework support** — PyTorch, ONNX, TorchVision, Transformers 등 서로 다른
프레임워크의 모델을 하나의 시스템에서 호스팅한다. 구현은 **프레임워크별 Worker를 두고
메타데이터로 분기**하는 팩토리 패턴이다.

```python
if framework == "transformers":  worker = TransformerWorker(meta)
elif framework == "torchvision": worker = TorchVisionWorker(meta)
elif framework == "triton":      worker = TritonWorker(meta)
else: raise ValueError(f"Unsupported framework: {framework}")
```

**2. Unified API interface** — 텍스트 생성이든 이미지 분류든 하나의 엔드포인트로 받는다.

```
POST /predict
{ "model_id": "model-A", "input_data": "This movie was great!" }
```

요청에 **`model_id`를 실어 보내는 것**이 단일 모델 서빙과의 결정적 차이다.

#### NVIDIA Triton

직접 만든 멀티 모델 서버를 프로덕션 등급으로 바꾸면 Triton이 된다.

- 단일 HTTP/gRPC 엔드포인트
- 여러 프레임워크 백엔드 동시 지원
- 모델별 스케줄러/큐
- 모델 관리 API (로드/언로드)

Triton은 두 종류의 API를 제공한다.

```
모델 관리 API   POST /v2/repository/models/{name}/load   (그리고 /unload)
모델 추론 API   POST /v2/models/{name}/infer
```

우리 구조에 얹을 때는 **Triton Server(웹 서비스) + Triton Worker(래퍼)** 두 조각만 있으면 된다.
기존 Model Engine의 워커 자리에 Triton Worker를 끼워 넣으면, 워커는 직접 추론하는 대신
Triton의 관리 API로 모델을 로드하고 추론 API로 요청을 위임한다.
워커가 소멸할 때는 unload를 호출해 Triton 쪽 메모리까지 회수한다.

여기서 드러나는 게 실무에서 반복되는 패턴이다.

> **클라이언트 요청 처리·의존 시스템 통합·리소스와 모델 파일의 생명주기 관리는 래퍼 서비스가 맡고,
> 무거운 추론 워크로드는 전문 엔진에 위임한다.**

#### 멀티 모델 서빙이 새로 만드는 두 가지 과제

비용 절감 효과는 분명하지만, 현장에서 부딪히는 문제는 둘 다 **사용자 경험** 쪽이다.

**1. Cold start latency** — 로드되어 있지 않은 모델에 요청이 오면 모델 파일 다운로드 + 메모리 로드,
경우에 따라 다른 모델 축출까지 필요하다. 수 초에서 수십 초의 지연이 생긴다.
콜드 상태 모델이 많은데 트래픽이 몰리면 요청 타임아웃과 하위 애플리케이션의 연쇄 장애로 번진다.

**2. Hot model scaling** — 특정 모델에 트래픽이 몰리면 지연이 올라간다. 그런데 확장이 단순하지 않다.
**인스턴스마다 독립적인 모델 캐시를 갖기 때문**에, 그 모델을 여러 인스턴스에 복제하고 라우팅 계층을
갱신하는 일이 복잡해진다. 결과적으로 성능이 비결정적으로 움직인다.

#### 두 가지 설계 방향

만능 해법은 없고, 무엇을 우선하느냐로 갈린다.

**비용 최적화 설계** — 지금까지 만든 멀티 모델 인스턴스(프론트엔드 + 백엔드) 여러 개를 두고,
그 위에 **모델 서비스 API와 라우팅 계층**을 얹는다. 이 계층이 모델↔인스턴스 매핑을 유지하면서
세 가지를 한다.

```
1. 이미 그 모델이 로드된 인스턴스로 라우팅   → 콜드 스타트 최소화
2. 모델별 replica 수를 추적해 분산 배치       → hot model 수평 확장
3. bin-packing으로 최소 서버 수에 몰아 배치   → 자원 사용 최적화 = 비용 절감
```

한계도 명확하다. **반응형(reactive) 시스템**이라 트래픽 패턴이 나타난 뒤에 따라간다.
항상 수요를 뒤쫓는 구조라 급증 시 지연은 구조적으로 피할 수 없다. 그리고 라우팅 로직·스케일링 결정·
인스턴스 간 캐시 상태 일관성까지 관리해야 해서 운영과 디버깅 난이도가 올라간다.

**지연시간 최적화 설계** — 분산 시스템의 고전적 트레이드오프인 **"용량을 희생해 성능을 얻는다"** 를
적용한다. 공유 인스턴스 대신 **모델마다 전용 인스턴스 그룹**을 두고, 요청 시점 로딩 대신
**사전 프로비저닝**한다.

```
클라이언트가 예측 요청 전에 먼저 프로비저닝 서비스를 호출
  → 해당 모델의 전용 인스턴스 그룹 생성
    → 라우팅 맵 갱신
      → 이후 요청은 맵을 참조해 전용 그룹으로 바로 전달
```

| | 비용 최적화 | 지연시간 최적화 |
|---|---|---|
| 자원 배치 | 여러 모델이 인스턴스를 **공유** | 모델마다 **전용 그룹** |
| 로딩 시점 | 요청 시 온디맨드 (LRU 캐시) | **사전 프로비저닝** (항상 가동) |
| 라우팅 | 로드된 인스턴스를 찾아 동적 라우팅 | 라우팅 맵 기반 정적 전달 |
| 콜드 스타트 | 발생 | **없음** |
| 확장 | 캐시 상태 때문에 복잡 | 모델별 독립 확장 |
| 구조 복잡도 | 높음 (동적 캐시 관리) | 낮음 (유지보수·트러블슈팅 쉬움) |
| **대가** | 반응형이라 급증에 취약 | **과다 프로비저닝으로 컴퓨트 낭비** |

지연시간 최적화 쪽의 약점은 **어떤 모델이 뜨거워질지 미리 알기 어렵다**는 데서 온다.
반대로 **꾸준하고 예측 가능한 수요**를 가진 모델이 다수라면 매우 효과적이다.

#### LLM에도 멀티 모델 서빙이 적용된다

LLM은 연산·메모리 요구량이 커서 보통 단일 모델 서빙으로 다루는데, 그럼에도 이 장의 개념이
그대로 재사용되는 두 지점이 있다.

| 케이스 | 내용 |
|---|---|
| **Prefix caching + 라우팅** | 프롬프트 앞부분이 같은 요청들을 **이미 그 KV 캐시가 채워진 레플리카로 라우팅**해 중복 연산을 줄인다 (교재 후반부) |
| **다중 LoRA 어댑터 서빙** | 하나의 공유 베이스 모델 위에 **여러 LoRA 어댑터를 동적으로 로드/축출**한다. 테넌트별·유스케이스별 개인화를 메모리 효율적으로 확장 (교재 후반부) |

두 경우 모두 구조가 같다.

> **무거운 리소스(베이스 모델, KV 캐시)는 공유하고, 그 위에 얹는 가벼운 변형(어댑터, 캐시 상태)을
> 어느 레플리카가 갖고 있느냐로 라우팅한다.**

즉 "로드 / 캐시 / 라우팅"이라는 멀티 모델 서빙의 세 축이 LLM 서빙에서도 그대로 살아난다.
앞 노트에서 본 Cache-Aware Routing이 왜 필요한지가 여기서 다시 설명된다.

### 단일 모델 서빙의 6가지 요구사항

교재가 정리하는 체크리스트. 단일 모델이라도 챙길 게 많다.

| 요구사항 | 내용 |
|---|---|
| **Low latency** | 추론을 신속히 완료하고 지연 없이 반환 (전체 완료 반환 vs 토큰별 스트리밍) |
| **High throughput** | 동시에 많은 요청 처리. QPS/TPS로 측정. 배칭과 PagedAttention이 이를 위한 기능 |
| **Scalability** | 트래픽 변동에 맞춰 수평 확장·축소. **단일 프로세스·단일 이벤트 루프로는 한계가 명확하다** |
| **Reliability & availability** | 일관되고 중단 없는 서비스, 장애 허용성 |
| **Resource efficiency** | 쿼리당 비용을 관리 가능한 수준으로. **모델 크기보다 설정값 튜닝이 좌우한다** |
| **Observability** | 지연시간·처리량·오류율 등 KPI와 모델별 지표 추적 |

Resource efficiency 항목이 특히 실무적이다. vLLM의 `gpu_memory_utilization` 기본값은 **0.9**라,
작은 모델을 올려도 **VRAM의 90%를 선점**한다. 모델이 작다고 메모리를 적게 쓰는 게 아니다.

### LLM이라서 추가로 붙는 요구사항 4가지

위 6가지는 일반 ML 모델 서빙에도 해당한다. LLM에는 여기에 네 가지가 더해진다.

| 요구사항 | 내용 |
|---|---|
| **큰 모델 크기와 메모리 사용량** | 수십~수백 GB를 요구하므로 GPU 자원과 메모리 할당을 신중히 관리해야 한다 |
| **KV 캐시 관리** | 긴 컨텍스트와 상태 기반 디코딩을 지원하려면 여러 요청·세션에 걸쳐 KV 캐시를 효과적으로 관리해야 한다 |
| **스트리밍 응답** | 인터랙티브 애플리케이션에서는 토큰 생성 즉시 클라이언트로 흘려보내야 한다 |
| **가변 길이 워크로드의 동시성·배칭** | LLM 요청은 입출력 길이가 제각각이다. 이 이기종 워크로드를 지능적으로 배치·스케줄링해야 처리량과 GPU 활용도가 유지된다 |

여기서 교재가 짚는 중요한 구분이 하나 있다.

> 전통적인 딥러닝 모델 서빙은 비교적 안정적이라 **모델을 블랙박스처럼 다룰 수 있었다.**
> 반면 LLM 서빙은 더 동적이고 **모델을 인지하는(model-aware) 처리**가 필요하다.

KV 캐시의 동작만 해도 사용자 입력 크기와 어텐션 메커니즘(MHA인지 GQA인지)에 따라 달라진다.
요구사항이 모델 아키텍처가 발전할 때마다 바뀌고, 대개 모델별로 다르다.

그래서 설계 원칙이 하나 도출된다.

```
변화하는 구성 요소  ←→  안정적인 인프라
       (모델별 요구)        (핵심 서빙 로직)
              이 둘을 분리하라
```

### 일반화된 설계 — 관심사를 세 영역으로 나눈다

앞의 요구사항들을 어떻게 담아낼 것인가. 교재의 답은 **관심사를 세 영역으로 쪼개서
각각 따로 다루라**는 것이다. 특정 도구나 플랫폼에 의존하지 않는 개념적 설계라,
온프레미스든 퍼블릭 클라우드든 그대로 적용된다.

| 영역 | 무엇을 다루나 |
|---|---|
| **Service infrastructure management** | 확장성, 가용성, 모니터링, 효율적 자원 할당 |
| **Business logic handling** | 고객 유스케이스 통합, 요청 배칭, 스트리밍 응답 |
| **Model serving performance** | LLM 특화 성능 — 지연시간, 처리량, 최적화 |

구조로 옮기면 3층이 된다.

**A. 인프라 관리** — 서빙 로직을 컨테이너나 Pod 같은 **재현 가능한 단위로 캡슐화**하고,
인프라 책임은 분산 컴퓨팅 플랫폼에 위임한다. 트래픽에 따른 복제본 수평 확장·축소,
비정상 인스턴스 재시작, 동적 자원 할당, 로깅·모니터링 인터페이스 노출이 여기 속한다.
Kubernetes나 클라우드 프로바이더가 기본 제공하는 것들이다.

최종 사용자는 개별 인스턴스와 직접 통신하지 않고 **로드 밸런서를 거친다.**
내부 확장의 복잡성을 감추면서 탄력성을 제공하기 위해서다.

**B. 서빙 프론트엔드 (비즈니스 로직)** — 각 인스턴스 안에서 웹 인터페이스와 비즈니스 로직을 담당한다.

- 고객 애플리케이션 요청의 인증·인가
- 사용자/고객 데이터, 모델 메타데이터, 감사 로그, 결제 시스템 등 외부 시스템 통합
- 모델 다운로드·설정, 런타임 컨텍스트 관리
- 요청 검증·정규화·배칭
- rate limiting, 로깅 같은 트래픽 제어

**C. 서빙 백엔드 (모델 추론)** — 실제 추론이 일어나는 곳. **별도 프로세스로 실행되고
서빙 프론트엔드만 접근할 수 있다.** 오로지 고성능 모델 실행에만 집중한다.
실무에서는 이 자리를 vLLM이나 Triton 같은 성숙한 프레임워크가 채운다.

```
A 인프라 관리        로드밸런서 · 복제본 스케일링 · 헬스체크 · 자원 할당 · 모니터링
     ↓
B 서빙 프론트엔드    인증/인가 · 외부 연동 · 요청 검증/배칭 · rate limiting   ← 우리가 직접 짜는 영역
     ↓  (프로세스 경계)
C 서빙 백엔드        고성능 추론 실행 (vLLM / Triton)                        ← 프레임워크에 위임
```

**이 3층 분리가 Part 3의 7계층 아키텍처로 그대로 확장된다.** 비즈니스 로직과 추론 성능을
프로세스 경계로 갈라두면, 모델별 요구가 바뀌어도 인프라와 비즈니스 로직은 그대로 둘 수 있다.

---

## Part 2. 에이전트 시대의 서빙

### 전제가 깨진다

여기까지 모델은 **독립적인 예측 엔진**이었다. 요청 하나 → 추론 한 번 → 응답 반환.
에이전트가 등장하면 이 전제가 무너진다.

> 모델이 요청당 한 번이 아니라, **제어 루프(control loop) 안에서 반복 호출**된다.

한 번의 사용자 상호작용이 촉발하는 것들:

- **다중 LLM 호출** (계획 수립 → 검색 → 분석 → 요약)
- **더 긴 컨텍스트 윈도우** (턴이 쌓일수록 누적)
- **검색 연산** (RAG)
- **메모리 재사용** (CAG)

서빙 시스템 입장에서의 결과는 셋이다.

```
토큰 사용량 증가
  → 체인으로 연결된 호출마다 지연시간 증폭 (tail latency)
    → 트래픽 패턴이 더 동적으로 변함
```

특히 **tail latency 증폭**이 무섭다. 단계가 4개면 각 단계의 P99가 곱해지듯 누적된다.
그래서 서빙은 "모델을 효율적으로 실행하는 일"을 넘어 **오케스트레이션·메모리 관리·시스템 레벨
조정**까지 떠안게 된다.

### 에이전트의 정의

다음이 가능한 자율적 LLM 기반 시스템:

1. 상위 수준 목표를 이해
2. 달성 방법을 추론
3. 외부 도구·데이터 소스를 선택·호출
4. 중간 결과를 바탕으로 적응·반복
5. 최소한의 사람 개입으로 최종 결과물 산출

규칙 기반 챗봇과의 본질적 차이는 **자율성**이다. 전통적 시스템은 개발자가 짜둔 워크플로우를
절차적으로 실행하지만, 에이전트는 **"무엇을 어떻게 할지"까지 스스로 판단**한다.

### 에이전트의 내부 구조

교재의 Knowledge Agent 예제는 4개 컴포넌트로 구성된다.

| 컴포넌트 | 역할 |
|---|---|
| **Agent (Orchestrator)** | 전체를 조율하는 중앙 컨트롤러 |
| **Planner** | LLM을 써서 **실행 계획을 수립** |
| **RAG System** | 문서 처리·임베딩 생성·벡터 검색 |
| **Actions (Executor)** | 질의/요약/분석 등 개별 작업 실행 |

여기서 짚고 갈 점.

> **LLM이 혼자 다 하는 게 아니다.**
> LLM = 생각하고 답을 생성 / RAG = 필요한 자료 찾기 / Planner = 작업 순서 결정 / Agent = 전체 조정

#### 워크플로우 — 요청 하나가 호출 여러 번이 되는 지점

```
1. 사용자 질문
2. Agent → Planner 호출
3. Planner → LLM 호출          ← LLM 호출 ①  (실행 계획 생성)
4. 계획 수립: ["query_rag_with_context", "generate_analysis", "generate_summary"]
5. Action 1: RAG 검색           ← 임베딩 호출  (쿼리 벡터화 + 유사도 검색)
              + LLM으로 답변 생성 ← LLM 호출 ②
6. Action 2: 앞 결과를 컨텍스트로 심층 분석 ← LLM 호출 ③
7. Action 3: 분석 결과를 요약            ← LLM 호출 ④
8. Agent가 최종 결과를 사용자에게 반환
```

**사용자는 질문 하나를 던졌는데, 백엔드는 여러 번의 왕복을 감당했다.**
이게 이 파트 도입부에서 말한 "단일 상호작용이 다중 LLM 호출을 촉발한다"의 실체이고,
tail latency가 체인마다 누적되는 이유다.

또 하나 눈여겨볼 것은 **컨텍스트 체이닝**이다. 2단계는 원본 문서를 다시 검색하지 않고
**1단계가 만든 출력 자체를 입력으로** 받는다. 즉 단계가 늘수록 검색 비용은 줄지만
**앞 단계의 오류가 뒤로 전파**된다.

### RAG

#### 왜 필요한가

LLM 단독의 세 가지 한계를 완화한다.

1. **지식이 고정적** — 학습 데이터 컷오프 이후 정보가 없다
2. **환각(hallucination)** — 그럴듯하지만 틀린 내용을 만든다
3. **도메인 지식 부족** — 사내 문서 같은 건 애초에 모른다

파인튜닝으로도 해결할 수 있지만 비용과 시간이 든다. RAG는 **벡터 DB만 갱신하면 모델 재학습 없이
지식을 바꿀 수 있다.**

#### 두 개의 워크플로우

```
[A] Index building (오프라인, 주기적)
  원시 문서(HTML/PDF) → 정제·파싱 → 청킹(보통 ~1,000 토큰)
    → 청크별 임베딩 대량 사전 계산  ← 오프라인 배치 추론이 일어나는 지점
      → 벡터 DB에 인덱싱

[B] Query / Retrieval (온라인, 실시간)
  사용자 쿼리 → 쿼리 임베딩 → 코사인 유사도로 최근접 벡터 검색
    → 관련 청크 반환 → 쿼리 + 청크를 함께 LLM에 전달 → 답변 생성
```

**청킹은 단순한 전처리가 아니다.** 검색의 세분화 정도(한 번에 고려할 텍스트 양)를 정의하고,
동시에 오프라인 배치 추론이 일어나는 단계다.

| | 작은 청크 | 큰 청크 |
|---|---|---|
| 검색 정밀도 | 높음 | 낮음 (관련 없는 내용이 섞여 희석) |
| 문맥 | 손실 위험 | 풍부 |

최적값은 도메인과 LLM 컨텍스트 윈도우에 따라 달라진다.

#### 흔한 오해 하나 — RAG는 모델 내부를 건드리지 않는다

"검색된 문서가 LLM 내부의 Q 행렬에 주입된다"거나 "RAG의 query와 attention의 Q가 같다"는
직관을 갖기 쉬운데, **틀렸다.**

```
사용자 query
  → [RAG 전용 임베딩 모델] → query 벡터
    → [Vector DB 코사인 유사도] → top-k 문서
      ─────────────────────────────────────
      여기부터는 그냥 "텍스트 이어붙이기"
      prompt = 검색된_문서들 + "\n\nQuestion: " + query
      ─────────────────────────────────────
        → [LLM] → 토큰화 → 평소와 똑같은 self-attention → 응답
```

**LLM 입장에서 RAG는 프롬프트가 길어진 것일 뿐**이다. 외부에서 검색했든 사람이 직접 붙여넣었든
모델은 구분하지 못한다. 그리고 RAG에 쓰는 임베딩 모델은 LLM과 **별개의 모델**이다.

비유하자면 RAG는 **오픈북 시험**(매번 책에서 찾아 답안에 첨부)이고,
파인튜닝은 **암기**(가중치에 지식을 내재화)다.

### CAG (Cache-Augmented Generation)

#### RAG의 대가

- **추가 지연시간** — LLM 호출 전에 임베딩 + 검색 단계가 끼어든다
- **선택 오류 위험** — 제한된 토큰 안에서 "가장 관련 있는" 것을 잘못 고를 수 있다
- **시스템 복잡도** — 임베딩·인덱스·벡터 DB를 구축하고 유지해야 한다

#### 컨텍스트 윈도우가 커지면서 생긴 대안

100만 토큰 컨텍스트를 지원하는 모델이 나오면서 "몇 개만 골라 넣는다"는 RAG의 전제 자체가 흔들렸다.
지식 베이스 상당 부분을 통째로 넣을 수 있다면?

**CAG는 쿼리 시점에 검색하는 대신, 지식을 미리 LLM의 KV 캐시에 프리로드**해두고
추론 시 그 캐시된 컨텍스트로 바로 답한다.

| | RAG | CAG |
|---|---|---|
| 동작 | 쿼리마다 동적 검색 | 지식을 한 번 캐시에 로드, 이후 바로 응답 |
| 강점 | 최신성, 대규모 지식베이스 | 지연시간, 시스템 단순화 |
| 대가 | 검색 지연, 복잡도 | 큰 컨텍스트 윈도우 = 메모리/연산 요구량 증가 |
| 적합 | 자주 바뀌는 지식 | 고정된 지식 세트, 반복 쿼리 |

**양자택일이 아니다.**

```
RAG로 입력을 보강하고 (답변 품질: 지식 접지·최신성)
CAG로 실행을 최적화한다 (서빙 효율: 지연시간·처리량·비용)
```

앞 노트에서 본 prefix caching이 CAG와 같은 뿌리라는 점도 이어진다 — 반복되는 컨텍스트의
KV 계산을 재사용한다는 발상이 동일하다.

### 도구 호출 (Tool Calling)

에이전트의 액션은 LLM 프롬프트에만 국한되지 않는다.

- **도구 호출**: 웹 검색, API 질의, DB 명령
- **시스템 연산**: 파일 관리, 워크플로우 트리거
- **추론 단계**: 사용자에게 노출되지 않는 중간 계산

동작은 4단계 루프다.

```
1. LLM이 사용자 요청 + 사용 가능한 도구 목록을 놓고 추론
2. 선택한 도구와 인자를 구조화된 출력(보통 JSON)으로 생성
3. Agent가 실제로 도구를 실행
4. 결과를 다시 LLM에 넣고, 작업이 끝날 때까지 반복
```

**MCP(Model Context Protocol)** 는 이 도구 사용을 표준화하려는 대표적 접근이다.
도구 발견 → 구조화된 호출 → 결과 반영을 일관된 인터페이스로 제공하고,
도구 정의를 에이전트 핵심 로직과 분리해 ad-hoc 프롬프트 엔지니어링의 취약성을 줄인다.

---

## Part 3. 엔터프라이즈 서빙 아키텍처

### "모델 실행"과 "엔터프라이즈 서빙"은 다른 문제다

지금까지 만든 것은 전부 **"모델을 호스팅하고 실행한다"**는 좁은 의미의 서빙이었다.
실제 대규모 프로바이더는 그 위에 다음을 얹어야 한다.

```
인증 · 과금 정책 · 리소스 관리 · 네트워킹 · 최적화 · 실험(A/B) · 관측성 · 온콜 지원
```

### 7계층 레퍼런스 아키텍처

| 레이어 | 역할 | 핵심 난제 |
|---|---|---|
| **1. Public API** | 외부 인터페이스. 네트워킹·인증·과금·rate limiting·라우팅 | 고동시성, 공정 사용·수익화, 저지연 글로벌 접근, 보안 |
| **2. Resource Management** | CPU/GPU/메모리/디스크/네트워킹을 리전 전반에서 관리, 예산·비용 배분 | 용량 계획, GPU 활용률, 고객 우선순위 |
| **3. Model Selection & Orchestration** | 요청마다 어떤 모델을 쓸지 결정 (정확도/지연/비용 균형) | 비용-품질 트레이드오프, 로드밸런싱, speculative decoding |
| **4. Distributed Serving** | 대형 모델 분산 호스팅 + 분산 캐싱(KV, prompt) | 하드웨어 한계, 멀티GPU/멀티노드 조율, 캐싱 효율 |
| **5. Core Inference** | 실제 추론 실행. vLLM/Triton/TensorRT-LLM/SGLang + FlashAttention/GEMM/PagedAttention | 콜드 스타트, hot model 스케일링 |
| **6. Model Optimization** | 재학습 없이 성능·효율 향상 | 양자화, LoRA, prefix caching, chunked prefill |
| **7. Model** | 학습된 모델을 서빙 시스템에 공급, 분류·추적·버전 관리 | 파이프라인 통합 |

레이어 3의 실무적 예시가 이해를 돕는다.

```
"1+1은?"          → 거대 reasoning 모델을 쓸 이유가 없다 → 경량 모델로 라우팅
복잡한 다단계 추론  → 대형 모델
```

Quality / Latency / Cost 세 축의 트레이드오프를 **요청 단위로** 판단하는 자리다.

레이어 4는 앞 노트의 NVLink 내용과 직결된다.

```
Model Weight = 160 GB
GPU VRAM     =  80 GB
→ 단일 GPU 불가 → 멀티 GPU → (그래도 부족하면) 멀티 노드
   그리고 노드를 넘는 순간 통신이 18배 느려진다
```

### 이건 조직 설계 문제이기도 하다

이 아키텍처링이 어려운 이유는 기술보다 **조직**에 가깝다.

- 서로 다른 책임을 가진 여러 팀이 병목이나 과도한 상호 의존성 없이 하나의 진화하는 시스템에
  동시에 기여할 수 있어야 한다
- 빠른 반복과 안정성 사이의 균형
- 이 모든 걸 비용·신뢰성·사용자 경험이라는 제약 안에서

> 서빙 아키텍처를 잘 설계한다는 건 "레이턴시를 줄이는 기술" 문제만이 아니라,
> **"어떤 레이어를 누가 소유하고, 경계를 어떻게 그어야 팀들이 서로 발목 잡지 않는가"** 라는
> 조직 설계 문제다.

계층을 나누는 진짜 이유가 여기 있다. API·오케스트레이션·추론·최적화 같은 관심사를 분리하면
**팀들이 독립적으로 혁신하면서도 하나의 플랫폼에 기여**할 수 있다.

### 오픈소스 스택으로 구현하기

교재는 단정적으로 말한다. **Kubernetes 위에 올려라.**

이유는 두 가지다.

1. 핵심 기능 자체 — 컨테이너 배포·스케일링·관리 자동화
2. **거대한 생태계** — 메트릭, 로깅, 네트워킹, 인가, 하드웨어 관리가 이미 붙어 있다

Kubernetes 한 층이 사실상 **레이어 1의 라우팅/네트워킹 부분과 레이어 2 전체**를 실제 컴포넌트로
구현해준다. AWS·GCP·Azure 같은 클라우드 프로바이더와 파운데이션 모델 벤더 양쪽 모두가
이걸 백본으로 채택한 이유이기도 하다.

#### Public API 레이어 구현

앞서 정리한 4가지 난제가 실제 코드/설정으로 어떻게 대응되는지 확인한다.

```python
@app.post("/v1/chat/completions")
async def chat(req: ChatReq, idp=Depends(require_auth)):   # 인증을 선언적으로 주입
    await rate_limit(idp["tenant"])                        # 테넌트별 쿼터
    ep = choose_endpoint(req.model, idp["tenant"])         # 모델 선택 + 라우팅
```

인증은 **JWT 또는 API 키** 두 경로를 지원하되, 둘 다 최종적으로 **"테넌트 식별"이라는 같은
목적지로 수렴**한다. 이후의 쿼터 강제·트래픽 라우팅·모델 선택이 전부 이 `tenant` 값 하나에
의존하기 때문이다.

rate limiting이 **두 레이어에 이중으로** 걸리는 것도 눈여겨볼 패턴이다.

| 위치 | 성격 | 목적 |
|---|---|---|
| Ingress (`limit-rps`) | 거친 방어선 | 애플리케이션에 닿기 전에 대량 어뷰징·DDoS성 트래픽을 값싸게 차단 |
| 애플리케이션 (`rate_limit(tenant)`) | 정교한 쿼터 | 테넌트별 계약 강제 |

전형적인 **defense in depth**다.

라우팅 정책에서도 순서가 의미를 갖는다.

```
1. 모델/별칭 조회 (없으면 404)
2. 가중치 기반 카나리 라우팅   ← 테넌트 오버라이드보다 "먼저" 평가
3. 테넌트별 오버라이드
4. 기본 라우트
```

카나리를 먼저 평가하는 건 의도적이다. 전용 라우팅을 가진 테넌트까지 포함해 **전체 트래픽에서
무작위 샘플**을 뽑아야 카나리 배포가 특정 고객군에 편향되지 않는다.

#### Ray Serve — 모델 호스팅 레이어

인스턴스 확장과 네트워크 관리 부담을 덜고 싶으면 **Ray Serve**를 쓴다.
Part 1에서 손으로 만든 것들이 프레임워크 기능으로 바뀌는 대응 관계가 명확하다.

| Part 1에서 직접 만든 것 | Ray Serve의 대응 |
|---|---|
| ModelManager (OrderedDict LRU, 최대 N개) | `@serve.multiplexed(max_num_models_per_replica=N)` |
| 요청 시 없으면 새로 로드 | `await self.load_model(model_id)` |
| `models.json` (model_id → 모델명) | `MODEL_REGISTRY` dict |
| 요청 body의 `model_id`로 라우팅 | HTTP 헤더 `ray_serve_multiplexed_model_id` |

단일 모델 호스팅은 데코레이터 한 줄로 끝난다.

```python
@serve.deployment(
    name="qwen_vllm",
    num_replicas=3,                                    # 서비스 인스턴스 3개
    ray_actor_options={"num_cpus": 0.5, "num_gpus": 1},  # 각각 GPU 1장
)
class QwenVLLM:
    def __init__(self):
        self.engine = AsyncLLMEngine.from_engine_args(args)   # vLLM 엔진에 위임
```

Part 1의 ModelWorker가 하던 일(모델 한 번 로드 → 요청마다 generate)과 개념적으로 동일하지만,
**수동 배칭·큐잉을 vLLM의 AsyncLLMEngine에 통째로 위임**한다는 게 다르다.
그리고 Kubernetes HPA YAML(minReplicas/maxReplicas/임계값)이 하던 일을 파이썬 데코레이터가 대체한다.

멀티 모델은 `@serve.multiplexed`가 **LRU 캐싱과 축출을 프레임워크 기능으로 내장**해준다.
Part 1에서 손으로 짠 ModelManager 로직이 그대로 프레임워크에 들어있다.

#### Ray 클러스터 구조

```
Ray Cluster
├─ Head Node    워커와 같은 프로세스 + 클러스터 관리 싱글톤
│                 ├─ Autoscaler
│                 ├─ GCS (Global Control Store, 메타데이터)
│                 └─ Driver process
└─ Worker Node  관리 기능 없이 Task/Actor 실행만 담당
```

Kubernetes 위에서 이를 운영하는 공식 방식이 **KubeRay**(Operator)다. CRD 3+1개를 설치한다.

| CRD | 용도 |
|---|---|
| `RayCluster` | Head/Worker 파드로 구성된 Ray 클러스터 자체의 생명주기 |
| `RayJob` | 클러스터를 만들어 단일 Job 실행 후 정리 (배치 작업) |
| **`RayService`** | RayCluster + 그 위의 Ray Serve 앱을 함께 관리. 무중단 업그레이드, 헬스체크 기반 HA |
| `RayCronJob` | RayJob을 크론 스케줄로 반복 |

**서빙이 목적이면 RayService를 쓰는 게 맞다.**

| | RayCluster | RayService |
|---|---|---|
| 범위 | Ray 클러스터만 | 클러스터 **+ Serve 앱** |
| Serve 라이프사이클 | 사용자가 직접 `serve run`, 장애 시 수동 복구 | 컨트롤러가 관리, 설정 변경 시 무중단 in-place 업데이트 |
| 헬스체크 | k8s가 Serve 상태를 모름 | `/-/routes` 기반으로 k8s가 Serve 상태까지 인지 |
| 적합 | 배치 작업, 대화형 실험 | **프로덕션 모델 서빙** |

RayService를 선언하면 **RayCluster 설정이 그 안에 포함**되므로, 둘을 따로 배포할 필요가 없다.

---

## Part 4. Build or Buy — 이분법이 아니라 스펙트럼

### AWS 기준 6가지 옵션

| 단계 | 방식 | 자유도 | 운영 부담 | 무엇을 직접 가져오나 |
|---|---|---|---|---|
| 1 | **Bedrock** | 낮음 | 매우 낮음 | 아무것도 (API만 호출) |
| 2 | **SageMaker JumpStart** | 조금 높음 | 낮음 | 인스턴스 타입 선택 |
| 3 | **Bring Your Own Model** (DLC) | 중간 | 중간 | **모델 아티팩트** |
| 4 | **Bring Your Own Code** (Script Mode/LMI) | 높음 | 높음 | **추론 코드** |
| 5 | **Bring Your Own Serving Image** | 매우 높음 | 매우 높음 | **컨테이너 전체** |
| 6 | **Build Your Own Infrastructure** | 최고 | 최고 | **인프라 전체** (EKS + vLLM/Triton/Ray) |

#### 자동차 비유

```
1 Bedrock         택시 — 목적지만 말한다, 차량 관리 없음
2 JumpStart       렌터카 — 차종은 고르지만 준비된 것 중에서
3 BYO Model       엔진은 내 것, 차량 플랫폼은 AWS (DLC 컨테이너)
4 BYO Code        컨테이너는 AWS 제공, 추론 코드는 직접
5 BYO Image       Docker/CUDA/Python/vLLM까지 직접 구성
6 BYO Infra       EKS + GPU 노드 + 런타임 + 관측성 전부 직접
```

#### 각 단계가 여는 것과 닫는 것

**Bedrock**은 서버리스 관리형이다. GPU 프로비저닝·모델 로딩·추론 실행·결과 반환을 전부
AWS가 감춘다. 과금은 **요청/토큰 단위**. 대신 인스턴스 타입 선택, 컨테이너 커스터마이징,
**자체 모델 배포가 모두 불가능**하다.

**JumpStart**의 결정적 차이는 **엔드포인트가 내 계정에 뜬다**는 것이다.
과금이 **시간당 인스턴스 요금**으로 바뀌고, 인스턴스 타입을 고를 수 있다.
다만 CUDA/PyTorch 버전, 전처리/배칭 동작, 요청 스키마는 여전히 고정이다.

**DLC(옵션 3)** 는 "bring-your-own-model이지 bring-your-own-code는 아니다"가 핵심이다.
학습된 모델은 직접 가져오되 서빙 코드는 컨테이너가 처리한다.
JumpStart가 자동 선택하던 컨테이너를 **직접 지정**한다는 게 차이다.

여기서 SageMaker의 **HTTP 계약**이 드러난다.

```
포트 8080에서
  POST /invocations   추론
  GET  /ping          헬스체크
기본 타임아웃 60초
```

사실상 AWS가 강제하는 미니 Public API 레이어다. 옵션 5까지 가도 이 계약은 유지된다.

**LMI 컨테이너(옵션 4)** 의 설정을 보면 앞 노트 내용과 곧바로 연결된다.

```
OPTION_SERVING_LOADER=vllm            ← 우리가 다룬 그 vLLM
OPTION_TENSOR_PARALLEL_DEGREE=4       ← 텐서 병렬화
OPTION_MAX_ROLLING_BATCH_SIZE=128     ← continuous batching
```

**관리형 서비스 뒤에서 도는 엔진이 결국 같다**는 걸 알아보는 것 — 이게 이 파트가 말하는
"벤더 기능 해독 능력"이다.

**옵션 6**만이 SageMaker의 엔드포인트 관리까지 벗어난다. 옵션 1~5는 컨테이너 내부를 100%
통제해도(옵션 5) 배포·스케일링·엔드포인트는 여전히 SageMaker가 관리한다.

### 언제 옮겨야 하는가 — 손익분기점 계산

교재의 예시로 직접 계산해보면 판단 기준이 손에 잡힌다.

```
Bedrock            입력 100만 토큰당 $0.10
전용 인스턴스        시간당 $1.172

시간당 동등 토큰 수 = ($1.172 ÷ $0.10) × 1,000,000 = 11,720,000 토큰
초당으로 환산       = 11,720,000 ÷ 3,600 ≈ 3,256 tokens/s
```

**초당 약 3,256 토큰 이상을 꾸준히 처리한다면** 전용 인스턴스 직접 호스팅이 더 싸진다.
(출력 토큰 과금, 인스턴스 유휴 시간 등은 단순화해서 제외한 근사치다.)

흔한 이동 경로는 이렇다.

```
아이디어 검증  → Bedrock (옵션 1)
사용량 증가    → JumpStart/DLC (옵션 2~3, 내 계정에 호스팅)
처리량↑ 지연↓  → 옵션 4~6
```

### 직접 만들 줄 아는 것이 왜 중요한가

클라우드를 쓰더라도 밑바닥을 알아야 하는 이유 4가지.

1. **벤더 기능 해독** — 벤더가 제공하는 옵션이 배칭/캐싱/양자화 중 무엇에 대응하고 한계가 어디인지
   알아본다 (`OPTION_SERVING_LOADER=vllm`, `@serve.multiplexed`를 보고 바로 알아채는 능력)
2. **트레이드오프 정량화** — 위의 손익분기점 계산 같은 판단
3. **80/20 커스터마이징** — 기본값 80%는 그대로 쓰고, 깊은 통제가 필요한 20%만 정밀 교체
4. **디버깅** — 벤더 솔루션은 블랙박스처럼 동작하고, 내부 로직 문서가 부족하며, 로깅도 다 안 잡힌다

### 선택 전략

| 상황 | 선택 |
|---|---|
| SLO 충족 + 비용 허용 + 기능 개발 속도가 최우선 | **벤더 관리형 유지** |
| 일부 엔드포인트에만 특별한 배칭·라우팅·튜닝·테넌트 격리 필요 | **하이브리드** |
| 하드웨어·런타임·네트워킹 직접 제어 필요, 비용이 커서 세밀한 튜닝 필요 | **BYO** |
| 트래픽이 낮게 안정되거나 시스템 복잡성이 성과를 못 냄 | **벤더 관리형으로 회귀** |

> 성공적인 전략은 **유연**하다. 안정적인 API와 공유 텔레메트리를 유지하고, 효과적인 부분만
> 맞춤화하며, 제품·트래픽·제약이 변하면 **스펙트럼 상의 위치도 바뀔 것을 예상**해야 한다.

---

## Part 5. 성능 측정 — 판단의 근거

아키텍처 설계만으로는 부족하다. 위의 모든 결정은 **측정 가능한 결과로 평가**되어야 한다.

### Latency 3종

| 지표 | 정의 | 대응 단계 |
|---|---|---|
| **E2E latency** | 요청 수신 ~ 전체 응답 완료까지. 큐 대기·네트워크·라우팅·확장 오버헤드 포함 가능 | 전체 |
| **TTFT** (Time To First Token) | 요청 수신 ~ 첫 토큰 발행까지 | **Prefill** |
| **ITL / TPOT** (Inter-Token Latency / Time Per Output Token) | 첫 토큰 이후 토큰 하나당 생성 시간 | **Decode** |

ITL이 일정하고 총 생성 토큰이 N개라면:

```
E2E latency = TTFT + ITL × (N − 1)

TTFT = 입력 토큰화 + prefill + 첫 토큰 디코드 + 디토크나이즈  ≈ prefill + 첫 토큰 디코드
ITL  = 새 토큰 하나 디코드 (+ 디토크나이즈 차분)              ≈ 새 토큰 하나 디코드
```

토큰화/디토크나이즈는 모델 추론 대비 미미해서 보통 무시한다.

#### 유스케이스별 우선순위

| 상황 | 최우선 지표 | 이유 |
|---|---|---|
| **에이전트 워크플로우** | **E2E** | 다음 단계가 앞 단계의 **완성된 전체 출력**을 기다린다. 첫 토큰만 빨라도 소용없다 |
| **챗봇 (스트리밍)** | **TTFT** | 응답성 그 자체 |
| **출력이 매우 긴 경우** | **ITL** | 높으면 "느릿느릿하다"고 체감된다 |

에이전트 케이스가 특히 중요하다. Part 2에서 본 체이닝 구조에서는 각 단계의 E2E가
다음 단계를 **블로킹**한다. 그래서 에이전트 워크로드는 지연이 곱셈으로 누적된다.

### Throughput 2종

| 지표 | 정의 | 함정 |
|---|---|---|
| **RPS / RPM** | 초당·분당 처리 요청 수 | **요청 패턴에 민감**. 입출력 길이와 동시 사용자 수에 크게 좌우되어, 트래픽 패턴이 다른 두 워크로드 간 직접 비교는 무의미 |
| **TPS** | 초당 생성 **출력** 토큰 수 | **입력 토큰이나 입출력 합산이 아니다.** 이 구분을 놓치면 벤치마크를 오해한다 |

#### TPS는 부풀릴 수 있다

- **입력 길이를 줄이면** TTFT가 크게 감소 → 요청당 작업량이 줄어 TPS가 실제보다 높아 보인다
- **배치를 크게 하거나 입출력 길이를 균일하게** 맞추면 GPU 유휴가 줄어 TPS가 개선된다

그래서 벤더가 발표하는 TPS를 볼 때는 **"어떤 조건에서 측정했는가"** 를 따져야 한다.

> 어디선가 과장된 성능 수치가 나왔다고 해서 그대로 믿지 말 것.
> 정확히 어떤 기준 환경과 조건에서 나온 값인지, 벤치마크 내용을 **해독하는 능력**이 중요하다.

Part 4의 "벤더 기능 해독" 능력이 실전에서 쓰이는 지점이 바로 여기다.

### 측정 모범 사례 9가지

1. **지연시간 vs 처리량 트레이드오프 파악** — 배치 워크로드는 처리량, 인터랙티브는 지연시간
2. **"충분히 좋은" 목표 설정** — 1초를 0.5초로 줄이는 게 체감상 무의미하면 그 노력을 비용 최적화로
3. **E2E를 TTFT/ITL로 분해** — 출력 길이가 제한적이면 TTFT가 ITL보다 중요할 수 있다
4. **실제 트래픽 패턴 시뮬레이션** — 긴 프롬프트+짧은 답변과 그 반대는 완전히 다르게 동작한다.
   그리고 트래픽은 절대 균일하지 않다
5. **실험의 일관성** — 한 번에 한 노브만. 여러 개를 동시에 바꾸면 효과를 분리할 수 없다
6. **하드웨어 활용률 모니터링** — 병목이 모델 때문인지 하드웨어 한계 때문인지 구분
7. **지표를 인위적으로 부풀리지 말 것**
8. **프로덕션에서 지속 모니터링** — 배포 후 사용자 행동 변화로 인한 급증까지
9. **테스트 스위트 주기적 재실행** — 회귀 방지, 스케일링 테스트, A/B 테스트

---

## 스스로 답해본 질문

### 서빙 엔진

**Q1. 왜 API server / LLM engine / Workload manager / Model executor / Model worker를
굳이 별도 프로세스·컴포넌트로 분리하는가? GPU와 CPU 작업을 격리하는 이유는?**

이유는 두 겹이다.

**GPU 격리(프로세스 분리)의 이유는 경제성**이다. 토크나이징·전처리·후처리는 CPU 작업인데,
이게 GPU와 같은 프로세스/스레드에 있으면 GPU가 그 작업이 끝날 때까지 논다. GPU는 가장 비싼
자원이라 유휴 시간이 곧 손실이다. Model Worker를 GPU 전용 프로세스로 떼어내고 나머지를 CPU에
남기면, GPU는 계산에만 CPU는 요청 관리에만 집중한다. 부수 효과로 **API/엔진 계층을 GPU 없는
노드에 배치할 수 있게 되고**, 모델 크래시가 API 프로세스를 죽이지 않는 장애 격리도 따라온다.

**컴포넌트 분리(모듈 분리)의 이유는 변경 지점을 국소화**하기 위해서다. 각 컴포넌트가 맡는 축이 다르다.

```
API Server        프로토콜(HTTP/SSE)이 바뀔 때
Workload Manager  스케줄링 정책(FIFO → dynamic → continuous)이 바뀔 때
Model Executor    IPC 방식이나 워커 수가 바뀔 때
Model Worker      프레임워크나 모델이 바뀔 때
```

실제로 이 분리 덕분에 백엔드를 통째로 vLLM으로 바꿔도 API 계약은 그대로 유지된다.
Part 1 도입부에서 말한 "나중에 서빙 프레임워크로 백엔드를 교체할 수 있게 구조를 분리한다"가 이것이다.

**Q2. 배칭과 스트리밍은 각각 어떤 문제를 해결하며, 두 기능이 어떻게 공존하는가?**

둘은 서로 **다른 방향의 문제**를 푼다.

| | 해결하는 문제 | 최적화 대상 |
|---|---|---|
| **배칭** | GPU가 프롬프트 하나씩만 처리해 활용률이 낮다 | **처리량** (서버 쪽) |
| **스트리밍** | 전체 생성이 끝날 때까지 사용자가 아무것도 못 본다 | **체감 지연** (사용자 쪽) |

배칭은 개별 요청의 지연을 오히려 늘릴 수 있고(배치가 찰 때까지 대기), 스트리밍은 총 연산량을
줄이지 않는다. 그래서 상충하는 것처럼 보이지만 실제로는 **층이 다르다.**

공존이 가능한 이유는 **실행 단위를 토큰으로 쪼개고, 요청 경계를 ID로 복원**하기 때문이다.

```
GPU 쪽:   여러 시퀀스를 묶어 한 번의 forward pass로 각각의 "다음 토큰 1개"를 동시 생성
            → 배칭의 이득
사용자 쪽: 생성된 토큰을 Sequence ID로 구분해 각자의 이벤트 큐로 밀어 넣고 SSE로 즉시 전송
            → 스트리밍의 이득
```

즉 **"안에서는 배치로 계산하고, 밖으로는 요청별로 흘려보낸다."**
이걸 가능하게 하는 장치가 세 개다 — 프롬프트마다의 **Sequence 객체(고유 ID)**,
요청별 **이벤트 큐**, 그리고 배치를 토큰 단위로 돌리는 **백그라운드 루프**.

여기에 배치 슬롯이 고정 크기라, 완료된 시퀀스가 빠지면 대기 중이던 요청이 그 자리를 채운다.
이 회전을 요청 완료 시점까지 기다리지 않고 매 스텝 하게 만든 것이 continuous batching이다.

**Q3. 멀티 모델 서빙에서 리소스를 어떻게 동적으로 관리하며, 비용 효율과 지연시간·확장성 사이의
트레이드오프는 무엇인가?**

동적 관리는 **lazy loading + 캐시 + LRU eviction** 세 가지로 이루어진다.

```
요청 도착 → 캐시에 있나?
            ├─ 있음: 워커 재사용 (최근 사용으로 갱신)
            └─ 없음: 메타데이터 조회 → 워커 생성 → 모델 로드
                      캐시가 가득 찼으면 가장 오래 안 쓴 모델을 축출 후 등록
```

축출이 곧 메모리 회수라, **"동시에 몇 개까지 올려둘 것인가"** 라는 상한 하나가 비용과 성능을
동시에 결정한다.

트레이드오프는 이렇게 갈린다.

| | 비용 최적화 | 지연시간 최적화 |
|---|---|---|
| 방식 | 공유 인스턴스 + 온디맨드 로딩 + bin-packing | 모델별 전용 그룹 + 사전 프로비저닝 |
| 얻는 것 | 서버 수 최소화, 유휴 자원 최소화 | 콜드 스타트 제거, 모델별 독립 확장 |
| 잃는 것 | 콜드 스타트, 반응형이라 급증에 취약, 운영 복잡도 | 트래픽 적은 모델도 자원 점유 → 과다 프로비저닝 |
| 유리한 상황 | 모델은 많은데 동시 사용률이 낮을 때 | 수요가 꾸준하고 예측 가능할 때 |

결국 판단 기준은 **"어떤 모델이 뜨거워질지 미리 알 수 있는가"** 다. 예측 가능하면 전용 자원이
효율적이고, 예측 불가능하면 공유 + 동적 로딩이 낫다. 실무에서는 hot 모델만 전용으로 빼고
나머지는 공유 풀에 두는 혼합 형태로 가는 경우가 많다.

### 프로덕션 아키텍처

**Q1. 에이전트 워크플로우가 등장하면서 서빙 요구사항이 근본적으로 어떻게 달라지는가?**

단일 요청-응답에서는 **요청 1건 = 추론 1회**였고, 비용과 지연을 요청 단위로 예측할 수 있었다.
에이전트에서는 요청 1건이 **제어 루프 안에서 여러 번의 LLM 호출 + 임베딩 + 도구 실행**으로
분기한다. 세 가지가 달라진다.

- **부하가 곱해진다** — 플래닝 1회 + 액션마다 1~2회씩. 사용자는 질문 하나를 던졌는데 백엔드는
  여러 번의 왕복을 감당한다
- **지연이 누적된다** — 단계가 순차 의존이라 각 단계의 tail latency가 더해진다.
  체이닝 구조에서는 앞 단계의 **완성된 전체 출력**을 기다려야 해서 TTFT 최적화가 무의미하고
  E2E가 지배적이다
- **트래픽이 동적이 된다** — 계획이 LLM의 판단으로 매번 달라지므로 호출 횟수와 컨텍스트 길이를
  사전에 예측할 수 없다. 정적 용량 계획이 어려워진다

여기에 턴이 쌓이며 컨텍스트가 길어져 KV 캐시 부담도 계속 커진다. 그래서 서빙은 모델 실행을
넘어 **오케스트레이션·메모리 관리·캐시 재사용(prefix caching, CAG)** 까지 책임지게 된다.

**Q2. 계층형 아키텍처의 각 레이어는 어떤 역할과 트레이드오프를 가지며, 오픈소스로는 어떻게 구현하는가?**

7계층의 역할은 Part 3의 표에 정리했고, 트레이드오프의 축은 레이어마다 다르다.

```
L1 Public API      보안·공정성 ↔ 지연  (인증/rate limit이 홉을 늘린다)
L2 Resource Mgmt   활용률 ↔ 여유       (빈틈없이 채우면 버스트에 취약)
L3 Orchestration   품질 ↔ 비용·지연     (큰 모델은 정확하지만 비싸고 느리다)
L4 Distributed     용량 ↔ 통신비용      (노드를 넘으면 18배 느린 링크를 탄다)
L5 Core Inference  범용성 ↔ 최적화      (여러 프레임워크 지원 ↔ 커널 수준 튜닝)
L6 Optimization    효율 ↔ 정확도        (양자화가 대표적)
```

오픈소스 매핑은 **Kubernetes가 L1의 라우팅/네트워킹과 L2 전체**를 담당하고,
**Ray Serve가 L5의 모델 호스팅**(replica 선언, multiplexing, 오토스케일링)을,
**vLLM이 L5~L6의 실제 추론과 커널 최적화**를 맡는 구조다. L1의 인증·테넌시·rate limiting과
L3의 모델 선택 로직은 FastAPI 미들웨어 등으로 직접 구현하는 영역으로 남는다.

**Q3. 빌드 vs 클라우드를 어떤 기준으로 선택하고, 그 타당성은 어떤 지표로 판단하는가?**

선택 기준은 네 가지 축이다 — **사용 편의성, 비용, 커스터마이징 정도, 트래픽 규모**.
이분법이 아니라 6단계 스펙트럼이고, 대부분의 팀은 중간(벤더 플랫폼 + 표적 커스터마이징)에 있다.

타당성 판단은 두 종류의 데이터로 한다.

- **비용** — 손익분기점 계산. 위 예시 기준 초당 약 3,256 토큰이 Bedrock과 전용 인스턴스가
  같아지는 지점이고, 이걸 넘어서면 직접 호스팅이 싸진다
- **성능** — 유스케이스에 맞는 지표로 SLO 충족 여부를 확인한다.
  에이전트면 E2E, 챗봇이면 TTFT, 긴 출력이면 ITL. 처리량은 RPS/TPS로 보되
  **TPS는 출력 토큰만 센다**는 점과 측정 조건에 따라 부풀려질 수 있다는 점을 감안한다

이 판단은 **한 번으로 끝나지 않는다**. 트래픽과 제약이 변하면
스펙트럼 상의 위치도 옮겨야 하고, 그래서 안정적인 API와 공유 텔레메트리를 유지하는 게 중요하다.

---

## 정리

앞 노트가 "왜 이런 최적화가 필요한가"였다면, 이번 노트는 **"그 최적화를 누가 어디서 담당하는가"** 였다.

```
서빙 엔진 안쪽
  요청 하나를 처리하려면 API·큐·스케줄러·워커가 필요하다
    → 배칭하면 GPU 효율이 오르지만 사용자 경계가 사라진다 → Sequence ID로 복원
      → 스트리밍하면 체감이 좋아지지만 백엔드 비용은 그대로
        → 이 전부를 vLLM이 이미 해준다 (수백 줄 → 20줄)
          → 그래서 "쓰라"가 아니라 "무엇을 대신해주는지 알고 쓰라"

서빙 엔진 바깥
  에이전트가 오면서 요청 1건 = 추론 1회라는 전제가 깨진다
    → 부하·지연·트래픽 패턴이 전부 달라진다
      → 그래서 7계층으로 관심사를 분리하고 (기술 문제이자 조직 문제)
        → K8s + Ray Serve + vLLM으로 구현하거나, 클라우드 6단계 중 한 지점을 고른다
          → 그 선택이 옳은지는 E2E/TTFT/ITL과 RPS/TPS로 검증한다
```

두 파트가 만나는 지점이 명확하다. **Part 1에서 손으로 만들어본 컴포넌트들이 Part 2·3에서는
Ray Serve의 데코레이터 한 줄, LMI 컨테이너의 환경변수 한 줄로 등장한다.**
그걸 알아보는 능력이 곧 벤더 옵션을 평가하는 능력이다.

다음 노트([LLM 서빙의 병목과 필수 최적화 기법](serving-bottlenecks-and-optimization.md))에서는 서빙의 핵심 과제와 필수 최적화 방법을 다룬다.

---

## 이어서 볼 것

- Speculative decoding — 레이어 3에서 이름만 나왔고, 교재 후반부(고급 최적화)에서 본격적으로 다뤄진다
- KV 캐시 어웨어 라우팅과 분산 KV 캐시 공유 풀 (P/D 분리와 직결)
- MCP의 도구 정의·권한 경계 설계
- 청킹 전략이 검색 품질과 서빙 비용에 미치는 영향
- 에이전트 워크플로우의 tail latency를 실제로 어떻게 예산화(budgeting)하는가

---

## 참고 자료

**교재**
- Hands-On LLM Serving and Optimization (2026)
- 예제 코드: https://github.com/orca3/llm-model-inference (`ch03/`, `ch04/`)

**프레임워크 문서**
- vLLM: https://docs.vllm.ai
- NVIDIA Triton Inference Server: https://github.com/triton-inference-server
- Ray Serve: https://docs.ray.io/en/latest/serve/index.html
- Ray Serve LLM: https://docs.ray.io/en/latest/serve/llm/serving-llms.html
- KubeRay: https://docs.ray.io/en/latest/cluster/kubernetes/index.html
- Model Context Protocol: https://modelcontextprotocol.io

**클라우드**
- Amazon Bedrock: https://docs.aws.amazon.com/bedrock/
- Amazon SageMaker AI: https://docs.aws.amazon.com/sagemaker/
- AWS Deep Learning Containers (DLC): https://github.com/aws/deep-learning-containers

**참고 발표**
- Simplifying Advanced AI Model Serving on Kubernetes Using Helm Charts (KubeCon 2025)
